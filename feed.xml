<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-08-05T11:57:51+00:00</updated><id>/feed.xml</id><title type="html">Saptarshi Nag</title><subtitle>Embedded System Engineer</subtitle><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><entry><title type="html">Kalman Filter In Embedded</title><link href="/blog/2026-07-03-kalman-filter-in-embedded/" rel="alternate" type="text/html" title="Kalman Filter In Embedded" /><published>2026-07-03T00:00:00+00:00</published><updated>2026-08-05T11:56:49+00:00</updated><id>/blog/kalman-filter-in-embedded</id><content type="html" xml:base="/blog/2026-07-03-kalman-filter-in-embedded/"><![CDATA[]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">I Built a Message Broker for IoT — Here’s How to Run It With Live Grafana Monitoring</title><link href="/blog/2026-07-02-psram-of-esp32/" rel="alternate" type="text/html" title="I Built a Message Broker for IoT — Here’s How to Run It With Live Grafana Monitoring" /><published>2026-07-02T00:00:00+00:00</published><updated>2026-08-05T11:56:49+00:00</updated><id>/blog/psram-of-esp32</id><content type="html" xml:base="/blog/2026-07-02-psram-of-esp32/"><![CDATA[<p><em>A hands-on walkthrough of MetricMQ: what it does differently, how to run the Python demos line by line, and how to watch your messages flow in real time through Prometheus and Grafana.</em></p>

<hr />

<p>I kept running into the same wall building IoT systems: existing brokers are either too heavy for microcontrollers (RabbitMQ needs a full server) or too simple to be trustworthy (basic Redis pub/sub drops messages silently and has no security at the message level). So I built MetricMQ — a lightweight broker with a 16-byte binary protocol, built-in Ed25519 message signing, and a Prometheus metrics endpoint baked directly into the binary.</p>

<p>This post is a complete walkthrough. By the end you’ll have the broker running, all six core features demonstrated through Python, and a live Grafana dashboard showing your message traffic in real time. I’ll break down every piece of code so you understand not just <em>what</em> to run but <em>why</em> it works the way it does.</p>

<hr />

<h2 id="what-a-message-broker-actually-does">What a Message Broker Actually Does</h2>

<p>Before touching any code, it helps to be clear on the problem.</p>

<p>Imagine you have a dozen ESP32 sensors publishing temperature readings, plus a database, a mobile dashboard, and an alert system that all want that data. Without a broker, every sensor needs a direct connection to every consumer:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Without a broker — messy, hard to scale:

  Sensor 1 ──→ Dashboard
  Sensor 1 ──→ Database
  Sensor 1 ──→ Alert System
  Sensor 2 ──→ Dashboard
  Sensor 2 ──→ Database
  Sensor 2 ──→ Alert System
  (12 sensors × 3 consumers = 36 connections to manage)

With MetricMQ — clean hub-and-spoke:

  Sensor 1 ──┐
  Sensor 2 ──┤──→ [ MetricMQ Broker :6379 ] ──→ Dashboard
  Sensor 3 ──┘                               ──→ Database
                                              ──→ Alert System
</code></pre></div></div>

<p>Sensors <strong>publish</strong> messages to named <strong>topics</strong> like <code class="language-plaintext highlighter-rouge">sensors/temperature</code>. Consumers <strong>subscribe</strong> to the topics they care about. The broker handles all the routing. Nobody needs to know about anybody else — you can add a new consumer without touching a single sensor.</p>

<hr />

<h2 id="why-metricmq-and-not-mqtt-or-redis">Why MetricMQ and Not MQTT or Redis?</h2>

<p>Honest comparison:</p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>MQTT</th>
      <th>Redis Pub/Sub</th>
      <th>NATS</th>
      <th>MetricMQ</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Binary size</td>
      <td>~500KB+</td>
      <td>Separate server</td>
      <td>~20MB</td>
      <td><strong>328KB</strong></td>
    </tr>
    <tr>
      <td>ESP32 compatible</td>
      <td>Yes, but TLS is heavy</td>
      <td>Not designed for MCUs</td>
      <td>No</td>
      <td>Yes — 2KB RAM client</td>
    </tr>
    <tr>
      <td>Message-level signing</td>
      <td>No (TLS only)</td>
      <td>No</td>
      <td>No</td>
      <td><strong>Ed25519 per message</strong></td>
    </tr>
    <tr>
      <td>Prometheus built in</td>
      <td>Via sidecar</td>
      <td>Via sidecar</td>
      <td>Via sidecar</td>
      <td><strong>Native, no sidecar</strong></td>
    </tr>
    <tr>
      <td>Sequence numbers</td>
      <td>No</td>
      <td>No</td>
      <td>Yes</td>
      <td>Yes</td>
    </tr>
  </tbody>
</table>

<p>Two things are genuinely different here — not just marketing claims:</p>

<p><strong>Ed25519 per-message signing.</strong> MQTT and Redis rely on TLS for all security. TLS is expensive on microcontrollers — the handshake alone can take hundreds of milliseconds on an ESP32. MetricMQ embeds a 64-byte cryptographic signature directly in every message header. The broker verifies it before routing. A tampered or unsigned message is rejected and counted in Prometheus as <code class="language-plaintext highlighter-rouge">metricmq_signature_failures_total</code>. You can literally watch attacks in your dashboard.</p>

<p><strong>Dual-protocol on the same port.</strong> The broker reads the first byte of every new connection and auto-detects whether it’s the MetricMQ binary protocol or Redis RESP text. An ESP32 using the C++ binary client and a Python script can publish and subscribe on the same topic namespace with zero configuration. This is why the demo uses plain Python sockets — no client library needed.</p>

<hr />

<h2 id="the-architecture-in-one-diagram">The Architecture in One Diagram</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────────────────────────────────────────────────────────┐
│                    Your Machine                              │
│                                                              │
│  ┌─────────────────┐    port 6379    ┌──────────────────┐   │
│  │  Python Demo    │ ──────────────→ │  MetricMQ Broker │   │
│  │  (publishers +  │ ←────────────── │  metricmq-       │   │
│  │   subscribers)  │   messages      │  broker.exe      │   │
│  └─────────────────┘                 └────────┬─────────┘   │
│                                               │ port 9091   │
│                                               │ /metrics    │
│                                      ┌────────▼─────────┐   │
│                                      │   Prometheus      │   │
│                                      │   (Docker)        │   │
│                                      │   scrapes every   │   │
│                                      │   5 seconds       │   │
│                                      └────────┬─────────┘   │
│                                               │ port 9090   │
│                                      ┌────────▼─────────┐   │
│                                      │   Grafana         │   │
│                                      │   (Docker)        │   │
│                                      │   localhost:3001  │   │
│                                      └──────────────────┘   │
└──────────────────────────────────────────────────────────────┘
</code></pre></div></div>

<p>The broker is one binary. It speaks to clients on port <code class="language-plaintext highlighter-rouge">6379</code> and exposes metrics on port <code class="language-plaintext highlighter-rouge">9091</code>. Prometheus scrapes port <code class="language-plaintext highlighter-rouge">9091</code> every 5 seconds and stores the time series. Grafana reads from Prometheus and draws the graphs. Nothing complicated — three processes, three ports.</p>

<hr />

<h2 id="setting-up-the-stack">Setting Up the Stack</h2>

<h3 id="step-1--start-the-metricmq-broker">Step 1 — Start the MetricMQ Broker</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cd</span><span class="w"> </span><span class="nx">C:\path\to\MetricMQ</span><span class="w">
</span><span class="o">.</span><span class="n">\build\Release\metricmq-broker.exe</span><span class="w">
</span></code></pre></div></div>

<p>You should see:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MetricMQ Broker v1.0
Lightweight Message Queue for IoT

Starting broker on port 6379...
Starting metrics server on port 9091...
Broker listening on 0.0.0.0:6379
Metrics server started successfully on http://0.0.0.0:9091/metrics
</code></pre></div></div>

<p>Two things started in one binary: the message broker on port <code class="language-plaintext highlighter-rouge">6379</code> and a Prometheus scrape endpoint on port <code class="language-plaintext highlighter-rouge">9091</code>. Most brokers need a separate exporter process for metrics — MetricMQ does it natively. That <code class="language-plaintext highlighter-rouge">0.0.0.0</code> means it’s listening on all network interfaces, not just localhost, which is what lets the ESP32 connect over WiFi later.</p>

<p>Verify the metrics endpoint is live — open a second terminal and run:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">curl</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="w">
</span></code></pre></div></div>

<p>You’ll see raw Prometheus text format. Each line is a metric the broker is currently tracking:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># HELP metricmq_messages_published_total Total number of messages published
# TYPE metricmq_messages_published_total counter
metricmq_messages_published_total 0

# HELP metricmq_active_connections Current number of active connections
# TYPE metricmq_active_connections gauge
metricmq_active_connections 0

# HELP metricmq_publish_latency_seconds Publish operation latency in seconds
# TYPE metricmq_publish_latency_seconds histogram
metricmq_publish_latency_seconds_bucket{le="0.001"} 0
metricmq_publish_latency_seconds_bucket{le="0.005"} 0
...
</code></pre></div></div>

<p>Right now everything is zero — no clients connected yet. These numbers will change as the demo runs.</p>

<h3 id="step-2--create-the-prometheus-config">Step 2 — Create the Prometheus Config</h3>

<p>Prometheus needs to know where to scrape metrics from. Create a config file:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">mkdir</span><span class="w"> </span><span class="nx">C:\metricmq-monitoring</span><span class="w">

</span><span class="sh">@"
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'metricmq'
    static_configs:
      - targets: ['host.docker.internal:9091']
"@</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Out-File</span><span class="w"> </span><span class="nt">-FilePath</span><span class="w"> </span><span class="nx">C:\metricmq-monitoring\prometheus.yml</span><span class="w"> </span><span class="nt">-Encoding</span><span class="w"> </span><span class="nx">utf8</span><span class="w">
</span></code></pre></div></div>

<p>Two decisions worth explaining here:</p>

<p><code class="language-plaintext highlighter-rouge">scrape_interval: 5s</code> — Prometheus fetches fresh metrics every 5 seconds. This is tight enough to watch individual demo runs in real time. In production you’d use 15s or 30s to reduce storage.</p>

<p><code class="language-plaintext highlighter-rouge">host.docker.internal</code> — This is Docker’s built-in DNS name that resolves to your Windows host machine from inside a container. Without this, a container can’t reach <code class="language-plaintext highlighter-rouge">localhost:9091</code> because “localhost” inside Docker means the container itself, not your PC.</p>

<h3 id="step-3--start-prometheus-and-grafana">Step 3 — Start Prometheus and Grafana</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Prometheus</span><span class="w">
</span><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">-d</span><span class="w"> </span><span class="nt">--name</span><span class="w"> </span><span class="nx">prometheus</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">9090:9090</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">--add-host</span><span class="o">=</span><span class="n">host.docker.internal:host-gateway</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="s2">"C:\metricmq-monitoring\prometheus.yml:/etc/prometheus/prometheus.yml"</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">prom/prometheus</span><span class="w">

</span><span class="c"># Grafana</span><span class="w">
</span><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">-d</span><span class="w"> </span><span class="nt">--name</span><span class="w"> </span><span class="nx">grafana</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">3001:3000</span><span class="w"> </span><span class="nx">grafana/grafana</span><span class="w">
</span></code></pre></div></div>

<p>Breaking down the Prometheus command:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">-d</code> — run in the background (detached)</li>
  <li><code class="language-plaintext highlighter-rouge">--name prometheus</code> — give the container a name so you can reference it later</li>
  <li><code class="language-plaintext highlighter-rouge">-p 9090:9090</code> — map container port 9090 to your machine’s port 9090</li>
  <li><code class="language-plaintext highlighter-rouge">--add-host=host.docker.internal:host-gateway</code> — tells the container that <code class="language-plaintext highlighter-rouge">host.docker.internal</code> means your actual Windows machine</li>
  <li><code class="language-plaintext highlighter-rouge">-v "..."</code> — mounts your config file into the container at the path Prometheus expects</li>
</ul>

<p>Grafana runs on port <code class="language-plaintext highlighter-rouge">3001</code> on your machine (mapped from container port <code class="language-plaintext highlighter-rouge">3000</code>) because something else was already using <code class="language-plaintext highlighter-rouge">3000</code>.</p>

<p>Confirm both are running:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">docker</span><span class="w"> </span><span class="nx">ps</span><span class="w">
</span></code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONTAINER ID   IMAGE             PORTS                    NAMES
9ab6a2f4c802   grafana/grafana   0.0.0.0:3001-&gt;3000/tcp   grafana
9fa970993610   prom/prometheus   0.0.0.0:9090-&gt;9090/tcp   prometheus
</code></pre></div></div>

<p>Check Prometheus is actually scraping MetricMQ — open <code class="language-plaintext highlighter-rouge">http://localhost:9090/targets</code>. You want to see the <code class="language-plaintext highlighter-rouge">metricmq</code> job with a green <strong>UP</strong> badge and a recent “Last scrape” timestamp.</p>

<h3 id="step-4--wire-grafana-to-prometheus">Step 4 — Wire Grafana to Prometheus</h3>

<p>Go to <code class="language-plaintext highlighter-rouge">http://localhost:3001</code>, log in with <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code>, then:</p>

<p><strong>Connections → Data sources → Add data source → Prometheus</strong></p>

<p>Set the connection URL to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://host.docker.internal:9090
</code></pre></div></div>

<p>Click <strong>Save &amp; test</strong>. You should see “Successfully queried the Prometheus API.” If you see a connection error, check that the Prometheus container is running with <code class="language-plaintext highlighter-rouge">docker ps</code>.</p>

<hr />

<h2 id="the-wire-protocol--whats-actually-happening-under-the-hood">The Wire Protocol — What’s Actually Happening Under the Hood</h2>

<p>Before looking at the Python demo, it helps to understand how clients talk to MetricMQ. Every message is wrapped in a <strong>16-byte binary header</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Byte position:  0      1      2-9        10-11      12-15
                ┌──────┬──────┬──────────┬──────────┬──────────┐
                │ Ver  │ Cmd  │ Sequence │ TopicLen │ PayloadLen│
                │ (1B) │ (1B) │  (8B)    │   (2B)   │   (4B)   │
                └──────┴──────┴──────────┴──────────┴──────────┘
                followed by: [Topic bytes] [Payload bytes]
</code></pre></div></div>

<p>This fixed-size header is one of MetricMQ’s core design decisions. Compare it to MQTT’s variable-length encoding — MQTT uses a complex multi-byte length field that requires branching logic to parse. MetricMQ’s header is always exactly 16 bytes, always the same structure. An ESP32 can parse it with a single <code class="language-plaintext highlighter-rouge">struct</code> read — no dynamic memory allocation, no parsing state machine.</p>

<p>The <code class="language-plaintext highlighter-rouge">Cmd</code> field tells the broker what to do:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0x01 = SUBSCRIBE  (client wants to receive messages on a topic)
0x03 = PUBLISH    (client is sending a message)
0x04 = MESSAGE    (broker delivering a message to a subscriber)
0x06 = PING       (keep-alive check)
0x07 = PONG       (keep-alive response)
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">Sequence</code> field is an 8-byte counter that the broker increments for every message. It’s stored in LMDB (an embedded database) so it survives broker restarts. If the broker crashes at sequence 5000 and restarts, the next message gets sequence 5001 — not 1. Subscribers can detect gaps.</p>

<hr />

<h2 id="the-python-demo--line-by-line">The Python Demo — Line by Line</h2>

<p>The demo script talks directly to MetricMQ using raw sockets and the 16-byte binary protocol. No broker-specific client library needed. Here’s how it’s structured, piece by piece.</p>

<h3 id="the-frame-builder">The Frame Builder</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">socket</span><span class="p">,</span> <span class="n">struct</span><span class="p">,</span> <span class="n">threading</span><span class="p">,</span> <span class="n">time</span><span class="p">,</span> <span class="n">json</span><span class="p">,</span> <span class="n">random</span>
<span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>

<span class="n">HOST</span> <span class="o">=</span> <span class="sh">"</span><span class="s">127.0.0.1</span><span class="sh">"</span>
<span class="n">PORT</span> <span class="o">=</span> <span class="mi">6379</span>
<span class="n">HEADER_SIZE</span>    <span class="o">=</span> <span class="mi">16</span>
<span class="n">PROTO_VERSION</span>  <span class="o">=</span> <span class="mh">0x01</span>
<span class="n">CMD_SUBSCRIBE</span>  <span class="o">=</span> <span class="mh">0x01</span>
<span class="n">CMD_PUBLISH</span>    <span class="o">=</span> <span class="mh">0x03</span>
<span class="n">CMD_MESSAGE</span>    <span class="o">=</span> <span class="mh">0x04</span>
<span class="n">CMD_PING</span>       <span class="o">=</span> <span class="mh">0x06</span>
<span class="n">CMD_PONG</span>       <span class="o">=</span> <span class="mh">0x07</span>
</code></pre></div></div>

<p>These constants mirror the broker’s internal definitions exactly. <code class="language-plaintext highlighter-rouge">PROTO_VERSION = 0x01</code> tells the broker which version of the protocol we’re speaking. If you ever update the protocol, clients on old versions get rejected cleanly rather than silently misbehaving.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">build_frame</span><span class="p">(</span><span class="n">cmd</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="o">=</span><span class="sa">b</span><span class="sh">""</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">topic</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">p</span> <span class="o">=</span> <span class="n">payload</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="nb">str</span><span class="p">)</span> <span class="k">else</span> <span class="n">payload</span>
    <span class="k">return</span> <span class="n">struct</span><span class="p">.</span><span class="nf">pack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">PROTO_VERSION</span><span class="p">,</span> <span class="n">cmd</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">t</span><span class="p">),</span> <span class="nf">len</span><span class="p">(</span><span class="n">p</span><span class="p">))</span> <span class="o">+</span> <span class="n">t</span> <span class="o">+</span> <span class="n">p</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">struct.pack("&gt;BBQHL", ...)</code> is where the 16-byte header gets assembled. The format string <code class="language-plaintext highlighter-rouge">&gt;BBQHL</code> means:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">&gt;</code> — big-endian byte order (network byte order, consistent across all platforms)</li>
  <li><code class="language-plaintext highlighter-rouge">B</code> — 1 unsigned byte (version)</li>
  <li><code class="language-plaintext highlighter-rouge">B</code> — 1 unsigned byte (command)</li>
  <li><code class="language-plaintext highlighter-rouge">Q</code> — 8 unsigned bytes (sequence number, fits up to 18 quintillion messages)</li>
  <li><code class="language-plaintext highlighter-rouge">H</code> — 2 unsigned bytes (topic length, up to 65535 characters)</li>
  <li><code class="language-plaintext highlighter-rouge">L</code> — 4 unsigned bytes (payload length, up to 4GB)</li>
</ul>

<p>Total: 1+1+8+2+4 = <strong>16 bytes exactly</strong>. Then the actual topic string and payload bytes are appended directly after. The broker reads the header first, gets the lengths, then reads exactly that many bytes for topic and payload. No delimiters, no parsing ambiguity.</p>

<h3 id="the-frame-reader">The Frame Reader</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">n</span><span class="p">):</span>
    <span class="n">buf</span> <span class="o">=</span> <span class="nf">bytearray</span><span class="p">()</span>
    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">:</span>
        <span class="n">chunk</span> <span class="o">=</span> <span class="n">sock</span><span class="p">.</span><span class="nf">recv</span><span class="p">(</span><span class="n">n</span> <span class="o">-</span> <span class="nf">len</span><span class="p">(</span><span class="n">buf</span><span class="p">))</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">chunk</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">EOFError</span><span class="p">()</span>
        <span class="n">buf</span> <span class="o">+=</span> <span class="n">chunk</span>
    <span class="k">return</span> <span class="nf">bytes</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span>
</code></pre></div></div>

<p>TCP does not guarantee that one <code class="language-plaintext highlighter-rouge">send()</code> equals one <code class="language-plaintext highlighter-rouge">recv()</code>. The network can split a 16-byte header across two packets, or merge multiple messages into one buffer. <code class="language-plaintext highlighter-rouge">recv_exactly</code> loops until it has exactly <code class="language-plaintext highlighter-rouge">n</code> bytes — not more, not less. This is called <strong>framing</strong> and it’s essential for any binary protocol.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">read_frame</span><span class="p">(</span><span class="n">sock</span><span class="p">):</span>
    <span class="n">raw</span> <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">HEADER_SIZE</span><span class="p">)</span>
    <span class="n">ver</span><span class="p">,</span> <span class="n">cmd</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">tlen</span><span class="p">,</span> <span class="n">plen</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">raw</span><span class="p">)</span>
    <span class="n">topic</span>   <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">tlen</span><span class="p">).</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">if</span> <span class="n">tlen</span> <span class="k">else</span> <span class="sh">""</span>
    <span class="n">payload</span> <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">plen</span><span class="p">)</span>                  <span class="k">if</span> <span class="n">plen</span> <span class="k">else</span> <span class="sa">b</span><span class="sh">""</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">:</span> <span class="n">cmd</span><span class="p">,</span> <span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">:</span> <span class="n">seq</span><span class="p">,</span> <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="n">topic</span><span class="p">,</span> <span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">}</span>
</code></pre></div></div>

<p>First read exactly 16 bytes. Unpack them according to the same format string used in <code class="language-plaintext highlighter-rouge">build_frame</code> — this is the mirror operation. Then read exactly <code class="language-plaintext highlighter-rouge">tlen</code> bytes for the topic and <code class="language-plaintext highlighter-rouge">plen</code> bytes for the payload. Return a clean dictionary. The broker does exactly the same thing on its side to parse incoming messages from clients.</p>

<h3 id="the-client-class">The Client Class</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Client</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">name</span>     <span class="o">=</span> <span class="n">name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span>     <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_lock</span>    <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Lock</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">received</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">sock</span>     <span class="o">=</span> <span class="n">socket</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="n">socket</span><span class="p">.</span><span class="n">AF_INET</span><span class="p">,</span> <span class="n">socket</span><span class="p">.</span><span class="n">SOCK_STREAM</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">connect</span><span class="p">((</span><span class="n">HOST</span><span class="p">,</span> <span class="n">PORT</span><span class="p">))</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">socket.AF_INET</code> means IPv4. <code class="language-plaintext highlighter-rouge">socket.SOCK_STREAM</code> means TCP — a reliable, ordered connection. Each <code class="language-plaintext highlighter-rouge">Client</code> instance is one TCP connection to the broker. The moment <code class="language-plaintext highlighter-rouge">.connect()</code> is called, the broker logs <code class="language-plaintext highlighter-rouge">New client connected: fd=XXX</code>.</p>

<p>The <code class="language-plaintext highlighter-rouge">_lock</code> is a threading mutex. The <code class="language-plaintext highlighter-rouge">listen()</code> method runs in a background thread (reading incoming messages), while the main thread can simultaneously call <code class="language-plaintext highlighter-rouge">publish()</code>. Without the lock, two threads could write to the socket at the same time and corrupt the data stream.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">def</span> <span class="nf">subscribe</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">topic</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_SUBSCRIBE</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">_seq</span><span class="p">))</span>
</code></pre></div></div>

<p>Subscribing is just sending a frame with <code class="language-plaintext highlighter-rouge">CMD_SUBSCRIBE</code> and the topic name. The payload is empty — the broker only needs the topic string to register this client as a subscriber. After this, any message published to this topic (or a matching wildcard) gets delivered to this socket.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">def</span> <span class="nf">publish</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_PUBLISH</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">_seq</span><span class="p">))</span>
</code></pre></div></div>

<p>Publishing sends <code class="language-plaintext highlighter-rouge">CMD_PUBLISH</code> with the topic and payload. The broker receives this, looks up all subscribers matching the topic, and delivers a copy to each one. The <code class="language-plaintext highlighter-rouge">_seq</code> here is the <em>client’s</em> sequence counter — separate from the broker’s global sequence counter.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">def</span> <span class="nf">listen</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">stop_event</span><span class="p">):</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="k">while</span> <span class="ow">not</span> <span class="n">stop_event</span><span class="p">.</span><span class="nf">is_set</span><span class="p">():</span>
                <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">settimeout</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>
                <span class="k">try</span><span class="p">:</span>
                    <span class="n">frame</span> <span class="o">=</span> <span class="nf">read_frame</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">)</span>
                    <span class="k">if</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="n">CMD_MESSAGE</span><span class="p">:</span>
                        <span class="n">inner</span> <span class="o">=</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">]</span>
                        <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">inner</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="n">HEADER_SIZE</span><span class="p">:</span>
                            <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">tlen</span><span class="p">,</span> <span class="n">plen</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">inner</span><span class="p">[:</span><span class="n">HEADER_SIZE</span><span class="p">])</span>
                            <span class="n">off</span>     <span class="o">=</span> <span class="n">HEADER_SIZE</span>
                            <span class="n">topic</span>   <span class="o">=</span> <span class="n">inner</span><span class="p">[</span><span class="n">off</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="p">].</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                            <span class="n">payload</span> <span class="o">=</span> <span class="n">inner</span><span class="p">[</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="o">+</span><span class="n">plen</span><span class="p">].</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                        <span class="k">else</span><span class="p">:</span>
                            <span class="n">topic</span>   <span class="o">=</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">]</span>
                            <span class="n">payload</span> <span class="o">=</span> <span class="n">inner</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                        <span class="n">self</span><span class="p">.</span><span class="n">received</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="n">topic</span><span class="p">,</span> <span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">,</span> <span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">:</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">]})</span>
                    <span class="k">elif</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="n">CMD_PING</span><span class="p">:</span>
                        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_PONG</span><span class="p">,</span> <span class="sh">""</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">]))</span>
                <span class="k">except</span> <span class="n">socket</span><span class="p">.</span><span class="n">timeout</span><span class="p">:</span>
                    <span class="k">continue</span>
        <span class="nf">except </span><span class="p">(</span><span class="nb">EOFError</span><span class="p">,</span> <span class="nb">OSError</span><span class="p">):</span>
            <span class="k">pass</span>
</code></pre></div></div>

<p>This runs in a background thread, constantly reading frames from the socket. Three things worth noting:</p>

<p><code class="language-plaintext highlighter-rouge">sock.settimeout(1.0)</code> — the socket blocks for at most 1 second waiting for data. Without this, <code class="language-plaintext highlighter-rouge">read_frame</code> would block forever if no messages arrive, and the thread could never check <code class="language-plaintext highlighter-rouge">stop_event.is_set()</code> to know it should exit.</p>

<p>The <code class="language-plaintext highlighter-rouge">CMD_MESSAGE</code> branch handles delivered messages. MetricMQ wraps the delivered message as an inner frame inside the outer delivery frame’s payload. So we unpack the outer frame first, then unpack again from the payload bytes to get the actual topic and message content.</p>

<p>The <code class="language-plaintext highlighter-rouge">CMD_PING</code> branch responds to keep-alive pings from the broker. The broker sends a PING every 60 seconds to check if the client is still alive. If the client doesn’t respond with PONG, the broker closes the connection and decrements <code class="language-plaintext highlighter-rouge">metricmq_active_connections</code>.</p>

<hr />

<h2 id="running-all-six-features">Running All Six Features</h2>

<p>Run the full script:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">python</span><span class="w"> </span><span class="nx">metricmq_demo.py</span><span class="w">
</span></code></pre></div></div>

<p>Or run it in a continuous loop to generate enough data for the Grafana graphs:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kr">while</span><span class="w"> </span><span class="p">(</span><span class="bp">$true</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">python</span><span class="w"> </span><span class="nx">metricmq_demo.py</span><span class="p">;</span><span class="w"> </span><span class="n">Start-Sleep</span><span class="w"> </span><span class="nx">2</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Here’s what each feature does and what MetricMQ mechanism it exercises.</p>

<hr />

<h3 id="feature-1--basic-pubsub">Feature 1 — Basic Pub/Sub</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_pubsub</span><span class="p">():</span>
    <span class="c1"># Create two separate TCP connections to the broker
</span>    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">subscriber-1</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">publisher-1</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Start the listener thread for the subscriber
</span>    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>

    <span class="c1"># Register the subscriber's interest in this topic
</span>    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>  <span class="c1"># wait for subscribe to register before publishing
</span></code></pre></div></div>

<p>Two clients, two separate TCP connections. The <code class="language-plaintext highlighter-rouge">time.sleep(0.5)</code> after subscribing is important — the subscribe frame needs to reach the broker and be registered before any messages are published. Without this pause, the publisher might send messages before the broker knows the subscriber exists, and they’d be dropped.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">6</span><span class="p">):</span>
        <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">msg</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">,</span> <span class="sh">"</span><span class="s">temp</span><span class="sh">"</span><span class="p">:</span> <span class="nf">round</span><span class="p">(</span><span class="mf">20.0</span> <span class="o">+</span> <span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">),</span> <span class="mi">1</span><span class="p">),</span> <span class="sh">"</span><span class="s">unit</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">C</span><span class="sh">"</span><span class="p">})</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → sensors/temperature  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>  <span class="c1"># wait for all deliveries to arrive
</span>    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">  ✅ Received </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">)</span><span class="si">}</span><span class="s">/5 messages</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">     [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] RECV ← </span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">topic</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">payload</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The payload is JSON — a human-readable format that works on both the ESP32 (C++ JSON parser) and Python. The broker doesn’t care what the payload contains — it’s opaque bytes as far as routing is concerned.</p>

<p><strong>Expected output:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[00:02:40] PUB  → sensors/temperature  {"msg": 1, "temp": 22.3, "unit": "C"}
[00:02:40] PUB  → sensors/temperature  {"msg": 2, "temp": 21.8, "unit": "C"}
...
✅ Received 5/5 messages
[00:02:43] RECV ← sensors/temperature  {"msg": 1, "temp": 22.3, "unit": "C"}
</code></pre></div></div>

<p><strong>What to watch in Grafana:</strong> The <strong>Published vs Delivered</strong> panel should show two identical lines — every published message was delivered. The <strong>Publish Rate</strong> panel shows a small burst, then silence.</p>

<hr />

<h3 id="feature-2--wildcard-subscriptions">Feature 2 — Wildcard Subscriptions</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_wildcard</span><span class="p">():</span>
    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">wildcard-sub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">wildcard-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>

    <span class="c1"># Subscribe to a wildcard pattern — catches ALL subtopics under sensors/
</span>    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/#</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="c1"># Publish to four completely different subtopics
</span>    <span class="n">topics</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/humidity</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/pressure</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/co2</span><span class="sh">"</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">topic</span> <span class="ow">in</span> <span class="n">topics</span><span class="p">:</span>
        <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">value</span><span class="sh">"</span><span class="p">:</span> <span class="nf">round</span><span class="p">(</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="mi">10</span><span class="p">,</span> <span class="mi">100</span><span class="p">),</span> <span class="mi">1</span><span class="p">)})</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → </span><span class="si">{</span><span class="n">topic</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.4</span><span class="p">)</span>
</code></pre></div></div>

<p>The subscriber never named <code class="language-plaintext highlighter-rouge">sensors/humidity</code>, <code class="language-plaintext highlighter-rouge">sensors/pressure</code>, or <code class="language-plaintext highlighter-rouge">sensors/co2</code> explicitly. It only said <code class="language-plaintext highlighter-rouge">sensors/#</code>. The <code class="language-plaintext highlighter-rouge">#</code> is a <strong>wildcard</strong> that matches any continuation of the path — including multiple levels deep, so <code class="language-plaintext highlighter-rouge">sensors/room1/temperature</code> would also match.</p>

<p>This matters in real IoT deployments. If you have a monitoring dashboard subscribed to <code class="language-plaintext highlighter-rouge">sensors/#</code>, you can add new sensor types to your network and they automatically appear in the dashboard. No reconfiguration, no redeployment.</p>

<p><strong>Expected output:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Subscribed to: sensors/#
[00:02:44] PUB  → sensors/temperature  {"value": 73.7}
[00:02:44] PUB  → sensors/humidity     {"value": 13.7}
[00:02:45] PUB  → sensors/pressure     {"value": 82.8}
[00:02:45] PUB  → sensors/co2          {"value": 75.5}

✅ Wildcard caught 4/4 messages across all subtopics
</code></pre></div></div>

<hr />

<h3 id="feature-3--fan-out">Feature 3 — Fan-out</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_fanout</span><span class="p">():</span>
    <span class="n">N</span>    <span class="o">=</span> <span class="mi">5</span>
    <span class="c1"># Create 5 independent subscriber clients (5 separate TCP connections)
</span>    <span class="n">subs</span> <span class="o">=</span> <span class="p">[</span><span class="nc">Client</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">fan-sub-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">)]</span>
    <span class="n">pub</span>  <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">fan-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stops</span> <span class="o">=</span> <span class="p">[</span><span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">)]</span>

    <span class="c1"># Each subscriber gets its own listener thread
</span>    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">sub</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">subs</span><span class="p">):</span>
        <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stops</span><span class="p">[</span><span class="n">i</span><span class="p">],),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>
        <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">broadcast/alert</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="c1"># One publish — broker delivers to all 5
</span>    <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">alert</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">high_temp</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">value</span><span class="sh">"</span><span class="p">:</span> <span class="mf">87.3</span><span class="p">})</span>
    <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">broadcast/alert</span><span class="sh">"</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → broadcast/alert  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>

    <span class="n">received</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="mi">1</span> <span class="k">for</span> <span class="n">s</span> <span class="ow">in</span> <span class="n">subs</span> <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">.</span><span class="n">received</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">  ✅ Fan-out: </span><span class="si">{</span><span class="n">received</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">N</span><span class="si">}</span><span class="s"> subscribers received the message</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The broker maintains an internal map of <code class="language-plaintext highlighter-rouge">topic → [list of subscriber connections]</code>. When a publish arrives for <code class="language-plaintext highlighter-rouge">broadcast/alert</code>, the broker iterates that list and writes a copy of the message to each socket. The publisher sent one frame; five deliveries happened.</p>

<p>This is the mechanism behind alert systems, live dashboards, and logging pipelines — one event source, many consumers, zero coordination between consumers required.</p>

<p><strong>What to watch in Prometheus:</strong> <code class="language-plaintext highlighter-rouge">metricmq_messages_published_total</code> increases by 1. <code class="language-plaintext highlighter-rouge">metricmq_messages_delivered_total</code> increases by 5. The ratio tells you the average fan-out factor.</p>

<hr />

<h3 id="feature-4--sequence-numbers">Feature 4 — Sequence Numbers</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_sequence</span><span class="p">():</span>
    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">seq-sub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">seq-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>
    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">stream/data</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="c1"># Send 8 messages as fast as possible
</span>    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">8</span><span class="p">):</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">stream/data</span><span class="sh">"</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">index</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">}))</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.05</span><span class="p">)</span>  <span class="c1"># 50ms between sends — fast but not instant
</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  ✅ Received </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">)</span><span class="si">}</span><span class="s">/8 in order:</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">:</span>
        <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">payload</span><span class="sh">'</span><span class="p">])</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">     broker_seq=</span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">seq</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="s">  index=</span><span class="si">{</span><span class="n">data</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">index</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">?</span><span class="sh">'</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>Each received message has a <code class="language-plaintext highlighter-rouge">broker_seq</code> — the broker’s global sequence counter at the time of delivery. Watch the numbers:</p>

<p><strong>Expected output:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>✅ Received 8/8 in order:
   broker_seq=4808  index=0
   broker_seq=4809  index=1
   broker_seq=4810  index=2
   broker_seq=4811  index=3
   broker_seq=4812  index=4
   broker_seq=4813  index=5
   broker_seq=4814  index=6
   broker_seq=4815  index=7
</code></pre></div></div>

<p>No gaps in the sequence numbers — <code class="language-plaintext highlighter-rouge">4808, 4809, 4810...</code>. If you ever saw <code class="language-plaintext highlighter-rouge">4808, 4810</code> with 4809 missing, a message was dropped. The broker’s sequence numbers make that immediately visible.</p>

<p>Now stop the broker (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>) and restart it. The first line of broker output will be:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Restored sequence counter from persistence: 4816
</code></pre></div></div>

<p>The next message after restart gets sequence <code class="language-plaintext highlighter-rouge">4816</code>, not <code class="language-plaintext highlighter-rouge">1</code>. This is LMDB doing its job — the sequence counter is persisted to disk after every message, so broker crashes don’t reset the counter and create ambiguous sequence numbers.</p>

<hr />

<h3 id="feature-5--per-topic-routing-and-prometheus-labels">Feature 5 — Per-Topic Routing and Prometheus Labels</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_multi_topic</span><span class="p">():</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">topic-pub</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Publish deliberately different counts to each topic
</span>    <span class="n">topics</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">:</span> <span class="mi">10</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">sensors/humidity</span><span class="sh">"</span><span class="p">:</span>     <span class="mi">6</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">commands/actuator</span><span class="sh">"</span><span class="p">:</span>    <span class="mi">4</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">logs/system</span><span class="sh">"</span><span class="p">:</span>          <span class="mi">8</span><span class="p">,</span>
    <span class="p">}</span>
    <span class="k">for</span> <span class="n">topic</span><span class="p">,</span> <span class="n">count</span> <span class="ow">in</span> <span class="n">topics</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">count</span><span class="p">):</span>
            <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="n">topic</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">i</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">}))</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">count</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s"> msgs → </span><span class="si">{</span><span class="n">topic</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.2</span><span class="p">)</span>
</code></pre></div></div>

<p>This demo’s output isn’t in the terminal — it’s in Prometheus. After running this, open <code class="language-plaintext highlighter-rouge">http://localhost:9090</code> and query:</p>

<pre><code class="language-promql">metricmq_topic_messages_total
</code></pre>

<p>You’ll see:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>metricmq_topic_messages_total{topic="sensors/temperature"} 10
metricmq_topic_messages_total{topic="sensors/humidity"}    6
metricmq_topic_messages_total{topic="commands/actuator"}   4
metricmq_topic_messages_total{topic="logs/system"}         8
</code></pre></div></div>

<p>Each topic is a separate <strong>label</strong> in Prometheus. This is how the broker tracks per-topic traffic without creating a different metric for each topic — the topic name becomes a dimension of the same metric. In Grafana, this shows up as separate coloured lines on the <strong>Messages per Topic</strong> panel, one per topic.</p>

<hr />

<h3 id="feature-6--connection-tracking">Feature 6 — Connection Tracking</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_connections</span><span class="p">():</span>
    <span class="n">clients</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">5</span><span class="p">):</span>
        <span class="n">clients</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Client</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">conn-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">))</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] +1 connected  (total: </span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.3</span><span class="p">)</span>  <span class="c1"># stagger connections so Prometheus captures each step
</span>
    <span class="c1"># Hold all 5 open for 3 seconds
</span>    <span class="c1"># Prometheus scrapes every 5s — this window ensures at least one scrape
</span>    <span class="c1"># captures metricmq_active_connections = 5
</span>    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">c</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">clients</span><span class="p">):</span>
        <span class="n">c</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] -1 disconnected  (remaining: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">clients</span><span class="p">)</span><span class="o">-</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.3</span><span class="p">)</span>
</code></pre></div></div>

<p>Every <code class="language-plaintext highlighter-rouge">Client()</code> constructor calls <code class="language-plaintext highlighter-rouge">sock.connect()</code>, which the broker counts. Every <code class="language-plaintext highlighter-rouge">client.close()</code> closes the TCP connection, which the broker detects via a socket read returning zero bytes.</p>

<p>The <code class="language-plaintext highlighter-rouge">time.sleep(0.3)</code> between each connection and disconnection is deliberate — it staggers the changes so the Grafana graph shows a visible staircase pattern (0→1→2→3→4→5→4→3→2→1→0) rather than a single spike.</p>

<p><strong>What to watch in Grafana:</strong> The <strong>Active Connections</strong> stat panel steps up from 0 to 5, holds for 3 seconds, then steps back down. This is how you’d detect a reconnect storm in production — a spike in <code class="language-plaintext highlighter-rouge">metricmq_connections_total</code> with a corresponding spike in <code class="language-plaintext highlighter-rouge">metricmq_disconnections_total</code> a few seconds later.</p>

<hr />

<h2 id="the-grafana-dashboard">The Grafana Dashboard</h2>

<p>Go to <code class="language-plaintext highlighter-rouge">http://localhost:3001/dashboard/import</code>, paste this JSON, and click <strong>Load → Import</strong>:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"MetricMQ"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"uid"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"schemaVersion"</span><span class="p">:</span><span class="w"> </span><span class="mi">36</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"panels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Active Connections"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_active_connections"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Active Subscribers"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_active_subscribers"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">3</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Total Published"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_messages_published_total"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Total Delivered"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_messages_delivered_total"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">5</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Publish Rate (msg/s)"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_published_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"publish rate"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">6</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"P99 Publish Latency (s)"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"histogram_quantile(0.99, rate(metricmq_publish_latency_seconds_bucket[1m]))"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"p99"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">7</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Messages per Topic"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_topic_messages_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">""</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Published vs Delivered"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_published_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"published"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">},</span><span class="w">
        </span><span class="p">{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_delivered_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"delivered"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"B"</span><span class="p">}</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Change the time range dropdown (top right) from <strong>Last 6 hours</strong> to <strong>Last 15 minutes</strong> — the demo bursts become much more visible at that zoom level.</p>

<hr />

<h2 id="reading-the-dashboard">Reading the Dashboard</h2>

<p>After running the demo loop for a few minutes, here’s what the panels tell you:</p>

<p><strong>Active Connections / Active Subscribers</strong> — live gauges. During Feature 6 you’ll see these climb to 5 then drop to 0.</p>

<p><strong>Total Published = Total Delivered</strong> — the important one. If these numbers ever diverge, messages were dropped. Across thousands of publishes in the demo, they stay identical — zero delivery loss.</p>

<p><strong>Publish Rate</strong> — shows bursts during the demo runs, flat between runs. The y-axis is messages per second, calculated by Prometheus using <code class="language-plaintext highlighter-rouge">rate()</code> over the last 1 minute.</p>

<p><strong>P99 Publish Latency</strong> — the 99th percentile of publish operation time. On Windows loopback this sits around 60–100ms due to Python socket overhead. On actual hardware with the C++ client it drops to microseconds.</p>

<p><strong>Messages per Topic</strong> — each topic gets its own coloured line. You can see Feature 5’s deliberate imbalance (10 messages to temperature, 4 to actuator) as different line heights.</p>

<p><strong>Published vs Delivered</strong> — the two lines track each other exactly when everything is healthy. In a system with overloaded subscribers you’d see delivered lag behind published — an early warning before messages start backing up.</p>

<hr />

<h2 id="useful-promql-queries-to-explore">Useful PromQL Queries to Explore</h2>

<p>Open <code class="language-plaintext highlighter-rouge">http://localhost:9090</code> and try these directly:</p>

<pre><code class="language-promql"># Live publish rate in messages per second
rate(metricmq_messages_published_total[1m])

# P99 latency — 99% of messages were published faster than this value
histogram_quantile(0.99, rate(metricmq_publish_latency_seconds_bucket[1m]))

# Per-topic breakdown — which topics are busiest
sum by (topic) (rate(metricmq_topic_messages_total[1m]))

# Delivery loss ratio — should be exactly 0.0 under normal conditions
1 - (metricmq_messages_delivered_total / metricmq_messages_published_total)

# Client churn — how fast are clients connecting and disconnecting
rate(metricmq_connections_total[5m])

# Security check — any rejected messages (should be 0 unless under attack)
rate(metricmq_errors_total[1m])
</code></pre>

<hr />

<h2 id="whats-next">What’s Next</h2>

<p>Everything above covers the transport and observability layers. The piece not shown here is the <strong>security layer</strong> — publishing a message to a <code class="language-plaintext highlighter-rouge">secure/*</code> topic without a valid Ed25519 key and watching the broker reject it. That’s the feature no equivalent lightweight broker has, and it’s visible in Prometheus as <code class="language-plaintext highlighter-rouge">metricmq_signature_failures_total</code> incrementing.</p>

<p>The <strong>ESP32 client</strong> is also worth wiring up once you have a device handy. The broker auto-detects the binary protocol from the first byte — the same Python demo running on your PC and a C++ client running on an ESP32 can both publish to the same topic, and both show up in the same Grafana dashboard. That combination — firmware and backend in one monitoring view — is what MetricMQ is built for.</p>

<p>Source code and build instructions: <a href="https://github.com/Saptarshi-max/MetricMQ">github.com/Saptarshi-max/MetricMQ</a></p>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><summary type="html"><![CDATA[A hands-on walkthrough of MetricMQ: what it does differently, how to run the Python demos line by line, and how to watch your messages flow in real time through Prometheus and Grafana.]]></summary></entry><entry><title type="html">Getting Started with MetricMQ: Local Setup and Grafana Monitoring</title><link href="/blog/es/2026-07-01-metricmq-getting-started-no-hardware/" rel="alternate" type="text/html" title="Getting Started with MetricMQ: Local Setup and Grafana Monitoring" /><published>2026-07-01T00:00:00+00:00</published><updated>2026-08-05T11:56:49+00:00</updated><id>/blog/es/metricmq-getting-started-no-hardware</id><content type="html" xml:base="/blog/es/2026-07-01-metricmq-getting-started-no-hardware/"><![CDATA[<p>A technical guide to running MetricMQ, understanding its wire protocol, executing Python client demos, and setting up Prometheus and Grafana for real-time message tracking.</p>

<hr />

<p>I built MetricMQ because existing message brokers often represent trade-offs when working with low-power microcontrollers. Full-featured brokers like RabbitMQ require significant system resources, while lightweight options like basic Redis pub/sub lack message-level security and persistent sequence tracking.</p>

<p>MetricMQ is a lightweight C++20 broker (328 KB binary) that uses a fixed 16-byte binary wire protocol, enforces optional per-message Ed25519 signature verification at the broker level, persists sequence IDs via LMDB for exact sequence tracking, and exposes a native Prometheus metrics endpoint on every path.</p>

<p>This post covers how to run the broker locally, execute Python scripts demonstrating its core feature set without external dependencies, and set up a Grafana dashboard to observe metrics in real time.</p>

<hr />

<h2 id="message-broker-overview">Message Broker Overview</h2>

<p>A message broker decouples data producers (publishers) from data consumers (subscribers).</p>

<p>For example, when multiple microcontrollers record sensor data that needs to feed a database, a web dashboard, and an alerting service, connecting every sensor directly to every service scales poorly:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Direct connections (hard to scale):

  Sensor 1 ──→ Dashboard
  Sensor 1 ──→ Database
  Sensor 1 ──→ Alert System
  Sensor 2 ──→ Dashboard
  Sensor 2 ──→ Database
  Sensor 2 ──→ Alert System
  (12 sensors × 3 consumers = 36 connections)

Broker model (hub-and-spoke):

  Sensor 1 ──┐
  Sensor 2 ──┤──→ [ MetricMQ Broker :6379 ] ──→ Dashboard
  Sensor 3 ──┘                               ──→ Database
                                              ──→ Alert System
</code></pre></div></div>

<p>Publishers send messages to specified topic strings (e.g., <code class="language-plaintext highlighter-rouge">sensors/temperature</code>). Subscribers listen to topics of interest. The broker handles message routing so components can be added or removed independently.</p>

<hr />

<h2 id="technical-comparison">Technical Comparison</h2>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>MQTT</th>
      <th>Redis Pub/Sub</th>
      <th>NATS</th>
      <th>MetricMQ</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Binary size</td>
      <td>~500 KB+</td>
      <td>Separate server</td>
      <td>~20 MB</td>
      <td><strong>328 KB</strong></td>
    </tr>
    <tr>
      <td>ESP32 compatible</td>
      <td>Heavy (TLS overhead)</td>
      <td>No</td>
      <td>No</td>
      <td>Yes (2 KB RAM client)</td>
    </tr>
    <tr>
      <td>Message signing</td>
      <td>No (TLS transport only)</td>
      <td>No</td>
      <td>No</td>
      <td><strong>Ed25519 per message</strong></td>
    </tr>
    <tr>
      <td>Prometheus metrics</td>
      <td>Requires exporter</td>
      <td>Requires exporter</td>
      <td>Requires exporter</td>
      <td><strong>Native HTTP endpoint</strong></td>
    </tr>
    <tr>
      <td>Sequence numbers</td>
      <td>No</td>
      <td>No</td>
      <td>Yes</td>
      <td><strong>Yes (LMDB persisted)</strong></td>
    </tr>
  </tbody>
</table>

<p>Key architectural differences:</p>

<ol>
  <li><strong>Message-level Ed25519 signatures</strong>: Standard MQTT relies entirely on TLS for security. TLS handshakes add substantial latency and memory pressure on microcontrollers. MetricMQ can include a 64-byte signature directly in the binary message header. The broker validates the signature prior to routing. Failed signatures are rejected immediately and increment the <code class="language-plaintext highlighter-rouge">metricmq_signature_failures_total</code> counter.</li>
  <li><strong>Dual-protocol single-port handler</strong>: Upon establishing a TCP socket connection, the broker reads the initial byte to detect whether the client is sending MetricMQ binary frames or Redis RESP text. A microcontroller sending binary frames and a Python script using Redis protocol commands can publish to and consume from the same topics simultaneously.</li>
</ol>

<hr />

<h2 id="system-architecture">System Architecture</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────────────────────────────────────────────────────────┐
│                    Local Host Machine                        │
│                                                              │
│  ┌─────────────────┐    port 6379    ┌──────────────────┐   │
│  │  Python Demo    │ ──────────────→ │  MetricMQ Broker │   │
│  │  (publishers +  │ ←────────────── │  metricmq-       │   │
│  │   subscribers)  │   messages      │  broker.exe      │   │
│  └─────────────────┘                 └────────┬─────────┘   │
│                                               │ port 9091   │
│                                               │ /metrics    │
│                                      ┌────────▼─────────┐   │
│                                      │   Prometheus      │   │
│                                      │   (Docker)        │   │
│                                      │   scrapes every   │   │
│                                      │   5 seconds       │   │
│                                      └────────┬─────────┘   │
│                                               │ port 9090   │
│                                      ┌────────▼─────────┐   │
│                                      │   Grafana         │   │
│                                      │   (Docker)        │   │
│                                      │   localhost:3001  │   │
│                                      └──────────────────┘   │
└──────────────────────────────────────────────────────────────┘
</code></pre></div></div>

<p>The broker binary handles message routing on TCP port <code class="language-plaintext highlighter-rouge">6379</code> and serves Prometheus metrics on HTTP port <code class="language-plaintext highlighter-rouge">9091</code>. A Prometheus instance running in Docker scrapes port <code class="language-plaintext highlighter-rouge">9091</code> every 5 seconds. Grafana queries Prometheus on port <code class="language-plaintext highlighter-rouge">9090</code> and renders the dashboard UI on port <code class="language-plaintext highlighter-rouge">3001</code>.</p>

<hr />

<h2 id="setting-up-the-infrastructure">Setting Up the Infrastructure</h2>

<h3 id="step-1-start-the-metricmq-broker">Step 1: Start the MetricMQ Broker</h3>

<p>Run the binary from your build directory:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cd</span><span class="w"> </span><span class="nx">C:\path\to\MetricMQ</span><span class="w">
</span><span class="o">.</span><span class="n">\build\Release\metricmq-broker.exe</span><span class="w">
</span></code></pre></div></div>

<p>Expected startup log:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MetricMQ Broker v1.0
Lightweight Message Queue for IoT

Starting broker on port 6379...
Starting metrics server on port 9091...
Broker listening on 0.0.0.0:6379
Metrics server started successfully on http://0.0.0.0:9091/metrics
</code></pre></div></div>

<p>The broker binds to <code class="language-plaintext highlighter-rouge">0.0.0.0</code>, listening across all available network interfaces.</p>

<p>To verify the metrics server, query port <code class="language-plaintext highlighter-rouge">9091</code>:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">curl</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="w">
</span></code></pre></div></div>

<p>Sample initial metrics response:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># HELP metricmq_messages_published_total Total number of messages published
# TYPE metricmq_messages_published_total counter
metricmq_messages_published_total 0

# HELP metricmq_active_connections Current number of active connections
# TYPE metricmq_active_connections gauge
metricmq_active_connections 0

# HELP metricmq_publish_latency_seconds Publish operation latency in seconds
# TYPE metricmq_publish_latency_seconds histogram
metricmq_publish_latency_seconds_bucket{le="0.001"} 0
metricmq_publish_latency_seconds_bucket{le="0.005"} 0
...
</code></pre></div></div>

<h3 id="step-2-configure-prometheus">Step 2: Configure Prometheus</h3>

<p>Create a minimal <code class="language-plaintext highlighter-rouge">prometheus.yml</code> configuration:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">mkdir</span><span class="w"> </span><span class="nx">C:\metricmq-monitoring</span><span class="w">

</span><span class="sh">@"
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'metricmq'
    static_configs:
      - targets: ['host.docker.internal:9091']
"@</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Out-File</span><span class="w"> </span><span class="nt">-FilePath</span><span class="w"> </span><span class="nx">C:\metricmq-monitoring\prometheus.yml</span><span class="w"> </span><span class="nt">-Encoding</span><span class="w"> </span><span class="nx">utf8</span><span class="w">
</span></code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">scrape_interval: 5s</code> provides granular time-series resolution during local testing.</li>
  <li><code class="language-plaintext highlighter-rouge">host.docker.internal</code> allows the Prometheus container to resolve and connect back to the host machine’s port <code class="language-plaintext highlighter-rouge">9091</code>.</li>
</ul>

<h3 id="step-3-run-prometheus-and-grafana-containers">Step 3: Run Prometheus and Grafana Containers</h3>

<p>Launch Prometheus and Grafana using Docker:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Prometheus</span><span class="w">
</span><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">-d</span><span class="w"> </span><span class="nt">--name</span><span class="w"> </span><span class="nx">prometheus</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">9090:9090</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">--add-host</span><span class="o">=</span><span class="n">host.docker.internal:host-gateway</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="s2">"C:\metricmq-monitoring\prometheus.yml:/etc/prometheus/prometheus.yml"</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">prom/prometheus</span><span class="w">

</span><span class="c"># Grafana</span><span class="w">
</span><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">-d</span><span class="w"> </span><span class="nt">--name</span><span class="w"> </span><span class="nx">grafana</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">3001:3000</span><span class="w"> </span><span class="nx">grafana/grafana</span><span class="w">
</span></code></pre></div></div>

<p>Verify that both containers are running:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">docker</span><span class="w"> </span><span class="nx">ps</span><span class="w">
</span></code></pre></div></div>

<p>Confirm that the targets status page (<code class="language-plaintext highlighter-rouge">http://localhost:9090/targets</code>) shows the <code class="language-plaintext highlighter-rouge">metricmq</code> target in an <strong>UP</strong> state.</p>

<h3 id="step-4-add-prometheus-data-source-in-grafana">Step 4: Add Prometheus Data Source in Grafana</h3>

<ol>
  <li>Open <code class="language-plaintext highlighter-rouge">http://localhost:3001</code> (default login: <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code>).</li>
  <li>Navigate to <strong>Connections → Data sources → Add data source → Prometheus</strong>.</li>
  <li>Set the Prometheus server URL to <code class="language-plaintext highlighter-rouge">http://host.docker.internal:9090</code>.</li>
  <li>Select <strong>Save &amp; test</strong>.</li>
</ol>

<p><img src="/cover_images/posts/metricmq-prometheus.png" alt="" /></p>

<p>If you want to see your data Raw:
Source: <a href="http://host.docker.internal:9091/metrics">http://host.docker.internal:9091/metrics</a></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># HELP metricmq_messages_published_total Total number of messages published</span>
<span class="c"># TYPE metricmq_messages_published_total counter</span>
metricmq_messages_published_total 3980

<span class="c"># HELP metricmq_messages_delivered_total Total number of messages delivered to subscribers</span>
<span class="c"># TYPE metricmq_messages_delivered_total counter</span>
metricmq_messages_delivered_total 3980

<span class="c"># HELP metricmq_subscriptions_total Total number of subscriptions</span>
<span class="c"># TYPE metricmq_subscriptions_total counter</span>
metricmq_subscriptions_total 720

<span class="c"># HELP metricmq_unsubscriptions_total Total number of unsubscriptions</span>
<span class="c"># TYPE metricmq_unsubscriptions_total counter</span>
metricmq_unsubscriptions_total 0

<span class="c"># HELP metricmq_connections_total Total number of client connections</span>
<span class="c"># TYPE metricmq_connections_total counter</span>
metricmq_connections_total 1620

<span class="c"># HELP metricmq_disconnections_total Total number of client disconnections</span>
<span class="c"># TYPE metricmq_disconnections_total counter</span>
metricmq_disconnections_total 1615

<span class="c"># HELP metricmq_acks_total Total number of message acknowledgements</span>
<span class="c"># TYPE metricmq_acks_total counter</span>
metricmq_acks_total 0

<span class="c"># HELP metricmq_errors_total Total number of errors</span>
<span class="c"># TYPE metricmq_errors_total counter</span>
metricmq_errors_total 0

<span class="c"># HELP metricmq_active_connections Current number of active connections</span>
<span class="c"># TYPE metricmq_active_connections gauge</span>
metricmq_active_connections 5

<span class="c"># HELP metricmq_active_subscribers Current number of active subscribers</span>
<span class="c"># TYPE metricmq_active_subscribers gauge</span>
metricmq_active_subscribers 0

<span class="c"># HELP metricmq_topics Current number of topics with subscribers</span>
<span class="c"># TYPE metricmq_topics gauge</span>
metricmq_topics 0

<span class="c"># HELP metricmq_publish_latency_seconds Publish operation latency in seconds</span>
<span class="c"># TYPE metricmq_publish_latency_seconds histogram</span>
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.0001"</span><span class="o">}</span> 0
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.0005"</span><span class="o">}</span> 0
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.001"</span><span class="o">}</span> 0
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.005"</span><span class="o">}</span> 1517
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.01"</span><span class="o">}</span> 2841
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.05"</span><span class="o">}</span> 3638
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.1"</span><span class="o">}</span> 3708
metricmq_publish_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"+Inf"</span><span class="o">}</span> 3980
metricmq_publish_latency_seconds_sum 131.265324
metricmq_publish_latency_seconds_count 3980

<span class="c"># HELP metricmq_delivery_latency_seconds Message delivery latency in seconds</span>
<span class="c"># TYPE metricmq_delivery_latency_seconds histogram</span>
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.0001"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.0005"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.001"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.005"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.01"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.05"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"0.1"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_bucket<span class="o">{</span><span class="nv">le</span><span class="o">=</span><span class="s2">"+Inf"</span><span class="o">}</span> 0
metricmq_delivery_latency_seconds_sum 0.000000
metricmq_delivery_latency_seconds_count 0

<span class="c"># HELP metricmq_topic_messages_total Messages published per topic</span>
<span class="c"># TYPE metricmq_topic_messages_total counter</span>
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"broadcast/alert"</span><span class="o">}</span> 90
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"commands/actuator"</span><span class="o">}</span> 328
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"logs/system"</span><span class="o">}</span> 642
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/co2"</span><span class="o">}</span> 90
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/humidity"</span><span class="o">}</span> 588
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/pressure"</span><span class="o">}</span> 90
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/temperature"</span><span class="o">}</span> 1438
metricmq_topic_messages_total<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"stream/data"</span><span class="o">}</span> 714

<span class="c"># HELP metricmq_topic_subscribers Current subscribers per topic</span>
<span class="c"># TYPE metricmq_topic_subscribers gauge</span>
metricmq_topic_subscribers<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"broadcast/alert"</span><span class="o">}</span> 450
metricmq_topic_subscribers<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/#"</span><span class="o">}</span> 90
metricmq_topic_subscribers<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"sensors/temperature"</span><span class="o">}</span> 90
metricmq_topic_subscribers<span class="o">{</span><span class="nv">topic</span><span class="o">=</span><span class="s2">"stream/data"</span><span class="o">}</span> 90


</code></pre></div></div>

<p>And then to Finally add to Grafana : Go to Grafana -&gt; add Source -&gt; Prometheus (select from the list of plugins) -&gt; add source URL -&gt; save &amp; test</p>

<p><img src="/cover_images/posts/metricmq-prometheus-update.png" alt="" /></p>

<h2 id="wire-protocol-specification">Wire Protocol Specification</h2>

<p>MetricMQ uses a fixed 16-byte header for all binary frames:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Byte position:  0      1      2-9        10-11      12-15
                ┌──────┬──────┬──────────┬──────────┬──────────┐
                │ Ver  │ Cmd  │ Sequence │ TopicLen │ PayloadLen│
                │ (1B) │ (1B) │  (8B)    │   (2B)   │   (4B)   │
                └──────┴──────┴──────────┴──────────┴──────────┘
                Followed by: [Topic bytes] [Payload bytes]
</code></pre></div></div>

<p>Using a fixed-width 16-byte header simplifies parsing on resource-constrained platforms compared to variable-length header structures. Memory allocation can be completely avoided when receiving packets into static buffers.</p>

<p>Supported command byte values (<code class="language-plaintext highlighter-rouge">Cmd</code>):</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">0x01</code> (<code class="language-plaintext highlighter-rouge">SUBSCRIBE</code>): Register interest in a topic.</li>
  <li><code class="language-plaintext highlighter-rouge">0x03</code> (<code class="language-plaintext highlighter-rouge">PUBLISH</code>): Send a message payload to a topic.</li>
  <li><code class="language-plaintext highlighter-rouge">0x04</code> (<code class="language-plaintext highlighter-rouge">MESSAGE</code>): Broker delivery of a payload to a subscriber.</li>
  <li><code class="language-plaintext highlighter-rouge">0x06</code> (<code class="language-plaintext highlighter-rouge">PING</code>): Connection keep-alive request.</li>
  <li><code class="language-plaintext highlighter-rouge">0x07</code> (<code class="language-plaintext highlighter-rouge">PONG</code>): Connection keep-alive response.</li>
</ul>

<p>The 8-byte <code class="language-plaintext highlighter-rouge">Sequence</code> field contains a monotonic 64-bit integer maintained by the broker using LMDB, ensuring continuity across process restarts.</p>

<hr />

<h2 id="python-client-implementation">Python Client Implementation</h2>

<p>The demo client uses standard Python libraries (<code class="language-plaintext highlighter-rouge">socket</code>, <code class="language-plaintext highlighter-rouge">struct</code>) to construct protocol frames directly.</p>

<h3 id="framing-utility">Framing Utility</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">socket</span><span class="p">,</span> <span class="n">struct</span><span class="p">,</span> <span class="n">threading</span><span class="p">,</span> <span class="n">time</span><span class="p">,</span> <span class="n">json</span><span class="p">,</span> <span class="n">random</span>
<span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>

<span class="n">HOST</span> <span class="o">=</span> <span class="sh">"</span><span class="s">127.0.0.1</span><span class="sh">"</span>
<span class="n">PORT</span> <span class="o">=</span> <span class="mi">6379</span>
<span class="n">HEADER_SIZE</span>    <span class="o">=</span> <span class="mi">16</span>
<span class="n">PROTO_VERSION</span>  <span class="o">=</span> <span class="mh">0x01</span>
<span class="n">CMD_SUBSCRIBE</span>  <span class="o">=</span> <span class="mh">0x01</span>
<span class="n">CMD_PUBLISH</span>    <span class="o">=</span> <span class="mh">0x03</span>
<span class="n">CMD_MESSAGE</span>    <span class="o">=</span> <span class="mh">0x04</span>
<span class="n">CMD_PING</span>       <span class="o">=</span> <span class="mh">0x06</span>
<span class="n">CMD_PONG</span>       <span class="o">=</span> <span class="mh">0x07</span>

<span class="k">def</span> <span class="nf">build_frame</span><span class="p">(</span><span class="n">cmd</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="o">=</span><span class="sa">b</span><span class="sh">""</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">topic</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">p</span> <span class="o">=</span> <span class="n">payload</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="nb">str</span><span class="p">)</span> <span class="k">else</span> <span class="n">payload</span>
    <span class="k">return</span> <span class="n">struct</span><span class="p">.</span><span class="nf">pack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">PROTO_VERSION</span><span class="p">,</span> <span class="n">cmd</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">t</span><span class="p">),</span> <span class="nf">len</span><span class="p">(</span><span class="n">p</span><span class="p">))</span> <span class="o">+</span> <span class="n">t</span> <span class="o">+</span> <span class="n">p</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">struct.pack("&gt;BBQHL", ...)</code> builds the header:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">&gt;</code>: Big-endian network byte order.</li>
  <li><code class="language-plaintext highlighter-rouge">B</code>: 1-byte protocol version (<code class="language-plaintext highlighter-rouge">0x01</code>).</li>
  <li><code class="language-plaintext highlighter-rouge">B</code>: 1-byte command code.</li>
  <li><code class="language-plaintext highlighter-rouge">Q</code>: 8-byte unsigned sequence number.</li>
  <li><code class="language-plaintext highlighter-rouge">H</code>: 2-byte unsigned topic length.</li>
  <li><code class="language-plaintext highlighter-rouge">L</code>: 4-byte unsigned payload length.</li>
</ul>

<h3 id="socket-io-helpers">Socket I/O Helpers</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">n</span><span class="p">):</span>
    <span class="n">buf</span> <span class="o">=</span> <span class="nf">bytearray</span><span class="p">()</span>
    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">:</span>
        <span class="n">chunk</span> <span class="o">=</span> <span class="n">sock</span><span class="p">.</span><span class="nf">recv</span><span class="p">(</span><span class="n">n</span> <span class="o">-</span> <span class="nf">len</span><span class="p">(</span><span class="n">buf</span><span class="p">))</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">chunk</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">EOFError</span><span class="p">()</span>
        <span class="n">buf</span> <span class="o">+=</span> <span class="n">chunk</span>
    <span class="k">return</span> <span class="nf">bytes</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">read_frame</span><span class="p">(</span><span class="n">sock</span><span class="p">):</span>
    <span class="n">raw</span> <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">HEADER_SIZE</span><span class="p">)</span>
    <span class="n">ver</span><span class="p">,</span> <span class="n">cmd</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">tlen</span><span class="p">,</span> <span class="n">plen</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">raw</span><span class="p">)</span>
    <span class="n">topic</span>   <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">tlen</span><span class="p">).</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">if</span> <span class="n">tlen</span> <span class="k">else</span> <span class="sh">""</span>
    <span class="n">payload</span> <span class="o">=</span> <span class="nf">recv_exactly</span><span class="p">(</span><span class="n">sock</span><span class="p">,</span> <span class="n">plen</span><span class="p">)</span>                  <span class="k">if</span> <span class="n">plen</span> <span class="k">else</span> <span class="sa">b</span><span class="sh">""</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">:</span> <span class="n">cmd</span><span class="p">,</span> <span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">:</span> <span class="n">seq</span><span class="p">,</span> <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="n">topic</span><span class="p">,</span> <span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">recv_exactly</code> handles TCP packet segmentation by reading until the buffer reaches requested length <code class="language-plaintext highlighter-rouge">n</code>.</p>

<h3 id="client-class-structure">Client Class Structure</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Client</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">name</span>     <span class="o">=</span> <span class="n">name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span>     <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_lock</span>    <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Lock</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">received</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">sock</span>     <span class="o">=</span> <span class="n">socket</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="n">socket</span><span class="p">.</span><span class="n">AF_INET</span><span class="p">,</span> <span class="n">socket</span><span class="p">.</span><span class="n">SOCK_STREAM</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">connect</span><span class="p">((</span><span class="n">HOST</span><span class="p">,</span> <span class="n">PORT</span><span class="p">))</span>

    <span class="k">def</span> <span class="nf">_send</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">data</span><span class="p">):</span>
        <span class="k">with</span> <span class="n">self</span><span class="p">.</span><span class="n">_lock</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">sendall</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">subscribe</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">topic</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_SUBSCRIBE</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">_seq</span><span class="p">))</span>

    <span class="k">def</span> <span class="nf">publish</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_seq</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_PUBLISH</span><span class="p">,</span> <span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">_seq</span><span class="p">))</span>

    <span class="k">def</span> <span class="nf">listen</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">stop_event</span><span class="p">):</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="k">while</span> <span class="ow">not</span> <span class="n">stop_event</span><span class="p">.</span><span class="nf">is_set</span><span class="p">():</span>
                <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">settimeout</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>
                <span class="k">try</span><span class="p">:</span>
                    <span class="n">frame</span> <span class="o">=</span> <span class="nf">read_frame</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">)</span>
                    <span class="k">if</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="n">CMD_MESSAGE</span><span class="p">:</span>
                        <span class="n">inner</span> <span class="o">=</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">]</span>
                        <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">inner</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="n">HEADER_SIZE</span><span class="p">:</span>
                            <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">tlen</span><span class="p">,</span> <span class="n">plen</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack</span><span class="p">(</span><span class="sh">"</span><span class="s">&gt;BBQHL</span><span class="sh">"</span><span class="p">,</span> <span class="n">inner</span><span class="p">[:</span><span class="n">HEADER_SIZE</span><span class="p">])</span>
                            <span class="n">off</span>     <span class="o">=</span> <span class="n">HEADER_SIZE</span>
                            <span class="n">topic</span>   <span class="o">=</span> <span class="n">inner</span><span class="p">[</span><span class="n">off</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="p">].</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                            <span class="n">payload</span> <span class="o">=</span> <span class="n">inner</span><span class="p">[</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="n">tlen</span><span class="o">+</span><span class="n">plen</span><span class="p">].</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                        <span class="k">else</span><span class="p">:</span>
                            <span class="n">topic</span>   <span class="o">=</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">]</span>
                            <span class="n">payload</span> <span class="o">=</span> <span class="n">inner</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">,</span> <span class="n">errors</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">)</span>
                        <span class="n">self</span><span class="p">.</span><span class="n">received</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="n">topic</span><span class="p">,</span> <span class="sh">"</span><span class="s">payload</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">,</span> <span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">:</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">]})</span>
                    <span class="k">elif</span> <span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">cmd</span><span class="sh">"</span><span class="p">]</span> <span class="o">==</span> <span class="n">CMD_PING</span><span class="p">:</span>
                        <span class="n">self</span><span class="p">.</span><span class="nf">_send</span><span class="p">(</span><span class="nf">build_frame</span><span class="p">(</span><span class="n">CMD_PONG</span><span class="p">,</span> <span class="sh">""</span><span class="p">,</span> <span class="n">seq</span><span class="o">=</span><span class="n">frame</span><span class="p">[</span><span class="sh">"</span><span class="s">seq</span><span class="sh">"</span><span class="p">]))</span>
                <span class="k">except</span> <span class="n">socket</span><span class="p">.</span><span class="n">timeout</span><span class="p">:</span>
                    <span class="k">continue</span>
        <span class="nf">except </span><span class="p">(</span><span class="nb">EOFError</span><span class="p">,</span> <span class="nb">OSError</span><span class="p">):</span>
            <span class="k">pass</span>

    <span class="k">def</span> <span class="nf">close</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">sock</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="k">except</span> <span class="nb">OSError</span><span class="p">:</span>
            <span class="k">pass</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">listen()</code> runs in a background thread with a 1.0-second socket timeout to permit non-blocking shutdown checks via <code class="language-plaintext highlighter-rouge">stop_event</code>. It also handles broker <code class="language-plaintext highlighter-rouge">PING</code> requests by returning a <code class="language-plaintext highlighter-rouge">PONG</code> response frame.</p>

<hr />

<h2 id="core-feature-demos">Core Feature Demos</h2>

<p>Run the script directly or in a PowerShell loop to generate continuous traffic for Grafana:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kr">while</span><span class="w"> </span><span class="p">(</span><span class="bp">$true</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">python</span><span class="w"> </span><span class="nx">metricmq_demo.py</span><span class="p">;</span><span class="w"> </span><span class="n">Start-Sleep</span><span class="w"> </span><span class="nx">2</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="1-basic-publish--subscribe">1. Basic Publish / Subscribe</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_pubsub</span><span class="p">():</span>
    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">subscriber-1</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">publisher-1</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>

    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">6</span><span class="p">):</span>
        <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">msg</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">,</span> <span class="sh">"</span><span class="s">temp</span><span class="sh">"</span><span class="p">:</span> <span class="nf">round</span><span class="p">(</span><span class="mf">20.0</span> <span class="o">+</span> <span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">),</span> <span class="mi">1</span><span class="p">),</span> <span class="sh">"</span><span class="s">unit</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">C</span><span class="sh">"</span><span class="p">})</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → sensors/temperature  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">  ✅ Received </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">)</span><span class="si">}</span><span class="s">/5 messages</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">     [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] RECV ← </span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">topic</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">payload</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>Sample console output:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[00:02:40] PUB  → sensors/temperature  {"msg": 1, "temp": 22.3, "unit": "C"}
[00:02:40] PUB  → sensors/temperature  {"msg": 2, "temp": 21.8, "unit": "C"}
...
✅ Received 5/5 messages
[00:02:43] RECV ← sensors/temperature  {"msg": 1, "temp": 22.3, "unit": "C"}
</code></pre></div></div>

<h3 id="2-wildcard-topic-filtering">2. Wildcard Topic Filtering</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_wildcard</span><span class="p">():</span>
    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">wildcard-sub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">wildcard-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>

    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">sensors/#</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="n">topics</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/humidity</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/pressure</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">sensors/co2</span><span class="sh">"</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">topic</span> <span class="ow">in</span> <span class="n">topics</span><span class="p">:</span>
        <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">value</span><span class="sh">"</span><span class="p">:</span> <span class="nf">round</span><span class="p">(</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="mi">10</span><span class="p">,</span> <span class="mi">100</span><span class="p">),</span> <span class="mi">1</span><span class="p">)})</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="n">topic</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → </span><span class="si">{</span><span class="n">topic</span><span class="si">}</span><span class="s">  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.4</span><span class="p">)</span>
</code></pre></div></div>

<p>The multi-level wildcard <code class="language-plaintext highlighter-rouge">#</code> matches any hierarchy trailing the specified topic prefix.</p>

<h3 id="3-subscriber-fan-out">3. Subscriber Fan-Out</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_fanout</span><span class="p">():</span>
    <span class="n">N</span>    <span class="o">=</span> <span class="mi">5</span>
    <span class="n">subs</span> <span class="o">=</span> <span class="p">[</span><span class="nc">Client</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">fan-sub-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">)]</span>
    <span class="n">pub</span>  <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">fan-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stops</span> <span class="o">=</span> <span class="p">[</span><span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">)]</span>

    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">sub</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">subs</span><span class="p">):</span>
        <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stops</span><span class="p">[</span><span class="n">i</span><span class="p">],),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>
        <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">broadcast/alert</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">alert</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">high_temp</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">value</span><span class="sh">"</span><span class="p">:</span> <span class="mf">87.3</span><span class="p">})</span>
    <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">broadcast/alert</span><span class="sh">"</span><span class="p">,</span> <span class="n">payload</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] PUB  → broadcast/alert  </span><span class="si">{</span><span class="n">payload</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>

    <span class="n">received</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="mi">1</span> <span class="k">for</span> <span class="n">s</span> <span class="ow">in</span> <span class="n">subs</span> <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">s</span><span class="p">.</span><span class="n">received</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">  ✅ Fan-out: </span><span class="si">{</span><span class="n">received</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">N</span><span class="si">}</span><span class="s"> subscribers received the message</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The broker duplicates published messages across all matching subscriber sockets. In Prometheus, <code class="language-plaintext highlighter-rouge">metricmq_messages_published_total</code> increments by 1 while <code class="language-plaintext highlighter-rouge">metricmq_messages_delivered_total</code> increments by <code class="language-plaintext highlighter-rouge">N</code> (5).</p>

<h3 id="4-sequence-number-verification">4. Sequence Number Verification</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_sequence</span><span class="p">():</span>
    <span class="n">sub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">seq-sub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">seq-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">stop</span> <span class="o">=</span> <span class="n">threading</span><span class="p">.</span><span class="nc">Event</span><span class="p">()</span>
    <span class="n">threading</span><span class="p">.</span><span class="nc">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">sub</span><span class="p">.</span><span class="n">listen</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">stop</span><span class="p">,),</span> <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="nf">start</span><span class="p">()</span>
    <span class="n">sub</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="sh">"</span><span class="s">stream/data</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.5</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">8</span><span class="p">):</span>
        <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="sh">"</span><span class="s">stream/data</span><span class="sh">"</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">index</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">}))</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.05</span><span class="p">)</span>

    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">1.0</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  ✅ Received </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">)</span><span class="si">}</span><span class="s">/8 in order:</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">sub</span><span class="p">.</span><span class="n">received</span><span class="p">:</span>
        <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">payload</span><span class="sh">'</span><span class="p">])</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">     broker_seq=</span><span class="si">{</span><span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">seq</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="s">  index=</span><span class="si">{</span><span class="n">data</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">'</span><span class="s">index</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">?</span><span class="sh">'</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>Sample output displaying sequential monotonically increasing sequence values:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>✅ Received 8/8 in order:
   broker_seq=4808  index=0
   broker_seq=4809  index=1
   ...
   broker_seq=4815  index=7
</code></pre></div></div>

<p>If the broker process restarts, sequence state is reloaded from LMDB storage:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Restored sequence counter from persistence: 4816
</code></pre></div></div>

<h3 id="5-topic-metrics-and-prometheus-labeling">5. Topic Metrics and Prometheus Labeling</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_multi_topic</span><span class="p">():</span>
    <span class="n">pub</span> <span class="o">=</span> <span class="nc">Client</span><span class="p">(</span><span class="sh">"</span><span class="s">topic-pub</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">topics</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">sensors/temperature</span><span class="sh">"</span><span class="p">:</span> <span class="mi">10</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">sensors/humidity</span><span class="sh">"</span><span class="p">:</span>     <span class="mi">6</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">commands/actuator</span><span class="sh">"</span><span class="p">:</span>    <span class="mi">4</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">logs/system</span><span class="sh">"</span><span class="p">:</span>          <span class="mi">8</span><span class="p">,</span>
    <span class="p">}</span>
    <span class="k">for</span> <span class="n">topic</span><span class="p">,</span> <span class="n">count</span> <span class="ow">in</span> <span class="n">topics</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">count</span><span class="p">):</span>
            <span class="n">pub</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="n">topic</span><span class="p">,</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span><span class="sh">"</span><span class="s">i</span><span class="sh">"</span><span class="p">:</span> <span class="n">i</span><span class="p">}))</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">count</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s"> msgs → </span><span class="si">{</span><span class="n">topic</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.2</span><span class="p">)</span>
</code></pre></div></div>

<p>Querying <code class="language-plaintext highlighter-rouge">metricmq_topic_messages_total</code> in Prometheus returns metrics labeled by topic string:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>metricmq_topic_messages_total{topic="sensors/temperature"} 10
metricmq_topic_messages_total{topic="sensors/humidity"}    6
metricmq_topic_messages_total{topic="commands/actuator"}   4
metricmq_topic_messages_total{topic="logs/system"}         8
</code></pre></div></div>

<h3 id="6-active-connection-monitoring">6. Active Connection Monitoring</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">demo_connections</span><span class="p">():</span>
    <span class="n">clients</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">5</span><span class="p">):</span>
        <span class="n">clients</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Client</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">conn-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">))</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] +1 connected  (total: </span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.3</span><span class="p">)</span>

    <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">c</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">clients</span><span class="p">):</span>
        <span class="n">c</span><span class="p">.</span><span class="nf">close</span><span class="p">()</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="nf">ts</span><span class="p">()</span><span class="si">}</span><span class="s">] -1 disconnected  (remaining: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">clients</span><span class="p">)</span><span class="o">-</span><span class="n">i</span><span class="o">-</span><span class="mi">1</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mf">0.3</span><span class="p">)</span>
</code></pre></div></div>

<p>This sequence tests <code class="language-plaintext highlighter-rouge">metricmq_active_connections</code> gauge tracking as connections open and close.</p>

<hr />

<h2 id="grafana-dashboard-configuration">Grafana Dashboard Configuration</h2>

<p>To import the pre-built dashboard:</p>

<ol>
  <li>Open <code class="language-plaintext highlighter-rouge">http://localhost:3001/dashboard/import</code>.</li>
  <li>Paste the following JSON dashboard model into the text box and click <strong>Import</strong>:</li>
</ol>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"MetricMQ"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"uid"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"schemaVersion"</span><span class="p">:</span><span class="w"> </span><span class="mi">36</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"panels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Active Connections"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_active_connections"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Active Subscribers"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_active_subscribers"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">3</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Total Published"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_messages_published_total"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"stat"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Total Delivered"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"metricmq_messages_delivered_total"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">5</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Publish Rate (msg/s)"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_published_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"publish rate"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">6</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"P99 Publish Latency (s)"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"histogram_quantile(0.99, rate(metricmq_publish_latency_seconds_bucket[1m]))"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"p99"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">7</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Messages per Topic"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_topic_messages_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">""</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">}]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">,</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"timeseries"</span><span class="p">,</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Published vs Delivered"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"gridPos"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="nl">"x"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"y"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"w"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"h"</span><span class="p">:</span><span class="w"> </span><span class="mi">8</span><span class="p">},</span><span class="w">
      </span><span class="nl">"datasource"</span><span class="p">:</span><span class="w"> </span><span class="s2">"prometheus"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"targets"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_published_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"published"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A"</span><span class="p">},</span><span class="w">
        </span><span class="p">{</span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"rate(metricmq_messages_delivered_total[1m])"</span><span class="p">,</span><span class="w"> </span><span class="nl">"legendFormat"</span><span class="p">:</span><span class="w"> </span><span class="s2">"delivered"</span><span class="p">,</span><span class="w"> </span><span class="nl">"refId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"B"</span><span class="p">}</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Adjust the Grafana dashboard time range to <strong>Last 15 minutes</strong> for detailed view of test bursts.</p>

<p><img src="/cover_images/posts/metricmq-grafana.png" alt="" /></p>

<hr />

<h2 id="promql-queries-for-verification">PromQL Queries for Verification</h2>

<p>Useful queries for checking metric states in Prometheus (<code class="language-plaintext highlighter-rouge">http://localhost:9090</code>):</p>

<pre><code class="language-promql"># Overall publish throughput in msg/sec
rate(metricmq_messages_published_total[1m])

# 99th percentile publish latency
histogram_quantile(0.99, rate(metricmq_publish_latency_seconds_bucket[1m]))

# Message throughput grouped by topic
sum by (topic) (rate(metricmq_topic_messages_total[1m]))

# Unacknowledged or dropped message ratio (0 under normal operation)
1 - (metricmq_messages_delivered_total / metricmq_messages_published_total)

# Client connection attempt rate
rate(metricmq_connections_total[5m])

# Signature validation failure rate
rate(metricmq_errors_total[1m])
</code></pre>

<hr />

<h2 id="source-code">Source Code</h2>

<p>Repository and build instructions for MetricMQ are available on GitHub: <a href="https://github.com/Saptarshi-max/MetricMQ">github.com/Saptarshi-max/MetricMQ</a></p>

<p>Repository and build instructions for MetricMQ’s Demo files are available on Github: https://github.com/Saptarshi-max/MetricMQ-Demo-Scripts</p>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="es" /><category term="c++" /><summary type="html"><![CDATA[MetricMQ is a 328 KB C++20 message broker featuring a 16-byte binary protocol, Ed25519 message signing, LMDB sequence persistence, native Prometheus metrics, and an ESP32 client. This guide covers running the broker, testing Python demos, and visualizing real-time metrics with Prometheus and Grafana.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/grafana_metricmq.webp" /><media:content medium="image" url="/cover_images/posts/grafana_metricmq.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Getting Started with MetricMQ on ESP32</title><link href="/blog/es/2026-05-15-metricmq-getting-started-1-esp32/" rel="alternate" type="text/html" title="Getting Started with MetricMQ on ESP32" /><published>2026-05-15T00:00:00+00:00</published><updated>2026-07-24T19:54:30+00:00</updated><id>/blog/es/metricmq-getting-started-1-esp32</id><content type="html" xml:base="/blog/es/2026-05-15-metricmq-getting-started-1-esp32/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#link-to-the-github-repo" id="markdown-toc-link-to-the-github-repo">Link to the Github Repo</a></li>
  <li><a href="#features" id="markdown-toc-features">Features</a></li>
  <li><a href="#the-demos-presented-in-this-post" id="markdown-toc-the-demos-presented-in-this-post">The Demos presented in this Post</a></li>
  <li><a href="#what-is-a-message-broker" id="markdown-toc-what-is-a-message-broker">What Is a Message Broker?</a>    <ul>
      <li><a href="#topics-and-wildcards" id="markdown-toc-topics-and-wildcards">Topics and Wildcards</a></li>
    </ul>
  </li>
  <li><a href="#setting-up-the-broker" id="markdown-toc-setting-up-the-broker">Setting Up the Broker</a>    <ul>
      <li><a href="#what-you-need" id="markdown-toc-what-you-need">What You Need</a></li>
      <li><a href="#build-the-broker" id="markdown-toc-build-the-broker">Build the Broker</a></li>
      <li><a href="#what-gets-built" id="markdown-toc-what-gets-built">What Gets Built</a></li>
      <li><a href="#windows-antivirus---expected-false-positive" id="markdown-toc-windows-antivirus---expected-false-positive">Windows Antivirus - Expected False Positive</a></li>
      <li><a href="#start-the-broker" id="markdown-toc-start-the-broker">Start the Broker</a></li>
      <li><a href="#open-the-firewall" id="markdown-toc-open-the-firewall">Open the Firewall</a></li>
      <li><a href="#install-redis-cli-and-verify" id="markdown-toc-install-redis-cli-and-verify">Install redis-cli and Verify</a></li>
      <li><a href="#find-your-pcs-ip-address" id="markdown-toc-find-your-pcs-ip-address">Find Your PC’s IP Address</a></li>
    </ul>
  </li>
  <li><a href="#setting-up-the-esp32-library" id="markdown-toc-setting-up-the-esp32-library">Setting Up the ESP32 Library</a>    <ul>
      <li><a href="#installing-the-library" id="markdown-toc-installing-the-library">Installing the Library</a></li>
      <li><a href="#the-platformioini-for-all-demos" id="markdown-toc-the-platformioini-for-all-demos">The platformio.ini for All Demos</a></li>
      <li><a href="#the-two-call-connect-pattern" id="markdown-toc-the-two-call-connect-pattern">The Two-Call Connect Pattern</a></li>
      <li><a href="#the-callback-signature" id="markdown-toc-the-callback-signature">The Callback Signature</a></li>
    </ul>
  </li>
  <li><a href="#demo-1-basic-pubsub--hello-broker" id="markdown-toc-demo-1-basic-pubsub--hello-broker">Demo 1: Basic Pub/Sub — “Hello, Broker”</a>    <ul>
      <li><a href="#srcmaincpp" id="markdown-toc-srcmaincpp">src/main.cpp</a></li>
      <li><a href="#how-to-run-it" id="markdown-toc-how-to-run-it">How to Run It</a></li>
      <li><a href="#what-to-expect" id="markdown-toc-what-to-expect">What to Expect</a></li>
      <li><a href="#what-the-broker-metrics-show" id="markdown-toc-what-the-broker-metrics-show">What the Broker Metrics Show</a></li>
    </ul>
  </li>
  <li><a href="#demo-2-exactly-once-delivery--surviving-a-network-drop" id="markdown-toc-demo-2-exactly-once-delivery--surviving-a-network-drop">Demo 2: Exactly-Once Delivery — Surviving a Network Drop</a>    <ul>
      <li><a href="#how-it-works" id="markdown-toc-how-it-works">How It Works</a></li>
      <li><a href="#srcmaincpp-1" id="markdown-toc-srcmaincpp-1">src/main.cpp</a></li>
      <li><a href="#what-to-expect-1" id="markdown-toc-what-to-expect-1">What to Expect</a></li>
      <li><a href="#what-the-broker-metrics-show-1" id="markdown-toc-what-the-broker-metrics-show-1">What the Broker Metrics Show</a></li>
    </ul>
  </li>
  <li><a href="#demo-3-fan-out-with-wildcards--one-publish-many-subscribers" id="markdown-toc-demo-3-fan-out-with-wildcards--one-publish-many-subscribers">Demo 3: Fan-Out with Wildcards — One Publish, Many Subscribers</a>    <ul>
      <li><a href="#srcmaincpp-2" id="markdown-toc-srcmaincpp-2">src/main.cpp</a></li>
      <li><a href="#what-to-expect-2" id="markdown-toc-what-to-expect-2">What to Expect</a></li>
    </ul>
  </li>
  <li><a href="#demo-4-signed-messages--proving-your-data-is-real" id="markdown-toc-demo-4-signed-messages--proving-your-data-is-real">Demo 4: Signed Messages — Proving Your Data Is Real</a>    <ul>
      <li><a href="#why-this-matters" id="markdown-toc-why-this-matters">Why This Matters</a></li>
      <li><a href="#step-1-generate-a-key-pair" id="markdown-toc-step-1-generate-a-key-pair">Step 1: Generate a Key Pair</a></li>
      <li><a href="#step-2-register-the-public-key-on-the-broker" id="markdown-toc-step-2-register-the-public-key-on-the-broker"><strong>Step 2:</strong> Register the Public Key on the Broker</a></li>
      <li><a href="#srcmaincpp-3" id="markdown-toc-srcmaincpp-3">src/main.cpp</a></li>
      <li><a href="#what-to-expect-3" id="markdown-toc-what-to-expect-3">What to Expect</a></li>
      <li><a href="#what-the-broker-metrics-show-2" id="markdown-toc-what-the-broker-metrics-show-2">What the Broker Metrics Show</a></li>
    </ul>
  </li>
  <li><a href="#prometheus--grafana-seeing-your-system-live" id="markdown-toc-prometheus--grafana-seeing-your-system-live">Prometheus + Grafana: Seeing Your System Live</a>    <ul>
      <li><a href="#what-value-does-this-actually-serve" id="markdown-toc-what-value-does-this-actually-serve">What Value Does This Actually Serve?</a></li>
      <li><a href="#the-metrics-that-matter" id="markdown-toc-the-metrics-that-matter">The Metrics That Matter</a></li>
      <li><a href="#setting-up-prometheus-10-minutes" id="markdown-toc-setting-up-prometheus-10-minutes">Setting Up Prometheus (10 Minutes)</a></li>
      <li><a href="#setting-up-grafana-10-more-minutes" id="markdown-toc-setting-up-grafana-10-more-minutes">Setting Up Grafana (10 More Minutes)</a></li>
      <li><a href="#what-each-alert-catches-in-a-real-deployment" id="markdown-toc-what-each-alert-catches-in-a-real-deployment">What Each Alert Catches in a Real Deployment</a></li>
    </ul>
  </li>
  <li><a href="#how-to-navigate-the-metricmq-docs" id="markdown-toc-how-to-navigate-the-metricmq-docs">How to Navigate the MetricMQ Docs</a>    <ul>
      <li><a href="#if-youre-just-getting-started" id="markdown-toc-if-youre-just-getting-started">If You’re Just Getting Started</a></li>
      <li><a href="#if-you-want-to-understand-the-wire-protocol" id="markdown-toc-if-you-want-to-understand-the-wire-protocol">If You Want to Understand the Wire Protocol</a></li>
      <li><a href="#if-youre-integrating-metricmq-into-an-existing-project" id="markdown-toc-if-youre-integrating-metricmq-into-an-existing-project">If You’re Integrating MetricMQ Into an Existing Project</a></li>
      <li><a href="#if-youre-evaluating-for-a-production-deployment" id="markdown-toc-if-youre-evaluating-for-a-production-deployment">If You’re Evaluating for a Production Deployment</a></li>
      <li><a href="#if-youre-running-tests" id="markdown-toc-if-youre-running-tests">If You’re Running Tests</a></li>
    </ul>
  </li>
</ul>

<h2 id="link-to-the-github-repo">Link to the Github Repo</h2>

<div class="note">
  <ul>
    <li>MetricMQ Github REPO - <a href="https://github.com/Saptarshi-max/MetricMQ">https://github.com/Saptarshi-max/MetricMQ</a></li>
    <li>MetricMQ Documenation - <a href="https://metricmq-docs.netlify.app/">https://metricmq-docs.netlify.app/</a></li>
    <li>MetricMQ My Project Page - <a href="https://www.sapnag.me/projects/2025-11-24-metricMQ/">https://www.sapnag.me/projects/2025-11-24-metricMQ/</a></li>
  </ul>

</div>
<div class="warning">

  <p>MetricMQ is still in its early beta stage, If you encounter any issues please report it at <a href="https://github.com/Saptarshi-max/MetricMQ/issues">https://github.com/Saptarshi-max/MetricMQ/issues</a>  that will help in making the lib better</p>
</div>

<p><strong>Key characteristics:</strong></p>
<ul>
  <li>328 KB binary size</li>
  <li>Embedded LMDB storage</li>
  <li>Dual protocol support (RESP + Binary)</li>
  <li>Runs on ESP32/ESP8266 and desktop platforms</li>
  <li>106K msg/s throughput (measured, 10KB messages)</li>
</ul>

<h2 id="features">Features</h2>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Dual Protocol</td>
      <td>RESP (Redis-compatible) + Binary Protocol</td>
    </tr>
    <tr>
      <td>Protocol Detection</td>
      <td>Automatic detection on first byte</td>
    </tr>
    <tr>
      <td>Pub/Sub</td>
      <td>Topic-based routing with wildcards</td>
    </tr>
    <tr>
      <td>Queue Mode</td>
      <td>PUSH/PULL with round-robin distribution</td>
    </tr>
    <tr>
      <td>Exactly-Once Delivery</td>
      <td>Sequence IDs with ACK tracking</td>
    </tr>
    <tr>
      <td>Persistence</td>
      <td>LMDB embedded database</td>
    </tr>
    <tr>
      <td>Metrics</td>
      <td>Prometheus endpoint on port 9091</td>
    </tr>
    <tr>
      <td>Platforms</td>
      <td>Windows, Linux, macOS, ESP32, ESP8266</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="the-demos-presented-in-this-post">The Demos presented in this Post</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Concepts]  What is pub/sub? Why not just MQTT?
[Setup]     Build the broker on your PC. Verify with redis-cli.
[Demo 1]    Pub/Sub  — ESP32 publishes → PC receives live
[Demo 2]    Exactly-Once — unplug mid-stream → reconnect → missed messages return
[Demo 3]    Fan-Out — one publish → multiple subscribers simultaneously
[Demo 4]    Signed Messages — broker rejects fake data cryptographically
</code></pre></div></div>

<p>Each demo has its own complete <code class="language-plaintext highlighter-rouge">platformio.ini</code> and sketch. Skip to any section independently or work through in order.</p>

<hr />

<h2 id="what-is-a-message-broker">What Is a Message Broker?</h2>

<blockquote>
  <p><strong>Already know pub/sub and MQTT? <a href="#setting-up-the-broker">Skip to Setup.</a></strong></p>
</blockquote>

<p>A <strong>message broker</strong> is a program that sits between devices that produce data (publishers) and devices or services that consume it (subscribers). Think of it like a post office: publishers drop messages off addressed to a topic, the broker sorts and routes them, subscribers collect what matches their subscription.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ESP32 (Publisher)                         PC Terminal (Subscriber)
      │                                           │
      │  publish("sensors/temp", "23.4")          │
      │─────────────► [Broker] ──────────────────►│
      │                                           │
      │                               receives:   │
      │                       "sensors/temp"      │
      │                           "23.4"          │
</code></pre></div></div>

<p>This pattern — pub/sub — means the ESP32 doesn’t need to know how many things are listening or where they are. It just publishes. New subscribers can join at any time without touching the firmware.</p>

<h3 id="topics-and-wildcards">Topics and Wildcards</h3>

<p>Topics are strings that work like file paths, using <code class="language-plaintext highlighter-rouge">/</code> as a separator:</p>

<table>
  <thead>
    <tr>
      <th>Subscription</th>
      <th>What it receives</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sensors/temperature</code></td>
      <td>Only exactly that topic</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sensors/+</code></td>
      <td><code class="language-plaintext highlighter-rouge">sensors/temperature</code>, <code class="language-plaintext highlighter-rouge">sensors/humidity</code> — any single segment</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sensors/#</code></td>
      <td>Everything under <code class="language-plaintext highlighter-rouge">sensors/</code> at any depth</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">#</code></td>
      <td>Every single message on the broker</td>
    </tr>
  </tbody>
</table>

<p>#</p>

<h2 id="setting-up-the-broker">Setting Up the Broker</h2>

<p>The broker runs on your PC. Your ESP32 connects to it over your local WiFi. The steps below are verified on <strong>Windows 11 with Visual Studio 2022</strong>.</p>

<h3 id="what-you-need">What You Need</h3>

<ul>
  <li><strong>Windows:</strong> Visual Studio 2022 Community (free) with “Desktop development with C++” workload</li>
  <li><strong>Linux:</strong> <code class="language-plaintext highlighter-rouge">sudo apt install build-essential</code></li>
  <li><strong>macOS:</strong> <code class="language-plaintext highlighter-rouge">xcode-select --install</code></li>
  <li><strong>CMake 3.20+</strong> — <a href="https://cmake.org/download">cmake.org/download</a></li>
  <li><strong>Conan 2.x</strong> — <code class="language-plaintext highlighter-rouge">pip install conan</code> then <code class="language-plaintext highlighter-rouge">conan profile detect</code></li>
</ul>

<h3 id="build-the-broker">Build the Broker</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">git</span><span class="w"> </span><span class="nx">clone</span><span class="w"> </span><span class="nx">https://github.com/Saptarshi-max/MetricMQ.git</span><span class="w">
</span><span class="n">cd</span><span class="w"> </span><span class="nx">MetricMQ</span><span class="w">
</span><span class="n">mkdir</span><span class="w"> </span><span class="nx">build</span><span class="w">
</span><span class="n">cd</span><span class="w"> </span><span class="nx">build</span><span class="w">
</span><span class="n">conan</span><span class="w"> </span><span class="nx">install</span><span class="w"> </span><span class="o">..</span><span class="w"> </span><span class="nt">--build</span><span class="o">=</span><span class="n">missing</span><span class="w"> </span><span class="nt">-of</span><span class="w"> </span><span class="o">.</span><span class="w">
</span><span class="n">cd</span><span class="w"> </span><span class="o">..</span><span class="w">
</span><span class="n">cmake</span><span class="w"> </span><span class="nt">-S</span><span class="w"> </span><span class="o">.</span><span class="w"> </span><span class="nt">-B</span><span class="w"> </span><span class="nx">build</span><span class="w"> </span><span class="nt">-DCMAKE_TOOLCHAIN_FILE</span><span class="o">=</span><span class="s2">"build/conan_toolchain.cmake"</span><span class="w"> </span><span class="nt">-DCMAKE_BUILD_TYPE</span><span class="o">=</span><span class="n">Release</span><span class="w">
</span><span class="nx">cmake</span><span class="w"> </span><span class="nt">--build</span><span class="w"> </span><span class="nx">build</span><span class="w"> </span><span class="nt">--config</span><span class="w"> </span><span class="nx">Release</span><span class="w">
</span></code></pre></div></div>

<p>The first build takes 5–15 minutes while Conan downloads libsodium, LMDB, Boost, spdlog, and Google Benchmark. Subsequent builds use the local cache and are near-instant.</p>

<p>You will see <code class="language-plaintext highlighter-rouge">'pwsh.exe' is not recognized</code> warnings during the build — these are harmless. The CMakeLists.txt has optional post-build scripts that look for PowerShell Core, which is different from standard <code class="language-plaintext highlighter-rouge">powershell.exe</code>. They don’t affect any output binaries.</p>

<h3 id="what-gets-built">What Gets Built</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dir</span><span class="w"> </span><span class="nx">build\Release\</span><span class="o">*.</span><span class="nf">exe</span><span class="w">
</span></code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Binary</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq-broker.exe</code></td>
      <td><strong>The broker — start this first, always</strong></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq-keygen.exe</code></td>
      <td>Ed25519 key pair generator (Demo 4)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">binary_pub_only.exe</code></td>
      <td>Desktop binary publisher for testing</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">binary_sub_only.exe</code></td>
      <td>Desktop binary subscriber for testing</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">exactly_once_test.exe</code></td>
      <td>ACK/replay automated test suite</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">signed_publish_test.exe</code></td>
      <td>Ed25519 crypto test suite</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">latency_benchmark.exe</code></td>
      <td>p50/p99/p99.9 latency measurement</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">throughput_benchmark.exe</code></td>
      <td>msg/s throughput measurement</td>
    </tr>
  </tbody>
</table>

<h3 id="windows-antivirus---expected-false-positive">Windows Antivirus - Expected False Positive</h3>

<p><img src="/cover_images/posts/Screenshot%202026-05-15%20153116.webp" alt="518" /></p>

<p>Avast, Defender, and most Windows antivirus tools will quarantine <code class="language-plaintext highlighter-rouge">metricmq-broker.exe</code> the first time you run it. This is a false positive — the <code class="language-plaintext highlighter-rouge">IDP.Generic</code> detection fires because the binary is unsigned, opens a TCP server socket, and uses a cryptographic library. All three together look suspicious to heuristic scanners even when completely benign.</p>

<p><strong>Fix in Avast:</strong> Quarantine → find <code class="language-plaintext highlighter-rouge">metricmq-broker.exe</code> → Restore → Menu → Settings → Exceptions → add <code class="language-plaintext highlighter-rouge">build\Release\</code>.</p>

<p><strong>Fix in Windows Defender:</strong></p>
<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Add-MpPreference</span><span class="w"> </span><span class="nt">-ExclusionPath</span><span class="w"> </span><span class="s2">"C:\path\to\MetricMQ\build\Release\"</span><span class="w">
</span></code></pre></div></div>

<h3 id="start-the-broker">Start the Broker</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">.</span><span class="n">\build\Release\metricmq-broker.exe</span><span class="w">
</span></code></pre></div></div>

<p>Expected output:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MetricMQ Broker v1.0 — Lightweight Message Queue for IoT

Starting broker on port 6379...
Starting metrics server on port 9091...
Press Ctrl+C for graceful shutdown

[info] Metrics server started on http://0.0.0.0:9091/metrics
[info] Broker listening on 0.0.0.0:6379
Broker listening on port 6379
</code></pre></div></div>

<blockquote>
  <p>The banner may show garbled Unicode characters on some Windows terminals — cosmetic only, does not affect anything.</p>
</blockquote>

<p><strong>Keep this terminal open for all four demos.</strong></p>

<h3 id="open-the-firewall">Open the Firewall</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">New-NetFirewallRule</span><span class="w"> </span><span class="nt">-DisplayName</span><span class="w"> </span><span class="s2">"MetricMQ"</span><span class="w"> </span><span class="se">`
</span><span class="w">    </span><span class="nt">-Direction</span><span class="w"> </span><span class="nx">Inbound</span><span class="w"> </span><span class="nt">-LocalPort</span><span class="w"> </span><span class="nx">6379</span><span class="p">,</span><span class="nx">9091</span><span class="w"> </span><span class="se">`
</span><span class="w">    </span><span class="nt">-Protocol</span><span class="w"> </span><span class="nx">TCP</span><span class="w"> </span><span class="nt">-Action</span><span class="w"> </span><span class="nx">Allow</span><span class="w"> </span><span class="nt">-Profile</span><span class="w"> </span><span class="nx">Any</span><span class="w">
</span></code></pre></div></div>

<h3 id="install-redis-cli-and-verify">Install redis-cli and Verify</h3>

<p><code class="language-plaintext highlighter-rouge">redis-cli</code> is how you interact with the broker from a terminal — you’ll use it in every demo:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">winget</span><span class="w"> </span><span class="nx">install</span><span class="w"> </span><span class="nx">Redis.Redis</span><span class="w">
</span></code></pre></div></div>

<p>Close and reopen PowerShell after installing so the PATH refreshes, then:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">redis-cli</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">6379</span><span class="w"> </span><span class="nx">PING</span><span class="w">
</span><span class="c"># Should return: PONG</span><span class="w">
</span></code></pre></div></div>

<p>If you see <code class="language-plaintext highlighter-rouge">PONG</code>, the broker is running and reachable. If not, fix this before touching the ESP32 — debugging network problems from a terminal is much easier than from Serial Monitor.</p>

<h3 id="find-your-pcs-ip-address">Find Your PC’s IP Address</h3>

<p>Your ESP32 sketch needs your PC’s local network IP:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Windows</span><span class="w">
</span><span class="n">ipconfig</span><span class="w">
</span><span class="c"># Look for "IPv4 Address" under your WiFi adapter</span><span class="w">
</span></code></pre></div></div>

<p>It will look like <code class="language-plaintext highlighter-rouge">192.168.1.X</code> or <code class="language-plaintext highlighter-rouge">10.0.0.X</code>. Your ESP32 and PC must be on the <strong>same WiFi network</strong> — same router, same subnet.</p>

<hr />

<h2 id="setting-up-the-esp32-library">Setting Up the ESP32 Library</h2>

<h3 id="installing-the-library">Installing the Library</h3>

<p>Copy the three files from <code class="language-plaintext highlighter-rouge">esp32-metricmq/src/</code> into your PlatformIO project’s <code class="language-plaintext highlighter-rouge">lib/MetricMQ/</code> folder:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>your-project/
├── lib/
│   └── MetricMQ/
│       ├── MetricMQ.h
│       ├── MetricMQ.cpp
│       └── library.properties
├── src/
│   └── main.cpp
└── platformio.ini
</code></pre></div></div>

<p>No <code class="language-plaintext highlighter-rouge">lib_deps</code> needed — PlatformIO finds libraries in <code class="language-plaintext highlighter-rouge">lib/</code> automatically.</p>

<h3 id="the-platformioini-for-all-demos">The platformio.ini for All Demos</h3>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[env:4d_systems_esp32s3_gen4_r8n16]</span><span class="w">
</span><span class="py">platform</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">espressif32</span>
<span class="py">board</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">4d_systems_esp32s3_gen4_r8n16</span>
<span class="py">framework</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">arduino</span>
<span class="py">monitor_speed</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">115200</span>
<span class="py">monitor_rts</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">0</span>
<span class="py">monitor_dtr</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">0</span>
<span class="py">build_flags</span><span class="w"> </span><span class="p">=</span>
<span class="w">    </span><span class="py">-DCORE_DEBUG_LEVEL</span><span class="p">=</span><span class="s">3</span>
</code></pre></div></div>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">monitor_rts = 0</code> and <code class="language-plaintext highlighter-rouge">monitor_dtr = 0</code> prevent the USB CDC disconnect cycle on Windows that causes the Serial Monitor to repeatedly reconnect after flashing. After uploading, wait 3 seconds before opening the monitor:</p>

  <div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pio</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">--target</span><span class="w"> </span><span class="nx">upload</span><span class="w">
</span><span class="n">Start-Sleep</span><span class="w"> </span><span class="nt">-Seconds</span><span class="w"> </span><span class="nx">3</span><span class="w">
</span><span class="n">pio</span><span class="w"> </span><span class="nx">device</span><span class="w"> </span><span class="nx">monitor</span><span class="w"> </span><span class="nt">--port</span><span class="w"> </span><span class="nx">COM8</span><span class="w"> </span><span class="nt">--baud</span><span class="w"> </span><span class="nx">115200</span><span class="w">
</span></code></pre></div>  </div>
</blockquote>

<h3 id="the-two-call-connect-pattern">The Two-Call Connect Pattern</h3>

<p>Every MetricMQ sketch uses the same two-step connect. This is the most important thing to memorise:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Step 1 — store broker address (does NOT open TCP yet)</span>
<span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="s">"192.168.1.100"</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>

<span class="c1">// Step 2 — open TCP connection with a stable client ID</span>
<span class="c1">// The client ID is how the broker tracks your ACK offset for replay.</span>
<span class="c1">// Use the same ID every boot — changing it means starting fresh.</span>
<span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="s">"my-device-01"</span><span class="p">);</span>
</code></pre></div></div>

<p>And in every <code class="language-plaintext highlighter-rouge">loop()</code>:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// The library does NOT auto-reconnect — you must do this</span>
<span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
    <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>   <span class="c1">// re-set address before reconnecting</span>
    <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>
    <span class="k">return</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// MUST run every loop() iteration — drives the 60s keep-alive PING</span>
<span class="c1">// Long blocking delays will cause the broker to drop the connection</span>
<span class="n">client</span><span class="p">.</span><span class="n">loop</span><span class="p">();</span>
</code></pre></div></div>

<h3 id="the-callback-signature">The Callback Signature</h3>

<p>When you subscribe to a topic, your callback receives four parameters:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">client</span><span class="p">.</span><span class="n">subscribe</span><span class="p">(</span><span class="s">"sensors/#"</span><span class="p">,</span>
    <span class="p">[](</span><span class="k">const</span> <span class="n">String</span><span class="o">&amp;</span> <span class="n">topic</span><span class="p">,</span>    <span class="c1">// topic the message arrived on</span>
       <span class="k">const</span> <span class="kt">uint8_t</span><span class="o">*</span> <span class="n">payload</span><span class="p">,</span> <span class="c1">// raw payload bytes</span>
       <span class="kt">size_t</span> <span class="n">length</span><span class="p">,</span>          <span class="c1">// payload length</span>
       <span class="kt">uint64_t</span> <span class="n">sequence</span><span class="p">)</span>      <span class="c1">// broker-assigned sequence number</span>
    <span class="p">{</span>
        <span class="n">String</span> <span class="n">msg</span><span class="p">((</span><span class="k">const</span> <span class="kt">char</span><span class="o">*</span><span class="p">)</span><span class="n">payload</span><span class="p">,</span> <span class="n">length</span><span class="p">);</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[MSG] %s → %s (seq=%llu)</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
                      <span class="n">topic</span><span class="p">.</span><span class="n">c_str</span><span class="p">(),</span> <span class="n">msg</span><span class="p">.</span><span class="n">c_str</span><span class="p">(),</span> <span class="n">sequence</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">);</span>
</code></pre></div></div>

<p>Note: <code class="language-plaintext highlighter-rouge">topic</code> is <code class="language-plaintext highlighter-rouge">const String&amp;</code>, not <code class="language-plaintext highlighter-rouge">const char*</code>. Use <code class="language-plaintext highlighter-rouge">.c_str()</code> when you need a raw C string.</p>

<hr />

<h2 id="demo-1-basic-pubsub--hello-broker">Demo 1: Basic Pub/Sub — “Hello, Broker”</h2>

<p><strong>What you learn:</strong> Publishing from the ESP32, subscribing from your PC, and seeing cross-protocol interop — the ESP32 speaks binary protocol, <code class="language-plaintext highlighter-rouge">redis-cli</code> speaks RESP text protocol, the broker routes between them transparently.</p>

<p><strong>Hardware:</strong> One ESP32-S3, USB cable, PC running the broker.</p>

<h3 id="srcmaincpp">src/main.cpp</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
 * MetricMQ Demo 1 — Basic Pub/Sub
 *
 * ESP32 publishes a simulated temperature reading every 3 seconds.
 * Subscribe on your PC: redis-cli -p 6379 SUBSCRIBE "sensors/#"
 *
 * The interesting part: the ESP32 uses MetricMQ's 16-byte binary protocol.
 * redis-cli uses RESP (Redis text protocol).
 * The broker detects both from the first byte and routes between them.
 * No configuration on either side.
 */</span>

<span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;WiFi.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;MetricMQ.h&gt;</span><span class="cp">
</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_SSID</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_SSID"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_PASS</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_PASSWORD"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">BROKER_IP</span> <span class="o">=</span> <span class="s">"192.168.1.100"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">CLIENT_ID</span> <span class="o">=</span> <span class="s">"esp32-demo1"</span><span class="p">;</span>

<span class="n">MetricMQClient</span> <span class="n">client</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">msgCount</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">lastPub</span>  <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">connectWiFi</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[WiFi] Connecting to %s"</span><span class="p">,</span> <span class="n">WIFI_SSID</span><span class="p">);</span>
    <span class="n">WiFi</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">WIFI_SSID</span><span class="p">,</span> <span class="n">WIFI_PASS</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">WiFi</span><span class="p">.</span><span class="n">status</span><span class="p">()</span> <span class="o">!=</span> <span class="n">WL_CONNECTED</span><span class="p">)</span> <span class="p">{</span> <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span> <span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="s">"."</span><span class="p">);</span> <span class="p">}</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">[WiFi] Connected: %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">WiFi</span><span class="p">.</span><span class="n">localIP</span><span class="p">().</span><span class="n">toString</span><span class="p">().</span><span class="n">c_str</span><span class="p">());</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">connectBroker</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[Broker] Connecting as '%s'...</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">CLIENT_ID</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">()</span>
                   <span class="o">?</span> <span class="s">"[Broker] Connected OK"</span>
                   <span class="o">:</span> <span class="s">"[Broker] FAILED — check IP and firewall"</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="mi">115200</span><span class="p">);</span>
    <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">=== MetricMQ Demo 1: Basic Pub/Sub ==="</span><span class="p">);</span>
    <span class="n">connectWiFi</span><span class="p">();</span>
    <span class="n">connectBroker</span><span class="p">();</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">);</span>
        <span class="n">connectBroker</span><span class="p">();</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">client</span><span class="p">.</span><span class="n">loop</span><span class="p">();</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">millis</span><span class="p">()</span> <span class="o">-</span> <span class="n">lastPub</span> <span class="o">&gt;</span> <span class="mi">3000</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">lastPub</span> <span class="o">=</span> <span class="n">millis</span><span class="p">();</span>
        <span class="n">msgCount</span><span class="o">++</span><span class="p">;</span>

        <span class="kt">float</span> <span class="n">temp</span> <span class="o">=</span> <span class="mf">20.0f</span> <span class="o">+</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">random</span><span class="p">(</span><span class="o">-</span><span class="mi">30</span><span class="p">,</span> <span class="mi">80</span><span class="p">)</span> <span class="o">/</span> <span class="mf">10.0f</span><span class="p">;</span>
        <span class="kt">char</span> <span class="n">payload</span><span class="p">[</span><span class="mi">64</span><span class="p">];</span>
        <span class="n">snprintf</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">payload</span><span class="p">),</span>
                 <span class="s">"{</span><span class="se">\"</span><span class="s">msg</span><span class="se">\"</span><span class="s">:%u,</span><span class="se">\"</span><span class="s">temp</span><span class="se">\"</span><span class="s">:%.1f}"</span><span class="p">,</span> <span class="n">msgCount</span><span class="p">,</span> <span class="n">temp</span><span class="p">);</span>

        <span class="n">client</span><span class="p">.</span><span class="n">publish</span><span class="p">(</span><span class="n">String</span><span class="p">(</span><span class="s">"sensors/temperature"</span><span class="p">),</span> <span class="n">String</span><span class="p">(</span><span class="n">payload</span><span class="p">));</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[PUB #%u] sensors/temperature → %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">msgCount</span><span class="p">,</span> <span class="n">payload</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="how-to-run-it">How to Run It</h3>

<p>Flash the sketch. Open Serial Monitor. In a second PC terminal:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>redis-cli <span class="nt">-p</span> 6379 SUBSCRIBE <span class="s2">"sensors/#"</span>
</code></pre></div></div>

<h3 id="what-to-expect">What to Expect</h3>

<p>Serial Monitor:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>=== MetricMQ Demo 1: Basic Pub/Sub ===
[WiFi] Connected: 192.168.1.10
[MetricMQ] Connected to 192.168.1.100:6379
[MetricMQ] Client ID: esp32-demo1 | Signing: OFF
[Broker] Connected OK
[PUB #1] sensors/temperature → {"msg":1,"temp":23.4}
[PUB #2] sensors/temperature → {"msg":2,"temp":21.1}
</code></pre></div></div>

<p>PC terminal:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) "message"
2) "sensors/temperature"
3) "{\"msg\":1,\"temp\":23.4}"
</code></pre></div></div>

<p>The ESP32 spoke binary protocol. <code class="language-plaintext highlighter-rouge">redis-cli</code> spoke RESP. The broker translated automatically.</p>

<h3 id="what-the-broker-metrics-show">What the Broker Metrics Show</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">curl</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Select-String</span><span class="w"> </span><span class="s2">"metricmq_messages"</span><span class="w">
</span></code></pre></div></div>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>metricmq_messages_published_total   4
metricmq_messages_delivered_total   4
metricmq_active_connections         2
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">published == delivered</code> — zero routing loss. <code class="language-plaintext highlighter-rouge">active_connections = 2</code> — one binary publisher (ESP32), one RESP subscriber (redis-cli).</p>

<hr />

<h2 id="demo-2-exactly-once-delivery--surviving-a-network-drop">Demo 2: Exactly-Once Delivery — Surviving a Network Drop</h2>

<p><strong>What you learn:</strong> What happens to messages while the device is offline, and how MetricMQ replays them on reconnect with no gaps and no duplicates.</p>

<h3 id="how-it-works">How It Works</h3>

<p>Every message in binary protocol mode gets a sequence number assigned by the broker. The broker stores every message to LMDB and tracks each subscriber’s last acknowledged sequence. When you reconnect with the same <code class="language-plaintext highlighter-rouge">client_id</code>, the broker replays from <code class="language-plaintext highlighter-rouge">last_ack + 1</code>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Publishes:  seq=1  seq=2  seq=3  seq=4  seq=5  seq=6  seq=7
                              │
                        Device drops              │
                        (offline)         Device reconnects
                                          same client_id
                                          last ACK was seq=3

Broker: "replay seq=4, 5, 6, 7 for this client"

Device receives: seq=4 (REPLAY) seq=5 (REPLAY) seq=6 (LIVE) seq=7 (LIVE)
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">client_id</code> is how the broker identifies you on reconnect. Without a stable ID, replay doesn’t happen.</p>

<h3 id="srcmaincpp-1">src/main.cpp</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
 * MetricMQ Demo 2 — Exactly-Once Delivery
 *
 * Subscribes with a stable client_id. Tracks sequence numbers.
 * On reconnect, the broker replays any messages that arrived while offline.
 *
 * How to test:
 *   1. Flash and open Serial Monitor
 *   2. Start publishing from PC (command in comment below)
 *   3. While messages 5-10 are publishing, press RST or pull USB
 *   4. Reconnect after 5 seconds — watch the REPLAY labels
 *   5. Total == 20, Duplicates == 0 confirms exactly-once held
 *
 * PC publisher:
 *   Windows: 1..20 | ForEach-Object { redis-cli -p 6379 PUBLISH sensors/temperature "{`"reading`":$_}"; Start-Sleep 1 }
 *   Linux:   for i in $(seq 1 20); do redis-cli -p 6379 PUBLISH sensors/temperature "{\"reading\":$i}"; sleep 1; done
 */</span>

<span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;WiFi.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;MetricMQ.h&gt;</span><span class="cp">
</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_SSID</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_SSID"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_PASS</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_PASSWORD"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">BROKER_IP</span> <span class="o">=</span> <span class="s">"192.168.1.100"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">CLIENT_ID</span> <span class="o">=</span> <span class="s">"esp32-demo2-sub"</span><span class="p">;</span>  <span class="c1">// MUST be stable across reboots</span>

<span class="n">MetricMQClient</span> <span class="n">client</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">totalReceived</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">duplicates</span>    <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">uint64_t</span> <span class="n">lastSeq</span>       <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">bool</span>     <span class="n">wasOffline</span>    <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">onMessage</span><span class="p">(</span><span class="k">const</span> <span class="n">String</span><span class="o">&amp;</span> <span class="n">topic</span><span class="p">,</span>
               <span class="k">const</span> <span class="kt">uint8_t</span><span class="o">*</span> <span class="n">payload</span><span class="p">,</span> <span class="kt">size_t</span> <span class="n">length</span><span class="p">,</span>
               <span class="kt">uint64_t</span> <span class="n">sequence</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">totalReceived</span><span class="o">++</span><span class="p">;</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">sequence</span> <span class="o">&lt;=</span> <span class="n">lastSeq</span> <span class="o">&amp;&amp;</span> <span class="n">lastSeq</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">duplicates</span><span class="o">++</span><span class="p">;</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[!!DUPE] seq=%llu (expected &gt; %llu)</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">sequence</span><span class="p">,</span> <span class="n">lastSeq</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">label</span> <span class="o">=</span> <span class="n">wasOffline</span> <span class="o">?</span> <span class="s">"REPLAY"</span> <span class="o">:</span> <span class="s">"LIVE  "</span><span class="p">;</span>
        <span class="n">lastSeq</span> <span class="o">=</span> <span class="n">sequence</span><span class="p">;</span>
        <span class="n">String</span> <span class="n">msg</span><span class="p">((</span><span class="k">const</span> <span class="kt">char</span><span class="o">*</span><span class="p">)</span><span class="n">payload</span><span class="p">,</span> <span class="n">length</span><span class="p">);</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[%s] seq=%-4llu | %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">label</span><span class="p">,</span> <span class="n">sequence</span><span class="p">,</span> <span class="n">msg</span><span class="p">.</span><span class="n">c_str</span><span class="p">());</span>
    <span class="p">}</span>

    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"         Total: %u | Duplicates: %u</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
                  <span class="n">totalReceived</span><span class="p">,</span> <span class="n">duplicates</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">connectAndSubscribe</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[Broker] Connecting as '%s' (last seq=%llu)...</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
                  <span class="n">CLIENT_ID</span><span class="p">,</span> <span class="n">lastSeq</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"[Broker] Connected — broker will replay missed messages"</span><span class="p">);</span>
        <span class="n">client</span><span class="p">.</span><span class="n">subscribe</span><span class="p">(</span><span class="s">"sensors/temperature"</span><span class="p">,</span> <span class="n">onMessage</span><span class="p">);</span>
        <span class="n">wasOffline</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"[Broker] FAILED"</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="mi">115200</span><span class="p">);</span>
    <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">=== MetricMQ Demo 2: Exactly-Once Delivery ==="</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"Pull USB or press RST while messages are publishing.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">WiFi</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">WIFI_SSID</span><span class="p">,</span> <span class="n">WIFI_PASS</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">WiFi</span><span class="p">.</span><span class="n">status</span><span class="p">()</span> <span class="o">!=</span> <span class="n">WL_CONNECTED</span><span class="p">)</span> <span class="p">{</span> <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span> <span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="s">"."</span><span class="p">);</span> <span class="p">}</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">[WiFi] %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">WiFi</span><span class="p">.</span><span class="n">localIP</span><span class="p">().</span><span class="n">toString</span><span class="p">().</span><span class="n">c_str</span><span class="p">());</span>

    <span class="n">connectAndSubscribe</span><span class="p">();</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">wasOffline</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">[Broker] Disconnected at seq=%llu — reconnecting...</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
                      <span class="n">lastSeq</span><span class="p">);</span>
        <span class="n">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">);</span>
        <span class="n">connectAndSubscribe</span><span class="p">();</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">client</span><span class="p">.</span><span class="n">loop</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="what-to-expect-1">What to Expect</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[LIVE  ] seq=1    | {"reading":1}
         Total: 1 | Duplicates: 0
[LIVE  ] seq=4    | {"reading":4}
         Total: 4 | Duplicates: 0

--- RST pressed here ---

[Broker] Disconnected at seq=4 — reconnecting...
[Broker] Connected — broker will replay missed messages
[REPLAY] seq=5    | {"reading":5}
[REPLAY] seq=6    | {"reading":6}
[REPLAY] seq=7    | {"reading":7}
[LIVE  ] seq=8    | {"reading":8}
...
         Total: 20 | Duplicates: 0
</code></pre></div></div>

<p>The ScreenShot, of the Platformio Serial Terminal :</p>

<p><img src="/cover_images/posts/Demo2-platformio-serialport.webp" alt="" /></p>
<h3 id="what-the-broker-metrics-show-1">What the Broker Metrics Show</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># While device is offline:
metricmq_ack_tracking_efficiency    62   ← drops during outage — expected
metricmq_lmdb_stored_messages       8    ← broker kept storing

# After reconnect and replay:
metricmq_ack_tracking_efficiency    100  ← restored after replay ACKs
metricmq_replay_messages_total      4    ← exactly how many were recovered
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">ack_tracking_efficiency</code> returning to 100 after reconnect is the broker confirming the replay worked and everything was acknowledged.</p>

<blockquote>
  <p><strong>Replay window:</strong> The broker retains the last 100,000 messages in LMDB. At 1 message/second that’s 27 hours of replay coverage — more than enough for typical WiFi outages.</p>
</blockquote>

<hr />

<h2 id="demo-3-fan-out-with-wildcards--one-publish-many-subscribers">Demo 3: Fan-Out with Wildcards — One Publish, Many Subscribers</h2>

<p><strong>What you learn:</strong> How one ESP32 publishing to a topic can be received by multiple subscribers simultaneously using wildcard patterns, with no firmware changes.</p>

<h3 id="srcmaincpp-2">src/main.cpp</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
 * MetricMQ Demo 3 — Fan-Out with Wildcards
 *
 * Publishes temperature, humidity, and CO2 to three subtopics every 5 seconds.
 *
 * Open three PC terminals and run:
 *   Terminal A: redis-cli -p 6379 SUBSCRIBE "sensors/env/temperature"
 *   Terminal B: redis-cli -p 6379 SUBSCRIBE "sensors/env/+"
 *   Terminal C: redis-cli -p 6379 SUBSCRIBE "sensors/#"
 *
 * A gets 1 message/cycle. B and C each get 3.
 * The publisher never changes — just add more subscribers.
 */</span>

<span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;WiFi.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;MetricMQ.h&gt;</span><span class="cp">
</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_SSID</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_SSID"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_PASS</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_PASSWORD"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">BROKER_IP</span> <span class="o">=</span> <span class="s">"192.168.1.100"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">CLIENT_ID</span> <span class="o">=</span> <span class="s">"esp32-demo3"</span><span class="p">;</span>

<span class="n">MetricMQClient</span> <span class="n">client</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">cycle</span>  <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">lastPub</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="mi">115200</span><span class="p">);</span>
    <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">=== MetricMQ Demo 3: Fan-Out with Wildcards ==="</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"Open 3 PC terminals (commands in sketch comment).</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">WiFi</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">WIFI_SSID</span><span class="p">,</span> <span class="n">WIFI_PASS</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">WiFi</span><span class="p">.</span><span class="n">status</span><span class="p">()</span> <span class="o">!=</span> <span class="n">WL_CONNECTED</span><span class="p">)</span> <span class="p">{</span> <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span> <span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="s">"."</span><span class="p">);</span> <span class="p">}</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[WiFi] %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">WiFi</span><span class="p">.</span><span class="n">localIP</span><span class="p">().</span><span class="n">toString</span><span class="p">().</span><span class="n">c_str</span><span class="p">());</span>

    <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">()</span> <span class="o">?</span> <span class="s">"[Broker] Connected"</span> <span class="o">:</span> <span class="s">"[Broker] FAILED"</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">);</span>
        <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>
        <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">client</span><span class="p">.</span><span class="n">loop</span><span class="p">();</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">millis</span><span class="p">()</span> <span class="o">-</span> <span class="n">lastPub</span> <span class="o">&gt;</span> <span class="mi">5000</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">lastPub</span> <span class="o">=</span> <span class="n">millis</span><span class="p">();</span>
        <span class="n">cycle</span><span class="o">++</span><span class="p">;</span>

        <span class="kt">float</span> <span class="n">temp</span> <span class="o">=</span> <span class="mf">21.0f</span> <span class="o">+</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">random</span><span class="p">(</span><span class="o">-</span><span class="mi">20</span><span class="p">,</span> <span class="mi">50</span><span class="p">)</span> <span class="o">/</span> <span class="mf">10.0f</span><span class="p">;</span>
        <span class="kt">float</span> <span class="n">hum</span>  <span class="o">=</span> <span class="mf">45.0f</span> <span class="o">+</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">random</span><span class="p">(</span><span class="o">-</span><span class="mi">100</span><span class="p">,</span> <span class="mi">200</span><span class="p">)</span> <span class="o">/</span> <span class="mf">10.0f</span><span class="p">;</span>
        <span class="kt">int</span>   <span class="n">co2</span>  <span class="o">=</span> <span class="mi">400</span> <span class="o">+</span> <span class="n">random</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">600</span><span class="p">);</span>

        <span class="n">client</span><span class="p">.</span><span class="n">publish</span><span class="p">(</span><span class="s">"sensors/env/temperature"</span><span class="p">,</span> <span class="n">String</span><span class="p">(</span><span class="n">temp</span><span class="p">,</span> <span class="mi">1</span><span class="p">));</span>
        <span class="n">client</span><span class="p">.</span><span class="n">publish</span><span class="p">(</span><span class="s">"sensors/env/humidity"</span><span class="p">,</span>    <span class="n">String</span><span class="p">(</span><span class="n">hum</span><span class="p">,</span> <span class="mi">1</span><span class="p">));</span>
        <span class="n">client</span><span class="p">.</span><span class="n">publish</span><span class="p">(</span><span class="s">"sensors/env/co2"</span><span class="p">,</span>         <span class="n">String</span><span class="p">(</span><span class="n">co2</span><span class="p">));</span>

        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[Cycle %u] temp=%.1f°C  hum=%.1f%%  co2=%d ppm</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span>
                      <span class="n">cycle</span><span class="p">,</span> <span class="n">temp</span><span class="p">,</span> <span class="n">hum</span><span class="p">,</span> <span class="n">co2</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="what-to-expect-2">What to Expect</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Terminal A (exact):   1 message/cycle  (temperature only)
Terminal B (+ wild):  3 messages/cycle (temp + hum + co2)
Terminal C (# wild):  3 messages/cycle (everything under sensors/)
</code></pre></div></div>

<p>Check the fan-out multiplier live:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="n">Invoke-WebRequest</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="p">)</span><span class="o">.</span><span class="nf">Content</span><span class="w"> </span><span class="o">|</span><span class="w">
    </span><span class="n">Select-String</span><span class="w"> </span><span class="s2">"delivered|published"</span><span class="w">
</span></code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>metricmq_messages_published_total   9    ← 3 topics × 3 cycles
metricmq_messages_delivered_total   21   ← A(3) + B(9) + C(9)
</code></pre></div></div>

<p>Fan-out ratio = <code class="language-plaintext highlighter-rouge">21 / 9 = 2.33</code>. Add a fourth subscriber and watch it climb to ~3.0 without touching the sketch.</p>

<hr />

<h2 id="demo-4-signed-messages--proving-your-data-is-real">Demo 4: Signed Messages — Proving Your Data Is Real</h2>

<p><strong>What you learn:</strong> Ed25519 message signing on the device. The broker verifies the signature before routing. Unsigned messages to <code class="language-plaintext highlighter-rouge">secure/</code> topics are rejected at the wire level.</p>

<h3 id="why-this-matters">Why This Matters</h3>

<p>Your ESP32 publishes <code class="language-plaintext highlighter-rouge">{"temp":23.4}</code>. Someone on the same WiFi publishes <code class="language-plaintext highlighter-rouge">{"temp":-99}</code> to the same topic. Both look identical to your backend. Ed25519 signing closes this gap: your device signs every message with a private key (64 bytes, never transmitted). The broker has the corresponding public key and verifies before routing. A message without a valid signature never reaches any subscriber.</p>

<p>This is <strong>authentication, not encryption.</strong> The payload is still visible on the wire. Signing proves <em>who</em> sent it and that it <em>wasn’t modified</em>.</p>

<h3 id="step-1-generate-a-key-pair">Step 1: Generate a Key Pair</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">.</span><span class="n">\build\Release\metricmq-keygen.exe</span><span class="w"> </span><span class="s2">"esp32-demo4"</span><span class="w"> </span><span class="s2">"secure/sensors/*"</span><span class="w">
</span></code></pre></div></div>

<p>Output:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Secret key (64 bytes) — device only, never share:
  { 0x4a, 0x3f, ... }

Public key (32 bytes) — register on broker:
  { 0x8c, 0x1d, ... }
</code></pre></div></div>

<p><strong>The screenshot on your terminal:</strong></p>

<p><img src="/cover_images/posts/GetImage.webp" alt="" /></p>

<p>Create <code class="language-plaintext highlighter-rouge">include/device_secrets.h</code> (add to <code class="language-plaintext highlighter-rouge">.gitignore</code>):</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// include/device_secrets.h — NEVER commit this file</span>
<span class="cp">#pragma once
</span><span class="k">static</span> <span class="k">const</span> <span class="kt">uint8_t</span> <span class="n">SECRET_KEY</span><span class="p">[</span><span class="mi">64</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span> <span class="mh">0x4a</span><span class="p">,</span> <span class="mh">0x3f</span><span class="p">,</span> <span class="cm">/* ... 64 bytes */</span> <span class="p">};</span>
<span class="k">static</span> <span class="k">const</span> <span class="kt">uint32_t</span> <span class="n">KEY_ID</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="step-2-register-the-public-key-on-the-broker"><strong>Step 2:</strong> Register the Public Key on the Broker</h3>

<p>In <code class="language-plaintext highlighter-rouge">MetricMQ/src/main.cpp</code>, add before <code class="language-plaintext highlighter-rouge">broker.run()</code>:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">uint8_t</span> <span class="n">pk</span><span class="p">[</span><span class="mi">32</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span> <span class="mh">0x8c</span><span class="p">,</span> <span class="mh">0x1d</span><span class="p">,</span> <span class="cm">/* ... 32 bytes */</span> <span class="p">};</span>
<span class="n">broker</span><span class="p">.</span><span class="n">get_keystore</span><span class="p">().</span><span class="n">register_key</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">pk</span><span class="p">,</span> <span class="p">{</span><span class="s">"secure/sensors/*"</span><span class="p">});</span>
</code></pre></div></div>

<p>Rebuild and restart:</p>
<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cmake</span><span class="w"> </span><span class="nt">--build</span><span class="w"> </span><span class="nx">build</span><span class="w"> </span><span class="nt">--config</span><span class="w"> </span><span class="nx">Release</span><span class="w">
</span><span class="o">.</span><span class="n">\build\Release\metricmq-broker.exe</span><span class="w">
</span></code></pre></div></div>

<h3 id="srcmaincpp-3">src/main.cpp</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/*
 * MetricMQ Demo 4 — Ed25519 Signed Messages
 *
 * Prerequisites (complete before flashing):
 *   1. Run metricmq-keygen "esp32-demo4" "secure/sensors/*"
 *   2. Paste secret key into include/device_secrets.h (in .gitignore)
 *   3. Register public key in broker src/main.cpp, rebuild, restart broker
 *
 * To test:
 *   Subscriber: redis-cli -p 6379 SUBSCRIBE "secure/sensors/#"
 *   Spoof:      redis-cli -p 6379 PUBLISH secure/sensors/temperature '{"fake":true}'
 *               → broker prints SIGNATURE_REQUIRED, subscriber gets NOTHING
 */</span>

<span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;WiFi.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;MetricMQ.h&gt;</span><span class="cp">
#include</span> <span class="cpf">"device_secrets.h"</span><span class="cp">
</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_SSID</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_SSID"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">WIFI_PASS</span> <span class="o">=</span> <span class="s">"YOUR_WIFI_PASSWORD"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">BROKER_IP</span> <span class="o">=</span> <span class="s">"192.168.1.100"</span><span class="p">;</span>
<span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">CLIENT_ID</span> <span class="o">=</span> <span class="s">"esp32-demo4"</span><span class="p">;</span>

<span class="n">MetricMQClient</span> <span class="n">client</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">msgCount</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="kt">uint32_t</span> <span class="n">lastPub</span>  <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">connectBroker</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// setSigningKey MUST come before begin()/connect()</span>
    <span class="n">client</span><span class="p">.</span><span class="n">setSigningKey</span><span class="p">(</span><span class="n">SECRET_KEY</span><span class="p">,</span> <span class="n">KEY_ID</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[Broker] Connecting in signed mode (key_id=%u)...</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">KEY_ID</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">BROKER_IP</span><span class="p">,</span> <span class="mi">6379</span><span class="p">);</span>
    <span class="n">client</span><span class="p">.</span><span class="n">connect</span><span class="p">(</span><span class="n">CLIENT_ID</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">()</span>
                   <span class="o">?</span> <span class="s">"[Broker] Connected — signed mode active"</span>
                   <span class="o">:</span> <span class="s">"[Broker] FAILED"</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="mi">115200</span><span class="p">);</span>
    <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">=== MetricMQ Demo 4: Signed Messages ==="</span><span class="p">);</span>

    <span class="n">WiFi</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">WIFI_SSID</span><span class="p">,</span> <span class="n">WIFI_PASS</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">WiFi</span><span class="p">.</span><span class="n">status</span><span class="p">()</span> <span class="o">!=</span> <span class="n">WL_CONNECTED</span><span class="p">)</span> <span class="p">{</span> <span class="n">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">);</span> <span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="s">"."</span><span class="p">);</span> <span class="p">}</span>
    <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[WiFi] %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">WiFi</span><span class="p">.</span><span class="n">localIP</span><span class="p">().</span><span class="n">toString</span><span class="p">().</span><span class="n">c_str</span><span class="p">());</span>

    <span class="n">connectBroker</span><span class="p">();</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">client</span><span class="p">.</span><span class="n">isConnected</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">);</span>
        <span class="n">connectBroker</span><span class="p">();</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">client</span><span class="p">.</span><span class="n">loop</span><span class="p">();</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">millis</span><span class="p">()</span> <span class="o">-</span> <span class="n">lastPub</span> <span class="o">&gt;</span> <span class="mi">5000</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">lastPub</span> <span class="o">=</span> <span class="n">millis</span><span class="p">();</span>
        <span class="n">msgCount</span><span class="o">++</span><span class="p">;</span>

        <span class="kt">float</span> <span class="n">temp</span> <span class="o">=</span> <span class="mf">22.5f</span> <span class="o">+</span> <span class="p">(</span><span class="kt">float</span><span class="p">)</span><span class="n">random</span><span class="p">(</span><span class="o">-</span><span class="mi">30</span><span class="p">,</span> <span class="mi">30</span><span class="p">)</span> <span class="o">/</span> <span class="mf">10.0f</span><span class="p">;</span>
        <span class="kt">char</span> <span class="n">payload</span><span class="p">[</span><span class="mi">96</span><span class="p">];</span>
        <span class="n">snprintf</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">payload</span><span class="p">),</span>
                 <span class="s">"{</span><span class="se">\"</span><span class="s">msg</span><span class="se">\"</span><span class="s">:%u,</span><span class="se">\"</span><span class="s">temp</span><span class="se">\"</span><span class="s">:%.1f,</span><span class="se">\"</span><span class="s">device</span><span class="se">\"</span><span class="s">:</span><span class="se">\"</span><span class="s">%s</span><span class="se">\"</span><span class="s">}"</span><span class="p">,</span>
                 <span class="n">msgCount</span><span class="p">,</span> <span class="n">temp</span><span class="p">,</span> <span class="n">CLIENT_ID</span><span class="p">);</span>

        <span class="n">client</span><span class="p">.</span><span class="n">publishSigned</span><span class="p">(</span><span class="n">String</span><span class="p">(</span><span class="s">"secure/sensors/temperature"</span><span class="p">),</span>
                             <span class="n">String</span><span class="p">(</span><span class="n">payload</span><span class="p">));</span>
        <span class="n">Serial</span><span class="p">.</span><span class="n">printf</span><span class="p">(</span><span class="s">"[SIGNED #%u] %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">msgCount</span><span class="p">,</span> <span class="n">payload</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="what-to-expect-3">What to Expect</h3>

<p>Broker console after a legitimate signed message:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[info] Key 1 verified OK — routing secure/sensors/temperature
</code></pre></div></div>

<p>After the spoof attempt:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[warn] SIGNATURE_REQUIRED — rejecting unsigned publish on secure/sensors/temperature
</code></pre></div></div>

<p>The subscriber receives nothing from the spoof.</p>

<h3 id="what-the-broker-metrics-show-2">What the Broker Metrics Show</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>metricmq_signature_verifications_total   8    ← legitimate signed messages
metricmq_signature_failures_total        1    ← the rejected spoof
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">signature_failures_total</code> should be 0 in a healthy deployment. Any non-zero value means either a device has a corrupted key or someone is probing your broker with unsigned frames.</p>

<hr />

<h2 id="prometheus--grafana-seeing-your-system-live">Prometheus + Grafana: Seeing Your System Live</h2>

<p>This is the part most tutorials skip. The broker exposes live metrics on port 9091 from the moment it starts — you don’t enable anything extra. Here’s why it matters and how to use it.</p>

<h3 id="what-value-does-this-actually-serve">What Value Does This Actually Serve?</h3>

<p>Without observability, your IoT system is a black box. You know the broker process is running, but you don’t know:</p>

<ul>
  <li>How many devices are currently connected?</li>
  <li>Are messages actually being delivered, or silently dropped?</li>
  <li>Has any device been offline long enough to risk losing data?</li>
  <li>Did someone just inject fake sensor data?</li>
  <li>Is the broker getting slower under load?</li>
</ul>

<p>Prometheus collects these numbers continuously. Grafana makes them visible. Together they answer all five questions in real time without SSH’ing into anything.</p>

<div class="note">

  <p>The Prometheus data (raw format) in the browser, Open up http://localhost:9091/metrics in your broswer when the broker is running: (ScreenShot Below)</p>

</div>

<p><img src="/cover_images/posts/prometheus-browser.webp" alt="" /></p>
<h3 id="the-metrics-that-matter">The Metrics That Matter</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># One-shot snapshot</span><span class="w">
</span><span class="p">(</span><span class="n">Invoke-WebRequest</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="p">)</span><span class="o">.</span><span class="nf">Content</span><span class="w"> </span><span class="o">|</span><span class="w">
    </span><span class="n">Select-String</span><span class="w"> </span><span class="s2">"^metricmq_"</span><span class="w">

</span><span class="c"># Live view, refreshes every 2 seconds</span><span class="w">
</span><span class="kr">while</span><span class="w"> </span><span class="p">(</span><span class="bp">$true</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="n">Clear-Host</span><span class="w">
    </span><span class="p">(</span><span class="n">Invoke-WebRequest</span><span class="w"> </span><span class="nx">http://localhost:9091/metrics</span><span class="w"> </span><span class="nt">-UseBasicParsing</span><span class="p">)</span><span class="o">.</span><span class="nf">Content</span><span class="w"> </span><span class="se">`
</span><span class="w">        </span><span class="o">-split</span><span class="w"> </span><span class="s2">"</span><span class="se">`n</span><span class="s2">"</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Where-Object</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="bp">$_</span><span class="w"> </span><span class="o">-match</span><span class="w"> </span><span class="s2">"^metricmq_"</span><span class="w"> </span><span class="o">-and</span><span class="w"> </span><span class="bp">$_</span><span class="w"> </span><span class="o">-notmatch</span><span class="w"> </span><span class="s2">"^#"</span><span class="w"> </span><span class="p">}</span><span class="w">
    </span><span class="n">Start-Sleep</span><span class="w"> </span><span class="nx">2</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>What it tells you</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_active_connections</code></td>
      <td>How many clients are connected right now</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_messages_published_total</code></td>
      <td>Total publishes the broker accepted</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_messages_delivered_total</code></td>
      <td>Total deliveries that reached a subscriber</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_ack_tracking_efficiency</code></td>
      <td>% of binary messages ACKed — target: ~100</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_replay_messages_total</code></td>
      <td>Messages replayed from LMDB after reconnects</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_publish_latency_microseconds</code></td>
      <td>p50/p99/p99.9 routing latency</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_signature_verifications_total</code></td>
      <td>Successful Ed25519 verifications</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_signature_failures_total</code></td>
      <td>Rejected/unsigned frames — target: 0</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">metricmq_lmdb_stored_messages</code></td>
      <td>Messages currently persisted in LMDB</td>
    </tr>
  </tbody>
</table>

<h3 id="setting-up-prometheus-10-minutes">Setting Up Prometheus (10 Minutes)</h3>

<blockquote>
  <p><strong>Note:</strong> Prometheus is not available via <code class="language-plaintext highlighter-rouge">winget</code> — it is distributed as a
zip archive from the official GitHub releases page. The commands below
download, extract, and configure it in one go.</p>
</blockquote>

<p><strong>Step 1:</strong> Download and extract Prometheus:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run this in PowerShell — downloads Prometheus and extracts to C:\prometheus</span><span class="w">
</span><span class="nv">$version</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"3.4.0"</span><span class="w">
</span><span class="nv">$url</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="s2">"https://github.com/prometheus/prometheus/releases/download/"</span><span class="w"> </span><span class="o">+</span><span class="w">
           </span><span class="s2">"v</span><span class="nv">$version</span><span class="s2">/prometheus-</span><span class="nv">$version</span><span class="s2">.windows-amd64.zip"</span><span class="w">

</span><span class="n">Invoke-WebRequest</span><span class="w"> </span><span class="nt">-Uri</span><span class="w"> </span><span class="nv">$url</span><span class="w"> </span><span class="nt">-OutFile</span><span class="w"> </span><span class="s2">"</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">TEMP</span><span class="s2">\prometheus.zip"</span><span class="w">
</span><span class="n">Expand-Archive</span><span class="w"> </span><span class="nt">-Path</span><span class="w"> </span><span class="s2">"</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">TEMP</span><span class="s2">\prometheus.zip"</span><span class="w"> </span><span class="se">`
</span><span class="w">               </span><span class="nt">-DestinationPath</span><span class="w"> </span><span class="s2">"C:\prometheus"</span><span class="w"> </span><span class="nt">-Force</span><span class="w">

</span><span class="n">Write-Host</span><span class="w"> </span><span class="s2">"Prometheus extracted to C:\prometheus"</span><span class="w">
</span></code></pre></div></div>

<p><strong>Step 2:</strong> Create <code class="language-plaintext highlighter-rouge">C:\prometheus\prometheus.yml</code> — open Notepad and save
this content to that path:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">global</span><span class="pi">:</span>
  <span class="na">scrape_interval</span><span class="pi">:</span> <span class="s">5s</span>

<span class="na">scrape_configs</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">job_name</span><span class="pi">:</span> <span class="s">metricmq</span>
    <span class="na">static_configs</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">targets</span><span class="pi">:</span> <span class="pi">[</span><span class="s1">'</span><span class="s">localhost:9091'</span><span class="pi">]</span>
</code></pre></div></div>

<p><strong>Step 3:</strong> Start Prometheus (keep this terminal open):</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Navigate into the extracted folder and start</span><span class="w">
</span><span class="nv">$version</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"3.4.0"</span><span class="w">
</span><span class="n">cd</span><span class="w"> </span><span class="s2">"C:\prometheus\prometheus-</span><span class="nv">$version</span><span class="s2">.windows-amd64"</span><span class="w">
</span><span class="o">.</span><span class="n">\prometheus.exe</span><span class="w"> </span><span class="nt">--config</span><span class="o">.</span><span class="nf">file</span><span class="o">=</span><span class="s2">"..\prometheus.yml"</span><span class="w">
</span></code></pre></div></div>

<p>You should see:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ts=... level=info msg="Server is ready to receive web requests."
</code></pre></div></div>

<p><strong>Step 4:</strong> Open <code class="language-plaintext highlighter-rouge">http://localhost:9090</code> in your browser. Type
<code class="language-plaintext highlighter-rouge">metricmq_messages_published_total</code> in the query box and press Enter.
You should see data points immediately if the broker is running.</p>

<p>If you see “No data”, check that the broker is running and that port 9091
is reachable: <code class="language-plaintext highlighter-rouge">curl http://localhost:9091/metrics</code> should return metric lines.</p>

<h3 id="setting-up-grafana-10-more-minutes">Setting Up Grafana (10 More Minutes)</h3>

<blockquote>
  <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">winget install GrafanaLabs.Grafana</code> does not exist. Use the
official MSI installer below.</p>
</blockquote>

<p><strong>Step 1:</strong> Download and install Grafana:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Downloads the Grafana MSI and installs it as a Windows service</span><span class="w">
</span><span class="nv">$grafanaVersion</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"12.0.1"</span><span class="w">
</span><span class="nv">$url</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"https://dl.grafana.com/oss/release/grafana-</span><span class="nv">$grafanaVersion</span><span class="s2">.windows-amd64.msi"</span><span class="w">

</span><span class="n">Invoke-WebRequest</span><span class="w"> </span><span class="nt">-Uri</span><span class="w"> </span><span class="nv">$url</span><span class="w"> </span><span class="nt">-OutFile</span><span class="w"> </span><span class="s2">"</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">TEMP</span><span class="s2">\grafana.msi"</span><span class="w">
</span><span class="n">Start-Process</span><span class="w"> </span><span class="nx">msiexec</span><span class="w"> </span><span class="nt">-ArgumentList</span><span class="w"> </span><span class="s2">"/i </span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">TEMP</span><span class="s2">\grafana.msi /quiet"</span><span class="w"> </span><span class="nt">-Wait</span><span class="w">

</span><span class="n">Write-Host</span><span class="w"> </span><span class="s2">"Grafana installed. Starting service..."</span><span class="w">
</span><span class="n">Start-Service</span><span class="w"> </span><span class="nt">-Name</span><span class="w"> </span><span class="s2">"Grafana"</span><span class="w">
</span></code></pre></div></div>

<p>Grafana installs as a Windows service and starts automatically. If the
service doesn’t start automatically, run <code class="language-plaintext highlighter-rouge">Start-Service -Name "Grafana"</code>
from an elevated PowerShell window.</p>

<p><strong>Step 2:</strong> Open <code class="language-plaintext highlighter-rouge">http://localhost:3000</code> in your browser.
Default login: <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code>. Grafana will ask you to set a new password
on first login — do that and continue.</p>

<p><strong>Step 3:</strong> Add Prometheus as a data source:</p>
<ul>
  <li>Left sidebar → Connections → Data Sources → Add data source</li>
  <li>Select <strong>Prometheus</strong></li>
  <li>URL: <code class="language-plaintext highlighter-rouge">http://localhost:9090</code></li>
  <li>Click <strong>Save &amp; Test</strong> — you should see “Data source is working”</li>
</ul>

<p><strong>Step 4:</strong> Create a new dashboard with four panels:</p>
<ul>
  <li>Top right → <strong>New Dashboard</strong> → <strong>Add visualization</strong></li>
  <li>For each panel: paste the PromQL query, set the panel type, set the title</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Panel 1 — Message Rate
PromQL: rate(metricmq_messages_published_total[1m])
Type: Time series | Shows: messages per second live

Panel 2 — Delivery Efficiency
PromQL: metricmq_messages_delivered_total / metricmq_messages_published_total
Type: Stat | Alert if: &lt; 0.95

Panel 3 — ACK Tracking
PromQL: metricmq_ack_tracking_efficiency
Type: Gauge | Alert if: &lt; 95 for &gt; 60s

Panel 4 — Security Events
PromQL: increase(metricmq_signature_failures_total[5m])
Type: Stat | Alert if: &gt; 0
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Panel 1 — Message Rate
PromQL: rate(metricmq_messages_published_total[1m])
Type: Time series | Shows: messages per second live

Panel 2 — Delivery Efficiency
PromQL: metricmq_messages_delivered_total / metricmq_messages_published_total
Type: Stat | Alert if: &lt; 0.95

Panel 3 — ACK Tracking
PromQL: metricmq_ack_tracking_efficiency
Type: Gauge | Alert if: &lt; 95 for &gt; 60s

Panel 4 — Security Events
PromQL: increase(metricmq_signature_failures_total[5m])
Type: Stat | Alert if: &gt; 0
</code></pre></div></div>

<h3 id="what-each-alert-catches-in-a-real-deployment">What Each Alert Catches in a Real Deployment</h3>

<p><strong><code class="language-plaintext highlighter-rouge">delivery efficiency</code> drops below 0.95:</strong> Some messages are not reaching subscribers. Could be a crashed consumer, a network partition, or a topic mismatch. In a building automation system with five consumers on a temperature stream, this dropping from 1.0 to 0.6 means three consumers silently went offline.</p>

<p><strong><code class="language-plaintext highlighter-rouge">ack_tracking_efficiency</code> drops below 95 for more than 60 seconds:</strong> A subscriber is falling behind - WiFi instability, device overload, or a bug in the application layer consuming messages too slowly. This fires before any data is actually lost.</p>

<p><strong><code class="language-plaintext highlighter-rouge">signature_failures_total</code> increments:</strong> Either a device has a corrupted key after a failed OTA update, or someone on the network is probing the broker with unsigned frames. This is your intrusion detection in a single counter — one alert rule covers the entire fleet.</p>

<p><strong><code class="language-plaintext highlighter-rouge">lmdb_stored_messages</code> grows without <code class="language-plaintext highlighter-rouge">replay_messages_total</code> growing:</strong> Messages are accumulating in LMDB but no subscriber is claiming them. A device has been permanently offline. At 100,000 stored messages the broker starts compacting and the oldest readings are lost. This gives you advance warning.</p>

<hr />

<h2 id="how-to-navigate-the-metricmq-docs">How to Navigate the MetricMQ Docs</h2>

<p>The official docs live at <a href="https://metricmq-docs.netlify.app/">metricmq-docs.netlify.app</a>. Here’s how to use them efficiently depending on what you’re trying to do.</p>

<h3 id="if-youre-just-getting-started">If You’re Just Getting Started</h3>

<p>Start with the <strong>Quick Start — ESP32</strong> section on the main page. It shows the minimal <code class="language-plaintext highlighter-rouge">setup()</code> and <code class="language-plaintext highlighter-rouge">loop()</code> pattern. Then come back to this blog post for the full working sketches — the docs show the pattern, the demos here show the complete code.</p>

<h3 id="if-you-want-to-understand-the-wire-protocol">If You Want to Understand the Wire Protocol</h3>

<p>The <strong>Wire Protocol</strong> table on the main page defines the exact 16-byte header layout:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Offset 0    : version (0x01)
Offset 1    : command byte
Offset 2–3  : topic length  (uint16 big-endian)
Offset 4–7  : payload length (uint32 big-endian)
Offset 8–15 : sequence number (uint64 big-endian)
Offset 16+  : topic bytes, then payload bytes
</code></pre></div></div>

<p>This is the ground truth for anyone implementing a client on a new platform. If you’re writing a client for STM32, Raspberry Pi, or any other platform, this table plus the <code class="language-plaintext highlighter-rouge">BinaryCommand</code> enum values (SUBSCRIBE=0x01, PUBLISH=0x03, MESSAGE=0x04, etc.) is everything you need.</p>

<h3 id="if-youre-integrating-metricmq-into-an-existing-project">If You’re Integrating MetricMQ Into an Existing Project</h3>

<p><strong>For ESP32/Arduino:</strong> Copy the three files from <code class="language-plaintext highlighter-rouge">esp32-metricmq/src/</code> into your project’s <code class="language-plaintext highlighter-rouge">lib/MetricMQ/</code> folder. That’s the complete integration — no package manager, no CMake, no external dependencies beyond arduino-esp32 v2.0.0+ which ships libsodium. Your existing WiFi code, sensor reads, and display code stay exactly as they are. MetricMQ adds three things: <code class="language-plaintext highlighter-rouge">begin()</code> + <code class="language-plaintext highlighter-rouge">connect()</code> in <code class="language-plaintext highlighter-rouge">setup()</code>, <code class="language-plaintext highlighter-rouge">client.loop()</code> in <code class="language-plaintext highlighter-rouge">loop()</code>, and <code class="language-plaintext highlighter-rouge">client.publish()</code> wherever you currently <code class="language-plaintext highlighter-rouge">Serial.printf()</code> your data.</p>

<p><strong>For native C++:</strong> Include <code class="language-plaintext highlighter-rouge">metricmq/pubsub.hpp</code> for RESP or <code class="language-plaintext highlighter-rouge">metricmq/binary_pubsub.hpp</code> for the binary protocol. The <code class="language-plaintext highlighter-rouge">BinaryPublisher</code> class handles frame construction and Ed25519 signing. The <code class="language-plaintext highlighter-rouge">BinarySubscriber</code> handles ACK tracking and replay. Add <code class="language-plaintext highlighter-rouge">metricmq</code> as a Conan dependency to your existing <code class="language-plaintext highlighter-rouge">conanfile.txt</code>.</p>

<h3 id="if-youre-evaluating-for-a-production-deployment">If You’re Evaluating for a Production Deployment</h3>

<p>Read the <strong>Production Risks</strong> table on the main page before anything else. The most important entries are: the single global mutex (limits concurrency above ~500 clients), no RESP authentication (any TCP client on port 6379 has full access), no session idle timeout (crashed clients leave zombie threads), and LMDB filling to 1 GB with no compaction or TTL. These are real constraints — understand them before deciding if MetricMQ is the right tool for your scale.</p>

<h3 id="if-youre-running-tests">If You’re Running Tests</h3>

<p>The <strong>Running Tests</strong> section on the main page lists every test binary and whether it needs a live broker. Start with <code class="language-plaintext highlighter-rouge">signed_publish_test</code> — it needs no broker and confirms your Ed25519 setup is correct. Then <code class="language-plaintext highlighter-rouge">exactly_once_test</code> with the broker running to confirm ACK tracking and replay. The <code class="language-plaintext highlighter-rouge">persistence_test</code> is the most useful for Demo 2 validation.</p>

<hr />]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="es" /><category term="c++" /><summary type="html"><![CDATA[MetricMQ is a 328 KB C++20 message broker built from scratch and still under development. In this blog post the metricmq is demostrated on the Esp32-s3 N16R8 Module]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/IMG_0313.webp" /><media:content medium="image" url="/cover_images/posts/IMG_0313.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Why Most Embedded Code Is Tightly Coupled — And How Dependency Injection Fixes It</title><link href="/blog/es/2026-04-17-di-embedded/" rel="alternate" type="text/html" title="Why Most Embedded Code Is Tightly Coupled — And How Dependency Injection Fixes It" /><published>2026-04-17T00:00:00+00:00</published><updated>2026-07-22T18:50:50+00:00</updated><id>/blog/es/di-embedded</id><content type="html" xml:base="/blog/es/2026-04-17-di-embedded/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#the-board-revision-nightmare" id="markdown-toc-the-board-revision-nightmare">The Board Revision Nightmare</a></li>
  <li><a href="#the-anti-pattern-hardware-leaking-into-logic" id="markdown-toc-the-anti-pattern-hardware-leaking-into-logic">The Anti-Pattern: Hardware Leaking into Logic</a>    <ul>
      <li><a href="#why-this-breaks-down" id="markdown-toc-why-this-breaks-down">Why this breaks down:</a></li>
    </ul>
  </li>
  <li><a href="#the-fix-part-1-dependency-inversion-dip" id="markdown-toc-the-fix-part-1-dependency-inversion-dip">The Fix Part 1: Dependency Inversion (DIP)</a>    <ul>
      <li><a href="#in-c-interface-via-struct-of-function-pointers" id="markdown-toc-in-c-interface-via-struct-of-function-pointers">In C: Interface via <code class="language-plaintext highlighter-rouge">struct</code> of function pointers</a></li>
      <li><a href="#in-c-abstract-interface-class" id="markdown-toc-in-c-abstract-interface-class">In C++: Abstract Interface Class</a></li>
    </ul>
  </li>
  <li><a href="#the-fix-part-2-dependency-injection-di" id="markdown-toc-the-fix-part-2-dependency-injection-di">The Fix Part 2: Dependency Injection (DI)</a>    <ul>
      <li><a href="#application-logic-c" id="markdown-toc-application-logic-c">Application Logic (C)</a></li>
    </ul>
  </li>
  <li><a href="#interactive-hands-on-unit-testing-on-pc-zero-hardware-needed" id="markdown-toc-interactive-hands-on-unit-testing-on-pc-zero-hardware-needed">Interactive Hands-On: Unit Testing on PC (Zero Hardware Needed)</a>    <ul>
      <li><a href="#what-you-just-unlocked" id="markdown-toc-what-you-just-unlocked">What you just unlocked:</a></li>
    </ul>
  </li>
  <li><a href="#the-performance-myth-how-bad-is-the-overhead" id="markdown-toc-the-performance-myth-how-bad-is-the-overhead">The Performance Myth: How Bad Is the Overhead?</a>    <ul>
      <li><a href="#1-function-pointer--vtable-call-cost" id="markdown-toc-1-function-pointer--vtable-call-cost">1. Function Pointer / Vtable Call Cost</a></li>
      <li><a href="#2-what-if-you-need-zero-overhead-eg-10-mhz-bit-bang-or-fast-isr" id="markdown-toc-2-what-if-you-need-zero-overhead-eg-10-mhz-bit-bang-or-fast-isr">2. What if you need Zero Overhead (e.g. 10 MHz Bit-Bang or Fast ISR)?</a>        <ul>
          <li><a href="#option-a-c-templates-static-polymorphism--crtp" id="markdown-toc-option-a-c-templates-static-polymorphism--crtp">Option A: C++ Templates (Static Polymorphism / CRTP)</a></li>
          <li><a href="#option-b-c-link-time-binding" id="markdown-toc-option-b-c-link-time-binding">Option B: C Link-Time Binding</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><a href="#when-not-to-use-dependency-injection" id="markdown-toc-when-not-to-use-dependency-injection">When NOT to Use Dependency Injection</a></li>
  <li><a href="#incremental-adoption-how-to-start-on-legacy-code" id="markdown-toc-incremental-adoption-how-to-start-on-legacy-code">Incremental Adoption: How to Start on Legacy Code</a></li>
</ul>

<h1 id="the-board-revision-nightmare">The Board Revision Nightmare</h1>

<p>Picture this scenario: You’re building a thermal management controller for an industrial gateway. You pick an STM32, hook up an I2C temperature sensor to <code class="language-plaintext highlighter-rouge">I2C1</code> (<code class="language-plaintext highlighter-rouge">GPIOB6/7</code>), write the driver using your vendor’s HAL, and verify it on the bench. It works great.</p>

<p>Six months later:</p>
<ol>
  <li><strong>The component shortage hits</strong>: Your MCU is lead-timed out for 40 weeks, so management swaps to an ESP32 or RP2040.</li>
  <li><strong>The PCB layout changes</strong>: The hardware engineer moves the sensor from <code class="language-plaintext highlighter-rouge">I2C1</code> to <code class="language-plaintext highlighter-rouge">I2C2</code> on a different pin bank.</li>
  <li><strong>The testing request arrives</strong>: The QA team asks for automated unit tests that verify fan triggers at 85°C — without someone manually heating the board with a heat gun.</li>
</ol>

<p>If your temperature checking logic calls <code class="language-plaintext highlighter-rouge">HAL_I2C_Master_Transmit(&amp;hi2c1, ...)</code> directly, you’re stuck:</p>
<ul>
  <li>You have to rewrite application logic for every new board or MCU.</li>
  <li>You can’t test safety-critical alarms without physical hardware.</li>
  <li>You can’t simulate I2C bus errors (NACKs, timeouts) on your PC.</li>
</ul>

<p>This is <strong>tight coupling</strong>. The problem isn’t the hardware — it’s that business logic knows <em>too much</em> about register addresses and vendor HAL handles.</p>

<hr />

<h1 id="the-anti-pattern-hardware-leaking-into-logic">The Anti-Pattern: Hardware Leaking into Logic</h1>

<p>Here is how embedded code typically gets written under pressure:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// thermal_control.c — BAD: Application logic hardcoded to STM32 HAL &amp; I2C1</span>
<span class="cp">#include</span> <span class="cpf">"stm32f4xx_hal.h"</span><span class="cp">
#include</span> <span class="cpf">"main.h"</span><span class="cp">
</span>
<span class="k">extern</span> <span class="n">I2C_HandleTypeDef</span> <span class="n">hi2c1</span><span class="p">;</span>
<span class="cp">#define TMP102_I2C_ADDR (0x48 &lt;&lt; 1)
</span>
<span class="kt">void</span> <span class="nf">check_system_temperature</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">uint8_t</span> <span class="n">reg</span> <span class="o">=</span> <span class="mh">0x00</span><span class="p">;</span> <span class="c1">// Temperature register</span>
    <span class="kt">uint8_t</span> <span class="n">data</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">};</span>

    <span class="c1">// Application logic is calling low-level STM32 HAL directly!</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">HAL_I2C_Master_Transmit</span><span class="p">(</span><span class="o">&amp;</span><span class="n">hi2c1</span><span class="p">,</span> <span class="n">TMP102_I2C_ADDR</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">reg</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">100</span><span class="p">)</span> <span class="o">==</span> <span class="n">HAL_OK</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">HAL_I2C_Master_Receive</span><span class="p">(</span><span class="o">&amp;</span><span class="n">hi2c1</span><span class="p">,</span> <span class="n">TMP102_I2C_ADDR</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">100</span><span class="p">)</span> <span class="o">==</span> <span class="n">HAL_OK</span><span class="p">)</span> <span class="p">{</span>
            <span class="kt">int16_t</span> <span class="n">raw</span> <span class="o">=</span> <span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">&lt;&lt;</span> <span class="mi">4</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">&gt;&gt;</span> <span class="mi">4</span><span class="p">);</span>
            <span class="kt">float</span> <span class="n">temp_c</span> <span class="o">=</span> <span class="n">raw</span> <span class="o">*</span> <span class="mi">0</span><span class="p">.</span><span class="mo">0625</span><span class="n">f</span><span class="p">;</span>

            <span class="k">if</span> <span class="p">(</span><span class="n">temp_c</span> <span class="o">&gt;</span> <span class="mi">80</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">)</span> <span class="p">{</span>
                <span class="c1">// Direct GPIO call to trigger cooling fan</span>
                <span class="n">HAL_GPIO_WritePin</span><span class="p">(</span><span class="n">FAN_GPIO_Port</span><span class="p">,</span> <span class="n">FAN_Pin</span><span class="p">,</span> <span class="n">GPIO_PIN_SET</span><span class="p">);</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="why-this-breaks-down">Why this breaks down:</h3>
<ol>
  <li><strong>Zero Portability</strong>: Porting to an ESP32 or RP2040 requires touching <code class="language-plaintext highlighter-rouge">thermal_control.c</code> because STM32 HAL headers are baked into it.</li>
  <li><strong>Zero Host Testability</strong>: You cannot compile this on your Linux/macOS/Windows PC because <code class="language-plaintext highlighter-rouge">stm32f4xx_hal.h</code> doesn’t exist on x86.</li>
  <li><strong>Zero Fault Injection</strong>: How do you test what happens when the sensor fails to respond? You have to physically desolder the chip or short the bus lines.</li>
</ol>

<hr />

<h1 id="the-fix-part-1-dependency-inversion-dip">The Fix Part 1: Dependency Inversion (DIP)</h1>

<p>The <strong>Dependency Inversion Principle</strong> states:</p>
<blockquote>
  <p>High-level business logic (e.g. <em>turning on a fan above 80°C</em>) should not depend on low-level details (e.g. <em>STM32 I2C1 registers</em>). Both should depend on an <strong>abstraction</strong>.</p>
</blockquote>

<p>Let’s separate <em>what</em> the application needs from <em>how</em> the hardware delivers it.</p>

<h3 id="in-c-interface-via-struct-of-function-pointers">In C: Interface via <code class="language-plaintext highlighter-rouge">struct</code> of function pointers</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// temp_sensor.h — The Abstraction (No HAL headers here!)</span>
<span class="cp">#pragma once
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">bool</span> <span class="p">(</span><span class="o">*</span><span class="n">read_temp_c</span><span class="p">)(</span><span class="kt">float</span> <span class="o">*</span><span class="n">temp_out</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">user_data</span><span class="p">);</span>
    <span class="kt">void</span> <span class="o">*</span><span class="n">user_data</span><span class="p">;</span> <span class="c1">// Context (e.g. bus handle, I2C address)</span>
<span class="p">}</span> <span class="n">temp_sensor_t</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="in-c-abstract-interface-class">In C++: Abstract Interface Class</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ITemperatureSensor.hpp — C++ Abstraction</span>
<span class="cp">#pragma once
</span>
<span class="k">class</span> <span class="nc">ITemperatureSensor</span> <span class="p">{</span>
<span class="nl">public:</span>
    <span class="k">virtual</span> <span class="o">~</span><span class="n">ITemperatureSensor</span><span class="p">()</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>
    <span class="k">virtual</span> <span class="kt">bool</span> <span class="n">read_temp_c</span><span class="p">(</span><span class="kt">float</span><span class="o">&amp;</span> <span class="n">temp_out</span><span class="p">)</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>Notice what is missing: No <code class="language-plaintext highlighter-rouge">#include "stm32f4xx_hal.h"</code>, no pin numbers, no register offsets. <code class="language-plaintext highlighter-rouge">temp_sensor_t</code> is pure C contract.</p>

<hr />

<h1 id="the-fix-part-2-dependency-injection-di">The Fix Part 2: Dependency Injection (DI)</h1>

<p>Dependency Inversion gave us the contract. <strong>Dependency Injection</strong> is simply passing that contract into your application module from the outside, rather than letting the module fetch its own globals.</p>

<h3 id="application-logic-c">Application Logic (C)</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// thermal_monitor.c — Pure application logic</span>
<span class="cp">#include</span> <span class="cpf">"temp_sensor.h"</span><span class="cp">
#include</span> <span class="cpf">"fan_control.h"</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">temp_sensor_t</span> <span class="n">sensor</span><span class="p">;</span>
    <span class="n">fan_control_t</span> <span class="n">fan</span><span class="p">;</span>
    <span class="kt">float</span> <span class="n">warning_threshold_c</span><span class="p">;</span>
<span class="p">}</span> <span class="n">thermal_monitor_t</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">thermal_monitor_init</span><span class="p">(</span><span class="n">thermal_monitor_t</span> <span class="o">*</span><span class="n">mon</span><span class="p">,</span> <span class="n">temp_sensor_t</span> <span class="n">sensor</span><span class="p">,</span> <span class="n">fan_control_t</span> <span class="n">fan</span><span class="p">,</span> <span class="kt">float</span> <span class="n">threshold</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">mon</span><span class="o">-&gt;</span><span class="n">sensor</span> <span class="o">=</span> <span class="n">sensor</span><span class="p">;</span>
    <span class="n">mon</span><span class="o">-&gt;</span><span class="n">fan</span> <span class="o">=</span> <span class="n">fan</span><span class="p">;</span>
    <span class="n">mon</span><span class="o">-&gt;</span><span class="n">warning_threshold_c</span> <span class="o">=</span> <span class="n">threshold</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">thermal_monitor_process</span><span class="p">(</span><span class="n">thermal_monitor_t</span> <span class="o">*</span><span class="n">mon</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">current_temp</span> <span class="o">=</span> <span class="mi">0</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">mon</span><span class="o">-&gt;</span><span class="n">sensor</span><span class="p">.</span><span class="n">read_temp_c</span><span class="p">(</span><span class="o">&amp;</span><span class="n">current_temp</span><span class="p">,</span> <span class="n">mon</span><span class="o">-&gt;</span><span class="n">sensor</span><span class="p">.</span><span class="n">user_data</span><span class="p">))</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">current_temp</span> <span class="o">&gt;=</span> <span class="n">mon</span><span class="o">-&gt;</span><span class="n">warning_threshold_c</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">mon</span><span class="o">-&gt;</span><span class="n">fan</span><span class="p">.</span><span class="n">set_speed</span><span class="p">(</span><span class="mi">100</span><span class="p">);</span> <span class="c1">// Max cooling</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="n">mon</span><span class="o">-&gt;</span><span class="n">fan</span><span class="p">.</span><span class="n">set_speed</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Now <code class="language-plaintext highlighter-rouge">thermal_monitor_process()</code> doesn’t know or care if the temperature comes from an I2C chip, an analog NTC thermistor, or a fake test driver.</p>

<hr />

<h1 id="interactive-hands-on-unit-testing-on-pc-zero-hardware-needed">Interactive Hands-On: Unit Testing on PC (Zero Hardware Needed)</h1>

<p>Because our application logic depends only on <code class="language-plaintext highlighter-rouge">temp_sensor_t</code>, we can write a <strong>fake driver</strong> that runs on Linux, macOS, or Windows in a standard GCC/Clang test binary.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// test_thermal_monitor.c — Runs on your development PC!</span>
<span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;assert.h&gt;</span><span class="cp">
#include</span> <span class="cpf">"temp_sensor.h"</span><span class="cp">
#include</span> <span class="cpf">"thermal_monitor.h"</span><span class="cp">
</span>
<span class="c1">// Fake sensor state for testing</span>
<span class="k">static</span> <span class="kt">float</span> <span class="n">simulated_temp</span> <span class="o">=</span> <span class="mi">25</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">;</span>
<span class="k">static</span> <span class="kt">int</span> <span class="n">simulated_fan_speed</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

<span class="k">static</span> <span class="n">bool</span> <span class="nf">fake_read_temp</span><span class="p">(</span><span class="kt">float</span> <span class="o">*</span><span class="n">temp_out</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">user_data</span><span class="p">)</span> <span class="p">{</span>
    <span class="p">(</span><span class="kt">void</span><span class="p">)</span><span class="n">user_data</span><span class="p">;</span>
    <span class="o">*</span><span class="n">temp_out</span> <span class="o">=</span> <span class="n">simulated_temp</span><span class="p">;</span>
    <span class="k">return</span> <span class="nb">true</span><span class="p">;</span> <span class="c1">// Simulate successful read</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">void</span> <span class="nf">fake_set_fan_speed</span><span class="p">(</span><span class="kt">int</span> <span class="n">speed</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">simulated_fan_speed</span> <span class="o">=</span> <span class="n">speed</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Running Host Unit Test: Thermal Monitor...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">temp_sensor_t</span> <span class="n">fake_sensor</span> <span class="o">=</span> <span class="p">{</span> <span class="p">.</span><span class="n">read_temp_c</span> <span class="o">=</span> <span class="n">fake_read_temp</span><span class="p">,</span> <span class="p">.</span><span class="n">user_data</span> <span class="o">=</span> <span class="nb">NULL</span> <span class="p">};</span>
    <span class="n">fan_control_t</span> <span class="n">fake_fan</span> <span class="o">=</span> <span class="p">{</span> <span class="p">.</span><span class="n">set_speed</span> <span class="o">=</span> <span class="n">fake_set_fan_speed</span> <span class="p">};</span>
    
    <span class="n">thermal_monitor_t</span> <span class="n">monitor</span><span class="p">;</span>
    <span class="n">thermal_monitor_init</span><span class="p">(</span><span class="o">&amp;</span><span class="n">monitor</span><span class="p">,</span> <span class="n">fake_sensor</span><span class="p">,</span> <span class="n">fake_fan</span><span class="p">,</span> <span class="mi">80</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">);</span>

    <span class="c1">// Test 1: Normal temperature (25°C) -&gt; Fan should stay OFF</span>
    <span class="n">simulated_temp</span> <span class="o">=</span> <span class="mi">25</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">;</span>
    <span class="n">thermal_monitor_process</span><span class="p">(</span><span class="o">&amp;</span><span class="n">monitor</span><span class="p">);</span>
    <span class="n">assert</span><span class="p">(</span><span class="n">simulated_fan_speed</span> <span class="o">==</span> <span class="mi">0</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"✓ Normal temperature test passed</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="c1">// Test 2: Over-temperature condition (85°C) -&gt; Fan should turn ON (100%)</span>
    <span class="n">simulated_temp</span> <span class="o">=</span> <span class="mi">85</span><span class="p">.</span><span class="mi">0</span><span class="n">f</span><span class="p">;</span>
    <span class="n">thermal_monitor_process</span><span class="p">(</span><span class="o">&amp;</span><span class="n">monitor</span><span class="p">);</span>
    <span class="n">assert</span><span class="p">(</span><span class="n">simulated_fan_speed</span> <span class="o">==</span> <span class="mi">100</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"✓ Over-temperature alarm test passed</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">All host unit tests passed in 0.002 seconds!</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="what-you-just-unlocked">What you just unlocked:</h3>
<ul>
  <li>You ran an embedded safety check on your PC in <strong>2 milliseconds</strong>.</li>
  <li>You can plug this test into <strong>GitHub Actions</strong> or <strong>Woodpecker CI</strong>.</li>
  <li>You don’t need a single MCU, debugger, or heat gun on your desk to verify your alarm logic.</li>
</ul>

<hr />

<h1 id="the-performance-myth-how-bad-is-the-overhead">The Performance Myth: How Bad Is the Overhead?</h1>

<p>The first reaction from firmware developers is usually:</p>
<blockquote>
  <p><em>“Function pointers add overhead! Virtual functions waste clock cycles!”</em></p>
</blockquote>

<p>Let’s look at the numbers on actual microcontrollers.</p>

<h3 id="1-function-pointer--vtable-call-cost">1. Function Pointer / Vtable Call Cost</h3>
<p>On an ARM Cortex-M4 running at 168 MHz:</p>
<ul>
  <li>An indirect call via function pointer takes <strong>1 to 3 clock cycles</strong> (~6 to 18 nanoseconds).</li>
  <li>If your sensor loop runs at 10 Hz or 100 Hz, that indirect branch consumes less than <strong>0.00001%</strong> of your CPU budget.</li>
</ul>

<h3 id="2-what-if-you-need-zero-overhead-eg-10-mhz-bit-bang-or-fast-isr">2. What if you need Zero Overhead (e.g. 10 MHz Bit-Bang or Fast ISR)?</h3>
<p>For paths where every single cycle counts, you don’t have to sacrifice clean code. You can use <strong>Compile-Time / Static Dependency Injection</strong>:</p>

<h4 id="option-a-c-templates-static-polymorphism--crtp">Option A: C++ Templates (Static Polymorphism / CRTP)</h4>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="nc">TempSensor</span><span class="p">,</span> <span class="k">typename</span> <span class="nc">FanController</span><span class="p">&gt;</span>
<span class="k">class</span> <span class="nc">ThermalMonitor</span> <span class="p">{</span>
    <span class="n">TempSensor</span><span class="o">&amp;</span> <span class="n">sensor_</span><span class="p">;</span>
    <span class="n">FanController</span><span class="o">&amp;</span> <span class="n">fan_</span><span class="p">;</span>
<span class="nl">public:</span>
    <span class="n">ThermalMonitor</span><span class="p">(</span><span class="n">TempSensor</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">,</span> <span class="n">FanController</span><span class="o">&amp;</span> <span class="n">f</span><span class="p">)</span> <span class="o">:</span> <span class="n">sensor_</span><span class="p">(</span><span class="n">s</span><span class="p">),</span> <span class="n">fan_</span><span class="p">(</span><span class="n">f</span><span class="p">)</span> <span class="p">{}</span>
    
    <span class="kt">void</span> <span class="nf">process</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">sensor_</span><span class="p">.</span><span class="n">read_temp_c</span><span class="p">()</span> <span class="o">&gt;</span> <span class="mf">80.0f</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">fan_</span><span class="p">.</span><span class="n">set_speed</span><span class="p">(</span><span class="mi">100</span><span class="p">);</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div>
<p><em>Result</em>: The compiler inlines <code class="language-plaintext highlighter-rouge">sensor_.read_temp_c()</code> directly into the assembly call. <strong>0 cycles overhead, 0 vtables, full testability.</strong></p>

<h4 id="option-b-c-link-time-binding">Option B: C Link-Time Binding</h4>
<p>In C, keep the header interface clean and provide two different <code class="language-plaintext highlighter-rouge">.c</code> files during compilation:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">bsp_stm32_sensor.c</code> (compiled into the target firmware)</li>
  <li><code class="language-plaintext highlighter-rouge">fake_host_sensor.c</code> (compiled into your PC test binary)</li>
</ul>

<hr />

<h1 id="when-not-to-use-dependency-injection">When NOT to Use Dependency Injection</h1>

<p>Design patterns are tools, not commandments. Avoid DI abstractions when:</p>
<ol>
  <li><strong>Ultra-Constrained MCUs</strong>: Sub-4KB Flash targets (ATtiny, PIC16) where saving 40 bytes of function pointer pointers is critical.</li>
  <li><strong>High-Frequency ISRs</strong>: Interrupt routines firing at 500 kHz where an extra indirect branch breaks timing constraints.</li>
  <li><strong>One-Off Validation Scripts</strong>: A quick 20-line test script written to verify a hardware pin on an oscilloscope before throwing it away.</li>
</ol>

<hr />

<h1 id="incremental-adoption-how-to-start-on-legacy-code">Incremental Adoption: How to Start on Legacy Code</h1>

<p>You don’t need to rewrite your entire 100,000-line codebase overnight. Pick one module boundary and follow these steps:</p>

<ol>
  <li><strong>Find a Seam</strong>: Choose a single peripheral or sensor (e.g., an EEPROM or temperature sensor).</li>
  <li><strong>Create a Clean Header</strong>: Move direct HAL calls out of your application files and behind a simple C <code class="language-plaintext highlighter-rouge">struct</code> interface or C++ class.</li>
  <li><strong>Write a Host Fake</strong>: Create a 20-line fake implementation in C/C++ and write a PC host test for that module.</li>
  <li><strong>Wire it in <code class="language-plaintext highlighter-rouge">main()</code></strong>: Pass the real hardware handle in <code class="language-plaintext highlighter-rouge">main.c</code> on the MCU, and pass the fake handle in your PC test runner.</li>
</ol>

<p>Doing this for just one driver gives you immediate unit testing capability on your development laptop and shields your core logic from future hardware board revisions.</p>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="es" /><category term="c++" /><summary type="html"><![CDATA[Why direct register writes and HAL calls inside your application logic make firmware impossible to unit test on PC. Here is how to use Dependency Inversion and Injection in C and C++ using real I2C sensor hardware.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/Di.webp" /><media:content medium="image" url="/cover_images/posts/Di.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">An Experimental Setup - for Building Local First, Portable Embedded C++ pipeline with self-hosted CI</title><link href="/blog/es/2026-02-28-pigweed-cosmopolitan-woodpeaker/" rel="alternate" type="text/html" title="An Experimental Setup - for Building Local First, Portable Embedded C++ pipeline with self-hosted CI" /><published>2026-02-28T00:00:00+00:00</published><updated>2026-07-22T14:18:12+00:00</updated><id>/blog/es/pigweed-cosmopolitan-woodpeaker</id><content type="html" xml:base="/blog/es/2026-02-28-pigweed-cosmopolitan-woodpeaker/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#the-problem-nobody-is-talking-about" id="markdown-toc-the-problem-nobody-is-talking-about">The Problem Nobody Is Talking About</a></li>
  <li><a href="#what-hil-testing-is-and-why-its-a-bit-broken-" id="markdown-toc-what-hil-testing-is-and-why-its-a-bit-broken-">What HIL Testing Is, and Why It’s a bit Broken ?</a></li>
  <li><a href="#three-tools-one-insight" id="markdown-toc-three-tools-one-insight">Three Tools, One Insight</a></li>
  <li><a href="#what-this-stack-actually-gets-you-and-what-it-doesnt" id="markdown-toc-what-this-stack-actually-gets-you-and-what-it-doesnt">What This Stack Actually Gets You (and What It Doesn’t)</a></li>
  <li><a href="#getting-started" id="markdown-toc-getting-started">Getting Started</a></li>
  <li><a href="#the-testing-pyramid-this-stack-enables" id="markdown-toc-the-testing-pyramid-this-stack-enables">The Testing Pyramid This Stack Enables</a></li>
  <li><a href="#level-1-unit-tests" id="markdown-toc-level-1-unit-tests">Level 1 (Unit Tests):</a></li>
  <li><a href="#level-2-host-simulation" id="markdown-toc-level-2-host-simulation">Level 2 (Host Simulation):</a></li>
  <li><a href="#level-3-on-device-tests" id="markdown-toc-level-3-on-device-tests">Level 3 (On-Device Tests):</a></li>
  <li><a href="#level-4-scheduled-on-target-regression" id="markdown-toc-level-4-scheduled-on-target-regression">Level 4 (Scheduled On-Target Regression):</a></li>
  <li><a href="#pillar-one-pigweed--write-once-test-everywhere" id="markdown-toc-pillar-one-pigweed--write-once-test-everywhere">Pillar One: Pigweed — Write Once, Test Everywhere</a>    <ul>
      <li><a href="#the-facade-pattern-in-practice" id="markdown-toc-the-facade-pattern-in-practice">The Facade Pattern in Practice</a></li>
      <li><a href="#pw_unit_test-one-test-binary-two-targets" id="markdown-toc-pw_unit_test-one-test-binary-two-targets">pw_unit_test: One Test Binary, Two Targets</a></li>
      <li><a href="#pw_watch-and-pw_target_runner-automated-on-device-ci" id="markdown-toc-pw_watch-and-pw_target_runner-automated-on-device-ci">pw_watch and pw_target_runner: Automated On-Device CI</a></li>
    </ul>
  </li>
  <li><a href="#pillar-two-cosmopolitan--the-toolchain-portability-problem" id="markdown-toc-pillar-two-cosmopolitan--the-toolchain-portability-problem">Pillar Two: Cosmopolitan — The Toolchain Portability Problem</a>    <ul>
      <li><a href="#what-this-means-for-an-embedded-pipeline" id="markdown-toc-what-this-means-for-an-embedded-pipeline">What This Means for an Embedded Pipeline</a></li>
    </ul>
  </li>
  <li><a href="#pillar-three-woodpecker-ci--self-hosted-hardware-attached-free" id="markdown-toc-pillar-three-woodpecker-ci--self-hosted-hardware-attached-free">Pillar Three: Woodpecker CI — Self-Hosted, Hardware-Attached, Free</a>    <ul>
      <li><a href="#the-agent-model-for-hardware" id="markdown-toc-the-agent-model-for-hardware">The Agent Model for Hardware</a></li>
    </ul>
  </li>
  <li><a href="#summary" id="markdown-toc-summary">Summary</a>    <ul>
      <li><a href="#why-not-github-actions-with-self-hosted-runners" id="markdown-toc-why-not-github-actions-with-self-hosted-runners">Why Not GitHub Actions with Self-Hosted Runners?</a></li>
    </ul>
  </li>
  <li><a href="#where-host-tests-meet-hardware" id="markdown-toc-where-host-tests-meet-hardware">Where Host Tests Meet Hardware</a></li>
  <li><a href="#resources" id="markdown-toc-resources">Resources</a></li>
  <li><a href="#sources" id="markdown-toc-sources">Sources</a></li>
</ul>

<p>In good DevOps practice, automation across building, testing, and releasing software plays a prominent role. CI (continuous integration) acts as a safety net: it takes raw materials (code), assembles them (builds), and quality-checks them (tests). If CI fails, the second half of the acronym (CD: continuous delivery/deployment) becomes too risky and can lead to serious production issues. There are several DevOps tools providing complete CI/CD services, but things get tricky while developing and testing embedded software.</p>

<h2 id="the-problem-nobody-is-talking-about">The Problem Nobody Is Talking About</h2>

<p>CI/CD is very stable in web and backend development. For embedded firmware, the picture is more uneven.</p>

<p>Some teams have real pipelines, which include tools like GitLab CI/CD runners that cross-compile, run QEMU simulations, flash a board over a self-hosted runner with USB access, collect serial logs, and push artifacts to a dashboard.</p>

<p>These setups work and they exist in production today. But they’re almost always custom-built, tricky to maintain, and tightly coupled to one specific board, one specific CI platform, and one team’s institutional knowledge. When the engineer or team who set it up leaves, the pipeline starts to rot.</p>

<p>At the other end, many small firmware teams have no CI at all, or a GitHub Actions workflow that cross-compiles and calls it a day. But logical bugs that a 10-second host simulation would have caught survive to hardware integration, where they take days to diagnose. Full regression testing is sometimes missing, so QA engineers have to do it manually.</p>

<div class="think">

  <p>The gap isn’t between “no CI” and “perfect CI.” It’s between ad-hoc, fragile setups and something that’s <strong>portable across development boards and OSes, self-hosted without cloud dependency (due to compliance requirements, aerospace, defense, and medical teams often resist pushing proprietary source code or massive firmware binaries to Microsoft or GitLab’s public cloud platforms), and structured enough that it doesn’t collapse when the individuals who set up the entire CI pipeline leave the team</strong>.</p>
</div>

<p>The structural problems are the same across teams of this size: the build system and test hardware are physically separate, hosted cloud CI runners have no USB or JTAG access, host simulation and device results live in separate codebases maintained by different people. When simulation passes and the board fails, diagnosis starts from zero. That is the gap this stack closes - not full analog HIL, but the step before it that most small teams are missing entirely.</p>

<h2 id="what-hil-testing-is-and-why-its-a-bit-broken-">What HIL Testing Is, and Why It’s a bit Broken ?</h2>

<p>Hardware-in-the-loop (HIL) testing is the gold standard for embedded validation: production firmware runs on a real controller while surrounding sensors, actuators, and bus traffic are simulated in a controlled, repeatable way.<sup id="fnref:4" role="doc-noteref"><a href="#fn:4" class="footnote" rel="footnote">1</a></sup><sup id="fnref:8" role="doc-noteref"><a href="#fn:8" class="footnote" rel="footnote">2</a></sup> In high-reliability domains such as automotive, aerospace, and medical systems, this class of testing is often mandatory in practice because it catches timing and integration failures that pure host-side tests can miss.<sup id="fnref:1" role="doc-noteref"><a href="#fn:1" class="footnote" rel="footnote">3</a></sup><sup id="fnref:3" role="doc-noteref"><a href="#fn:3" class="footnote" rel="footnote">4</a></sup></p>

<div class="important">

  <p><strong>Why HIL is relevant to this post</strong>
This post is not claiming to replace full HIL. It is showing the practical path that makes HIL adoption possible for small firmware teams.</p>

  <p>This stack (Woodpeacker CI + Cosmopolitian + Pigweed) covers the missing middle layer between no-hardware CI and full industrial HIL, while keeping the boundaries explicit.</p>

</div>

<ol>
  <li><strong>Problem evidence:</strong> Embedded defects often survive compile-only CI and appear only during hardware interaction.</li>
  <li><strong>HIL relevance:</strong> HIL exists to expose exactly those hardware-interaction failures under repeatable conditions.<sup id="fnref:4:1" role="doc-noteref"><a href="#fn:4" class="footnote" rel="footnote">1</a></sup><sup id="fnref:8:1" role="doc-noteref"><a href="#fn:8" class="footnote" rel="footnote">2</a></sup></li>
  <li><strong>Current gap in small teams:</strong> Full HIL benches are costly and operationally heavy, so many teams stop at host tests or manual bench checks.<sup id="fnref:2" role="doc-noteref"><a href="#fn:2" class="footnote" rel="footnote">5</a></sup><sup id="fnref:9" role="doc-noteref"><a href="#fn:9" class="footnote" rel="footnote">6</a></sup></li>
  <li><strong>What this stack contributes:</strong> Pigweed provides host/device test architecture, Cosmopolitan reduces host-tool portability friction, and Woodpecker provides self-hosted automation near real boards.</li>
  <li><strong>Systematic outcome:</strong> Teams move from manual build-flash-check loops to repeatable on-target regression, which is the operational precondition before scaling toward higher-fidelity HIL.</li>
</ol>

<div class="think">

  <p><strong>The problem is structural</strong> and it shows up the same way regardless of team size:</p>

</div>

<ol>
  <li><strong>The build system and the test hardware are physically separate</strong> Running on-device tests means manually flashing a binary and reading results, nothing in the build pipeline knows the board exists.</li>
  <li><strong>Hosted cloud CI has no direct concept of your lab hardware.</strong> GitHub-hosted runners don’t have your USB probes, serial adapters, or JTAG devices attached.</li>
  <li><strong>The validation gap.</strong>  Host tests and device tests are separate activities with separate codebases, often maintained by different people. When simulation passes and the board fails, there is no shared ground, diagnosis starts from zero.</li>
  <li><strong>Toolchain portability.</strong> The cross-compiler that produces the firmware binary behaves differently across developer machines, creating “works on my machine” problems before you even reach the hardware.</li>
</ol>

<p>This stack helps address all four in practical day-to-day engineering work - without mandatory cloud dependency and without requiring an expensive full HIL rig on day one <sup id="fnref:7" role="doc-noteref"><a href="#fn:7" class="footnote" rel="footnote">7</a></sup>. It can also be adopted incrementally instead of forcing an all-at-once CI platform migration. However, it does not simulate analog signals, environmental conditions, or replace compliance tooling for ISO 26262 or IEC 62304. Those gaps are real and worth knowing before you read further.<sup id="fnref:9:1" role="doc-noteref"><a href="#fn:9" class="footnote" rel="footnote">6</a></sup></p>

<hr />

<h2 id="three-tools-one-insight">Three Tools, One Insight</h2>

<p>The stack is <strong>Woodpecker CI + Cosmopolitan Libc + Google Pigweed</strong>. None of these tools were designed together, but they address complementary pain points:</p>

<div class="note">

  <p>Embedded teams don’t lack motivation for using CI/CD workflow, they lack infrastructure that isn’t hardware-locked, OS-specific, or overly cloud-dependent. This stack is all three: hardware-agnostic, portable, and cloud-independent. While full analog HIL (simulating every complex electrical characteristic) may remain out of scope for small teams, the combination of these tools targets the automation gap. They move the needle from “manual laboratory testing” to automated on-target testing, allowing teams to catch bugs early in the CI/CD pipeline rather than at the very end of the V-Diagram</p>

</div>

<p>Here’s what each tool contributes:</p>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>Role in the solution</th>
      <th>Why it matters</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Google Pigweed</strong></td>
      <td>The Embedded Framework</td>
      <td>Pigweed is a collection of libraries designed for embedded development. It provides the hardware-agnostic C++ architecture layer by offering robust modules for unit testing, RPCs, and device abstraction that work the same on your host machine as they do on the microcontroller, collapsing the gap between simulation and real hardware testing.</td>
    </tr>
    <tr>
      <td><strong>Cosmopolitan Libc</strong></td>
      <td>The Portability Engine</td>
      <td>This is the “secret sauce.” Cosmopolitan Libc allows you to compile C++ once into an Actually Portable Executable (APE) that runs natively on Linux, Mac, Windows, FreeBSD, and OpenBSD. This eliminates OS-specific barriers and makes build tools and test runners platform-independent.</td>
    </tr>
    <tr>
      <td><strong>Woodpecker CI</strong></td>
      <td>The Orchestrator</td>
      <td>A self-hosted, lightweight container-based CI runner. Because it is easy to self-host, it eliminates cloud dependence. You can run it on a local server or even a Raspberry Pi. It keeps your IP and binaries on-premises, on your lab bench, with USB access to real hardware and no cloud dependency.</td>
    </tr>
  </tbody>
</table>

<p><img src="/cover_images/posts/mermaid+cosmo+wood.webp" alt="mermaid" /></p>

<p>This stack is particularly potent because of how these three interact:</p>

<p>Eliminating “It works on my machine”: With Cosmopolitan, your build toolchain or test runner is a single binary that runs on any dev’s laptop and the CI runner without needing to manage complex dependencies or specific OS versions.</p>

<p>Local-First Development: Pigweed emphasizes that code should be testable on the host (your PC). You can run thousands of unit tests in seconds using Woodpecker before the code ever touches a physical chip.</p>

<p>Low Overhead: Unlike heavy enterprise CI suites, Woodpecker is tiny and fast. When paired with the efficiency of C++/Pigweed, your feedback loop (the time from “code save” to “test pass”) shrinks from minutes to seconds.</p>

<hr />

<h2 id="what-this-stack-actually-gets-you-and-what-it-doesnt">What This Stack Actually Gets You (and What It Doesn’t)</h2>

<div class="important">

  <p><strong>Before overselling this:</strong> running <code class="language-plaintext highlighter-rouge">pw_unit_test</code> on a board via a USB probe is <em>not</em> the same as what the industry calls HIL testing. A real HIL rig simulates analog sensor signals, controls power rails, injects noise, and emulates bus traffic with precise timing.</p>

</div>

<p>This stack also doesn’t address:</p>
<ul>
  <li><strong>Analog signal simulation.</strong> If your firmware reads a 4–20 mA current loop, you still need a DAC or signal generator to produce that signal in a test.</li>
  <li><strong>Environmental testing.</strong> Temperature chambers, EMC testing, and vibration are outside the scope of any CI pipeline.</li>
  <li><strong>Compliance certification.</strong> ISO 26262 (automotive), IEC 62304 (medical), and DO-178C (aerospace) all require specific evidence and traceability that a YAML pipeline alone doesn’t provide.</li>
  <li><strong>Mock fidelity.</strong> The mock backends you write for host testing are only as good as your understanding of the hardware. If you mock away the very behavior that causes the bug, the test is green and useless.</li>
</ul>

<p>What this stack <em>does</em> get you: a free, self-hosted pipeline that automates the dull parts like <em>compiling, running logic tests on the host, flashing firmware, and collecting results</em> so your limited engineering hours go toward the hard problems instead of manual “build-flash-check” cycles.</p>

<p>For a firmware team of 3–8 engineers at an industrial IoT company or a hardware startup that doesn’t have a $100K test rig budget. Also were the team are working with different Operating systems(OS), containerization is a necessity.</p>

<hr />

<h2 id="getting-started">Getting Started</h2>

<p>The recommended path to building this pipeline:</p>

<ol>
  <li><strong>Start with Pigweed’s Sense Showcase:</strong> Fork it and study how the facade/backend pattern is applied to a real project.</li>
  <li><strong>Add pw_unit_test to one module:</strong> Pick a logic-heavy module in your current firmware, abstract the hardware, and write five host-side tests.</li>
  <li><strong>Stand up Woodpecker CI:</strong> Run the Docker Compose file on a lab machine. Get host-side tests running in CI automatically.</li>
  <li><strong>Build your Cosmopolitan runner:</strong> Compile your host-side test harness with <code class="language-plaintext highlighter-rouge">cosmocc</code> and commit the binary to fix portability.</li>
  <li><strong>Attach a board to your Woodpecker agent:</strong> Configure <code class="language-plaintext highlighter-rouge">pw_target_runner</code>, write a hardware-gated step, and run your first automated on-device test in CI.</li>
</ol>

<h2 id="the-testing-pyramid-this-stack-enables">The Testing Pyramid This Stack Enables</h2>

<p>The embedded testing pyramid has four levels. This stack covers the first three well and partially addresses the fourth:</p>

<p><img src="/cover_images/posts/hil-pyramid.webp" alt="pic" /></p>

<p>Each level catches a different class of bug:</p>

<h2 id="level-1-unit-tests">Level 1 (Unit Tests):</h2>

<p>Pure logic, zero hardware dependency. Runs in &lt; 5 seconds on any machine.</p>
<ul>
  <li><strong>What it does?</strong> Pigweed provides a framework for host-side unit tests that can run rapidly without needing physical hardware.</li>
  <li><strong>Why it matters?</strong> This aligns with the “Model-in-the-Loop” (MIL) and “Software-in-the-Loop” (SIL) stages described in the V-Diagram, where the controller and environment are simulated to catch logic errors early.<sup id="fnref:6" role="doc-noteref"><a href="#fn:6" class="footnote" rel="footnote">8</a></sup></li>
</ul>

<h2 id="level-2-host-simulation">Level 2 (Host Simulation):</h2>

<p>Pigweed facade pattern — HAL mocked for host. Cosmopolitan test runner guarantees an identical environment on all OSes.</p>

<ul>
  <li>
    <p><strong>What it does?</strong> Pigweed is designed for “hermetic building,” meaning software is built in a completely isolated environment. This makes it less sensitive to the libraries, tools, or configurations installed on the host machine, ensuring the development environment is reproducible. Cosmopolitan Libc reinforces this by allowing C/C++ code to be “build-once run-anywhere,” providing an identical execution environment across Linux, Mac, Windows, and even BIOS.</p>
  </li>
  <li>
    <p><strong>Why it matters?</strong> Using Pigweed’s architecture (often utilizing a facade pattern for hardware abstraction layers) alongside Cosmopolitan ensures that host-side simulations are highly reliable and consistent across different developer operating systems.</p>
  </li>
</ul>

<h2 id="level-3-on-device-tests">Level 3 (On-Device Tests):</h2>

<p>Flashed firmware with automated test runner reporting over <code class="language-plaintext highlighter-rouge">pw_rpc</code>. Run on every PR via Woodpecker hardware-attached agent.</p>

<ul>
  <li><strong>What it does?</strong> Pigweed explicitly supports on-device unit tests and communication with hardware via <code class="language-plaintext highlighter-rouge">pw_rpc</code>.</li>
  <li><strong>Why it matters?</strong> Woodpecker CI provides the automation engine to trigger these tests. By using a “hardware-attached agent” (a common extensible use case for Woodpecker’s container-based pipelines), teams can automate the flashing and testing of firmware on every pull request.</li>
</ul>

<h2 id="level-4-scheduled-on-target-regression">Level 4 (Scheduled On-Target Regression):</h2>

<p>Triggered on main branch merges or nightly. Note: this level is where the gap between “on-target unit tests” and true regression tests (with analog signal simulation) becomes important. This stack automates on-target testing well; 
<strong>Full HIL with simulated physical signals still requires additional test equipment</strong>.</p>

<ul>
  <li><strong>What it does?</strong> On-target execution on the hardware confirms that your firmware runs correctly on real silicon. True HIL goes further , it electrically emulates the sensors and actuators your firmware controls, to simulating the entire plant setup.</li>
  <li><strong>Why it matters?</strong> While this stack automates the software side (on-target tests), the sources acknowledge that full HIL remains a “pivotal” and more complex stage involving mathematical representations of dynamic systems and specialized I/O interfaces. The stack effectively bridges the gap from “no testing” to “automated hardware testing.”</li>
</ul>

<div class="think">
  <p>The key advance is that levels 1 and 2 now share the same C++ source code as levels 3 and 4. When a test passes on the host and fails on the device, you’ve isolated a genuine hardware-specific bug.</p>
</div>

<hr />

<h2 id="pillar-one-pigweed--write-once-test-everywhere">Pillar One: Pigweed — Write Once, Test Everywhere</h2>
<p><img src="/cover_images/posts/Pigweed.webp" alt="pigweed" /></p>

<p>Pigweed is Google’s open-source embedded C++ framework, battle-tested in Pixel phones, Nest thermostats, DeepMind robots, and satellites. Its central architectural contribution is the <strong>facade/backend pattern</strong>.</p>

<h3 id="the-facade-pattern-in-practice">The Facade Pattern in Practice</h3>

<p>Every hardware interaction — SPI, GPIO, UART, I2C, a timer — is expressed as a C++ abstract class. In firmware, you instantiate the real backend that talks to hardware registers. In host simulation, you instantiate a mock backend that logs calls, returns scripted values, or asserts on call sequences. The business logic never changes.</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Your sensor driver — hardware-agnostic</span>
<span class="k">class</span> <span class="nc">PressureSensor</span> <span class="p">{</span>
 <span class="nl">public:</span>
  <span class="k">explicit</span> <span class="n">PressureSensor</span><span class="p">(</span><span class="n">pw</span><span class="o">::</span><span class="n">spi</span><span class="o">::</span><span class="n">Device</span><span class="o">&amp;</span> <span class="n">spi</span><span class="p">)</span> <span class="o">:</span> <span class="n">spi_</span><span class="p">(</span><span class="n">spi</span><span class="p">)</span> <span class="p">{}</span>

  <span class="n">pw</span><span class="o">::</span><span class="n">Result</span><span class="o">&lt;</span><span class="kt">float</span><span class="o">&gt;</span> <span class="n">ReadPressurePa</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// Reads over SPI — works on host (mock SPI) and on STM32 (real SPI)</span>
    <span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">byte</span><span class="p">,</span> <span class="mi">3</span><span class="o">&gt;</span> <span class="n">response</span><span class="p">;</span>
    <span class="n">PW_TRY</span><span class="p">(</span><span class="n">spi_</span><span class="p">.</span><span class="n">Read</span><span class="p">(</span><span class="n">response</span><span class="p">));</span>
    <span class="k">return</span> <span class="n">ParsePressureBytes</span><span class="p">(</span><span class="n">response</span><span class="p">);</span>
  <span class="p">}</span>

 <span class="k">private</span><span class="o">:</span>
  <span class="n">pw</span><span class="o">::</span><span class="n">spi</span><span class="o">::</span><span class="n">Device</span><span class="o">&amp;</span> <span class="n">spi_</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>On the host, <code class="language-plaintext highlighter-rouge">spi_</code> is a <code class="language-plaintext highlighter-rouge">FakeSpiDevice</code> that returns pre-programmed byte sequences. On an STM32 board, it’s a real SPI peripheral backed by DMA. The test for <code class="language-plaintext highlighter-rouge">ReadPressurePa()</code> runs identically in both environments.</p>

<h3 id="pw_unit_test-one-test-binary-two-targets">pw_unit_test: One Test Binary, Two Targets</h3>

<p>Pigweed’s unit test framework is GoogleTest-compatible but uses no dynamic memory allocation — it runs on bare-metal MCUs. The same test source compiles to an x86 binary for your CI container and an ARM binary for your board:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TEST</span><span class="p">(</span><span class="n">PressureSensorTest</span><span class="p">,</span> <span class="n">ReturnsCorrectPressureForKnownBytes</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">FakeSpiDevice</span> <span class="n">fake_spi</span><span class="p">({</span><span class="mh">0x00</span><span class="p">,</span> <span class="mh">0x67</span><span class="p">,</span> <span class="mh">0x1A</span><span class="p">});</span>
  <span class="n">PressureSensor</span> <span class="nf">sensor</span><span class="p">(</span><span class="n">fake_spi</span><span class="p">);</span>

  <span class="k">auto</span> <span class="n">result</span> <span class="o">=</span> <span class="n">sensor</span><span class="p">.</span><span class="n">ReadPressurePa</span><span class="p">();</span>

  <span class="n">ASSERT_TRUE</span><span class="p">(</span><span class="n">result</span><span class="p">.</span><span class="n">ok</span><span class="p">());</span>
  <span class="n">EXPECT_NEAR</span><span class="p">(</span><span class="n">result</span><span class="p">.</span><span class="n">value</span><span class="p">(),</span> <span class="mf">101325.0f</span><span class="p">,</span> <span class="mf">1.0f</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This test catches logic bugs at Level 1. When it also passes on the device at Level 3, you’ve confirmed hardware integration.</p>

<h3 id="pw_watch-and-pw_target_runner-automated-on-device-ci">pw_watch and pw_target_runner: Automated On-Device CI</h3>

<p><code class="language-plaintext highlighter-rouge">pw_watch</code> monitors your source tree and, on file save, compiles only the affected tests and flashes them to an attached device. <code class="language-plaintext highlighter-rouge">pw_target_runner</code> can distribute this across multiple boards running in parallel — the embedded equivalent of parallelized CI workers.</p>

<p>In a Woodpecker pipeline, this becomes:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># In a hardware-gated Woodpecker workflow file:</span>
<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">device-tests</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">ubuntu:22.04</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">pw_target_runner_client -host localhost -port 8080 -binary build/test_binary.elf</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">/dev/ttyACM0:/dev/ttyACM0</span>   <span class="c1"># The attached board</span>
</code></pre></div></div>

<hr />

<h2 id="pillar-two-cosmopolitan--the-toolchain-portability-problem">Pillar Two: Cosmopolitan — The Toolchain Portability Problem</h2>

<p><img src="/cover_images/posts/cosmo.webp" alt="cosmos" /></p>

<p>There’s a subtler problem that Pigweed alone doesn’t solve: your build tools themselves aren’t portable. A Python script that parses firmware map files on your Ubuntu CI machine fails on a Windows developer’s laptop because of path separator differences. These mismatches create builds that are “reproducible” in theory and brittle in practice.</p>

<p>Cosmopolitan Libc addresses this for host-side tooling. It reconfigures GCC and Clang to produce <strong>Actually Portable Executables (APEs)</strong>, polyglot binaries that run natively on Linux, macOS, Windows, FreeBSD, and NetBSD on both x86-64 and ARM64 without recompilation.</p>

<div class="warning">
  <p>The embedded toolchain itself (arm-none-eabi-gcc, OpenOCD, pw CLI, Python environment) still needs to be installed and version-matched. Cosmopolitan solves one narrow piece: the test runner binary itself being portable. But a Windows developer without the ARM toolchain, without OpenOCD, and without Pigweed’s bootstrap still can’t build firmware or run device tests.</p>
</div>

<h3 id="what-this-means-for-an-embedded-pipeline">What This Means for an Embedded Pipeline</h3>

<p>Your host-side test infrastructure, the binary that runs <code class="language-plaintext highlighter-rouge">pw_unit_test</code> results on the host, the serial port listener that collects device test output, the log parser and the code coverage reporter, all of these can all be built with <code class="language-plaintext highlighter-rouge">cosmocc</code> instead of <code class="language-plaintext highlighter-rouge">cc</code>.</p>

<p>The resulting binaries are checked into the repository and work identically everywhere.</p>

<p>A new team member on Windows clones the repo, runs <code class="language-plaintext highlighter-rouge">./tools/run_host_tests</code>, and gets the same results as the Linux CI agent. No Docker required for local development.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Build the host test runner once with cosmocc</span>
cosmocc <span class="nt">-o</span> tools/run_host_tests src/test_runner.c

<span class="c"># Now this binary works on:</span>
<span class="c"># - Ubuntu </span>
<span class="c"># - macOS </span>
<span class="c"># - Windows 11 (native, not WSL)</span>
<span class="c"># - Raspberry Pi 5 lab bench server (ARM64)</span>
</code></pre></div></div>

<div class="warning">

  <p><strong>Check Your Scope</strong>
 The firmware binary itself is still compiled with <code class="language-plaintext highlighter-rouge">arm-none-eabi-gcc</code>. Cosmopolitan is for your <em>development tooling</em>, not for the bare-metal MCU. Also note that Cosmopolitan ships its own C++ standard library (<code class="language-plaintext highlighter-rouge">ctl/</code>) rather than libstdc++, so complex C++ code with heavy template metaprogramming or bleeding-edge C++20/23 features may hit compatibility gaps. For simple CLI tools and test runners, this is rarely an issue.</p>

</div>

<hr />

<h2 id="pillar-three-woodpecker-ci--self-hosted-hardware-attached-free">Pillar Three: Woodpecker CI — Self-Hosted, Hardware-Attached, Free</h2>

<p><img src="/cover_images/posts/woodpeacker.webp" alt="woodpecker" /></p>

<p>GitHub Actions and GitLab CI aren’t built for embedded firmware: their runners are ephemeral cloud VMs with no physical I/O. You can’t attach a JTAG probe, a USB-serial adapter, or a power relay to a cloud runner.</p>

<p>Woodpecker CI runs on your hardware. The server is a lightweight Go binary that talks to your Git forge. Agents, the workers that execute pipeline steps (Not to be confused with Agentic AI agents) A Woodpecker agent is a separate process (often a container) which run wherever you need them. One agent on a spare laptop in the lab bench has access to every USB device connected to that machine.</p>

<h3 id="the-agent-model-for-hardware">The Agent Model for Hardware</h3>

<p>Woodpecker’s edge for embedded CI is its agent label system. You label an agent, say for example: <code class="language-plaintext highlighter-rouge">hw-stm32h750</code> and restrict workflows to that agent(machine) where the board is physically connected.</p>

<p>Woodpecker routes entire workflow files to agents via labels. So you split your pipeline across multiple workflow files:</p>

<p>for example, this yaml file builds the basic build for the stm32H750 MCU</p>

<p>Steps of operation:</p>

<ul>
  <li>
    <p>(1) CMake build with ARM GCC in Release mode</p>
  </li>
  <li>
    <p>(2) host-based tests via Cosmopolitan binary</p>
  </li>
  <li>
    <p>(3) Pigweed presubmit checks for linting, clang-format, and C++ analysis. Runs on generic agents</p>
  </li>
</ul>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .woodpecker/build-and-host.yaml</span>
<span class="c1"># Runs on any agent (no hardware needed)</span>

<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">build</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">gcc-arm:13</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">cmake -B build -DTARGET=stm32h750 -DCMAKE_BUILD_TYPE=Release</span>
      <span class="pi">-</span> <span class="s">cmake --build build -j$(nproc)</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">host-tests</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">ubuntu:22.04</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">./tools/run_host_tests</span>   <span class="c1"># Cosmopolitan binary, works anywhere</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">lint-and-presubmit</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">ubuntu:22.04</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">pw presubmit --step pragma_once --step clang_format --step cpp_checks</span>
</code></pre></div></div>
<p>Next, This ‘.woodpecker/device-tests.yaml’ is a Woodpecker CI pipeline for hardware-in-the-loop tests on an STM32H750 board.</p>

<p><em>Pipeline Triggers</em>
Routes to agents labeled hardware: stm32h750 (board attached). Triggers only on push events to the main branch.</p>

<p><em>Flash and Test Step</em>
Uses ubuntu:22.04 image. First, OpenOCD flashes build/firmware.elf to the board via stm32h750b-disco.cfg config (program, verify, reset, exit). Then, pw_target_runner_client executes build/test_binary.elf tests via Pigweed’s target runner server on localhost:8080.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .woodpecker/device-tests.yaml</span>
<span class="c1"># Routed to the agent with the board attached</span>

<span class="na">labels</span><span class="pi">:</span>
  <span class="na">hardware</span><span class="pi">:</span> <span class="s">stm32h750</span>

<span class="na">when</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">branch</span><span class="pi">:</span> <span class="s">main</span>
    <span class="na">event</span><span class="pi">:</span> <span class="s">push</span>

<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">flash-and-device-tests</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">ubuntu:22.04</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">openocd -f board/stm32h750b-disco.cfg -c "program build/firmware.elf verify reset exit"</span>
      <span class="pi">-</span> <span class="s">pw_target_runner_client -host localhost -port 8080 -binary build/test_binary.elf</span>
</code></pre></div></div>
<p>And lastly, This ‘.woodpecker/hil-nightly.yaml’ is for nightly Hardware-in-the-Loop (HIL) regression testing by Woodpecker CI pipeline on an STM32H750 board.</p>

<p><em>Pipeline Triggers</em>
Routes to agents labeled hardware: stm32h750 (board attached). Triggers on cron event scheduled as nightly (e.g., daily at midnight).</p>

<p><em>HIL Regression Step</em>
Uses ubuntu:22.04 image. Runs</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">python3</span> <span class="n">tools</span><span class="o">/</span><span class="n">hil</span><span class="o">/</span><span class="n">run_regression_suite</span><span class="p">.</span><span class="n">py</span> <span class="o">--</span><span class="n">port</span> <span class="o">/</span><span class="n">dev</span><span class="o">/</span><span class="n">ttyACM0</span> 
</code></pre></div></div>

<p>to execute a comprehensive Python-based test suite over the board’s serial port (ACM0, in Linux), verifying firmware stability overnight.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .woodpecker/hil-nightly.yaml</span>
<span class="c1"># Nightly regression on the hardware agent</span>

<span class="na">labels</span><span class="pi">:</span>
  <span class="na">hardware</span><span class="pi">:</span> <span class="s">stm32h750</span>

<span class="na">when</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">event</span><span class="pi">:</span> <span class="s">cron</span>
    <span class="na">cron</span><span class="pi">:</span> <span class="s">nightly</span>

<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">hil-regression</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">ubuntu:22.04</span>
    <span class="na">commands</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">python3 tools/hil/run_regression_suite.py --port /dev/ttyACM0</span>
</code></pre></div></div>

<h2 id="summary">Summary</h2>

<ul>
  <li>
    <p>The build and host-test workflow runs on every commit — no hardware needed.</p>
  </li>
  <li>
    <p>The device-test workflow triggers on merges to main, routed to the lab agent with the board attached.</p>
  </li>
  <li>
    <p>The HIL regression runs nightly.<sup id="fnref:5" role="doc-noteref"><a href="#fn:5" class="footnote" rel="footnote">9</a></sup></p>
  </li>
</ul>

<h3 id="why-not-github-actions-with-self-hosted-runners">Why Not GitHub Actions with Self-Hosted Runners?</h3>

<p>Woodpecker has three major advantages for the specific embedded use case:</p>
<ol>
  <li><strong>Air-gap friendly</strong>: It has no dependency on the cloud. In defense or medical devices, traffic to github.com is often prohibited. Woodpecker works fully offline.</li>
  <li><strong>Minimal overhead</strong>: The server uses a SQLite database and can run comfortably on a Raspberry Pi 5 with ~100MB of RAM.</li>
  <li><strong>Hardware declarations</strong>: The agent label mechanism is straightforward for routing hardware-gated workflows, though note that labels apply at the <em>workflow</em> level, not per-step. You’ll need separate workflow files for cloud-only and hardware-attached steps.</li>
</ol>

<hr />

<h2 id="where-host-tests-meet-hardware">Where Host Tests Meet Hardware</h2>

<p>The core payoff of this stack is narrowing the gap between what passes in simulation and what works on real silicon. It doesn’t eliminate that gap, hardware surprises are inherent to the domain but it makes the gap <em>visible and diagnosable</em> instead of something you discover at system integration.</p>

<p>Without this kind of setup, hardware surprises tend to surface during system integration which can take weeks,  escalating the cost of diagnosis.</p>

<hr />

<h2 id="resources">Resources</h2>

<ul>
  <li><a href="https://pigweed.dev">Pigweed documentation</a> — start with the Tour of Pigweed and the Sense showcase</li>
  <li><a href="https://opensource.googleblog.com/2024/08/introducing-pigweed-sdk.html">Pigweed SDK announcement (Google, Aug 2024)</a></li>
  <li><a href="https://github.com/jart/cosmopolitan">Cosmopolitan Libc</a> — cosmocc toolchain and APE format</li>
  <li><a href="https://woodpecker-ci.org">Woodpecker CI</a> — documentation and Docker Compose quick start</li>
  <li><a href="https://blog.golioth.io/automated-hardware-testing-using-pytest/">Golioth Firmware SDK HIL testing</a> — a detailed real-world HIL CI implementation worth studying</li>
</ul>

<h2 id="sources">Sources</h2>

<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:4" role="doc-endnote">
      <p><a href="https://www.ansys.com/simulation-topics/what-is-hardware-in-the-loop-testing">What is Hardware-in-the-Loop Testing?</a> <a href="#fnref:4" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:4:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p>
    </li>
    <li id="fn:8" role="doc-endnote">
      <p><a href="https://www.opal-rt.com/blog/a-guide-to-hardware-in-the-loop-testing/">A Guide to hardware-in-the-loop (HIL) testing in 2026 - OPAL-RT</a> <a href="#fnref:8" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:8:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p>
    </li>
    <li id="fn:1" role="doc-endnote">
      <p><a href="https://www.genuen.com/blog/why-hil-is-becoming-critical-for-medical-device-testing">Why HIL is Becoming Critical for Medical Device Testing</a> <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:3" role="doc-endnote">
      <p><a href="https://www.can-cia.org/fileadmin/cia/documents/publications/cnlm/december_2019/19-4_p30_hil_test_systems_in_the_automotive_industry_frank_heidemann_set.pdf">HIL test systems in the automotive industry</a> <a href="#fnref:3" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2" role="doc-endnote">
      <p><a href="https://www.vector.com/int/en/products/products-a-z/hardware/hil-test-systems/">HIL Test Systems</a> <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:9" role="doc-endnote">
      <p><a href="https://www.automotivetestingtechnologyinternational.com/industry-opinion/hardware-in-the-loop-key-challenges-and-considerations.html">Hardware-in-the-loop: key challenges and considerations</a> <a href="#fnref:9" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:9:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p>
    </li>
    <li id="fn:7" role="doc-endnote">
      <p><a href="https://mdipenta.github.io/files/msr2022-cps.pdf">Problems and Solutions in Applying Continuous Integration to Cyber-Physical Systems</a> <a href="#fnref:7" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:6" role="doc-endnote">
      <p><a href="https://testrigor.com/blog/what-is-hardware-in-loop-and-software-in-loop-testing/">What Is Hardware-In-Loop (HIL) and Software-In-Loop (SIL) Testing?</a> <a href="#fnref:6" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:5" role="doc-endnote">
      <p><a href="https://www.opal-rt.com/blog/building-a-regression-testing-pipeline-for-hil-in-electrification-teams/">Building a regression testing pipeline for HIL in electrification teams</a> <a href="#fnref:5" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="es" /><category term="c++" /><summary type="html"><![CDATA[A experimental setup on how to build stack using "Woodpecker CI, Cosmopolitan, and Google Pigweed" for small firmware teams to automate build-test-flash pipelines for their embedded systems]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/cpp-portable-pipeline.webp" /><media:content medium="image" url="/cover_images/posts/cpp-portable-pipeline.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">The Embedded Engineer’s Second Brain: PARA and CODE Systems</title><link href="/blog/productivity/2026-02-18-ui/" rel="alternate" type="text/html" title="The Embedded Engineer’s Second Brain: PARA and CODE Systems" /><published>2026-02-18T00:00:00+00:00</published><updated>2026-07-22T19:57:16+00:00</updated><id>/blog/productivity/ui</id><content type="html" xml:base="/blog/productivity/2026-02-18-ui/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#the-embedded-engineers-second-brain" id="markdown-toc-the-embedded-engineers-second-brain">The Embedded Engineer’s Second Brain</a>    <ul>
      <li><a href="#1-quick-primer-what-makes-hardware-notes-different" id="markdown-toc-1-quick-primer-what-makes-hardware-notes-different">1. Quick Primer: What Makes Hardware Notes Different?</a>        <ul>
          <li><a href="#the-information-overload-problem" id="markdown-toc-the-information-overload-problem">The Information Overload Problem</a></li>
        </ul>
      </li>
      <li><a href="#2-why-we-hoard-information" id="markdown-toc-2-why-we-hoard-information">2. Why We Hoard Information</a></li>
      <li><a href="#3-the-filing-cabinet-trap-organizing-by-topic-vs-action" id="markdown-toc-3-the-filing-cabinet-trap-organizing-by-topic-vs-action">3. The “Filing Cabinet” Trap: Organizing by Topic vs. Action</a></li>
      <li><a href="#4-the-code-method-your-information-pipeline" id="markdown-toc-4-the-code-method-your-information-pipeline">4. The CODE Method: Your Information Pipeline</a></li>
      <li><a href="#5-the-para-system-4-simple-buckets-for-everything" id="markdown-toc-5-the-para-system-4-simple-buckets-for-everything">5. The PARA System: 4 Simple Buckets for Everything</a>        <ul>
          <li><a href="#the-4-para-folders" id="markdown-toc-the-4-para-folders">The 4 PARA Folders</a></li>
          <li><a href="#simple-tagging-for-quick-searches" id="markdown-toc-simple-tagging-for-quick-searches">Simple Tagging for Quick Searches</a></li>
        </ul>
      </li>
      <li><a href="#6-progressive-summarization-highlighting-what-matters" id="markdown-toc-6-progressive-summarization-highlighting-what-matters">6. Progressive Summarization: Highlighting What Matters</a></li>
      <li><a href="#7-expressing-work-intermediate-packets-ips" id="markdown-toc-7-expressing-work-intermediate-packets-ips">7. Expressing Work: Intermediate Packets (IPs)</a></li>
      <li><a href="#8-real-world-case-study-one-device-four-notes" id="markdown-toc-8-real-world-case-study-one-device-four-notes">8. Real-World Case Study: One Device, Four Notes</a>        <ul>
          <li><a href="#note-1--datasheet-excerpt-power-down-modes" id="markdown-toc-note-1--datasheet-excerpt-power-down-modes">Note 1 — Datasheet Excerpt: Power-Down Modes</a></li>
          <li><a href="#note-2--datasheet-excerpt-led-current--timing" id="markdown-toc-note-2--datasheet-excerpt-led-current--timing">Note 2 — Datasheet Excerpt: LED Current &amp; Timing</a></li>
          <li><a href="#note-3--application-note-ambient-light-calibration" id="markdown-toc-note-3--application-note-ambient-light-calibration">Note 3 — Application Note: Ambient Light Calibration</a></li>
          <li><a href="#note-4--client-requirements" id="markdown-toc-note-4--client-requirements">Note 4 — Client Requirements</a></li>
          <li><a href="#the-synthesis-turning-4-notes-into-a-clear-decision" id="markdown-toc-the-synthesis-turning-4-notes-into-a-clear-decision">The Synthesis: Turning 4 Notes Into a Clear Decision</a></li>
        </ul>
      </li>
      <li><a href="#9-ready-to-use-note-templates" id="markdown-toc-9-ready-to-use-note-templates">9. Ready-to-Use Note Templates</a>        <ul>
          <li><a href="#template-1--datasheet-summary-single-device" id="markdown-toc-template-1--datasheet-summary-single-device">Template 1 — Datasheet Summary (Single Device)</a></li>
          <li><a href="#template-2--client-requirements-summary" id="markdown-toc-template-2--client-requirements-summary">Template 2 — Client Requirements Summary</a></li>
          <li><a href="#template-3--design-decision-note" id="markdown-toc-template-3--design-decision-note">Template 3 — Design Decision Note</a></li>
        </ul>
      </li>
      <li><a href="#10-summary-faq" id="markdown-toc-10-summary-faq">10. Summary FAQ</a></li>
    </ul>
  </li>
</ul>

<h1 id="the-embedded-engineers-second-brain">The Embedded Engineer’s Second Brain</h1>

<p><em>How to stop drowning in 500-page datasheets and start turning technical notes into shipping hardware</em></p>

<hr />

<h2 id="1-quick-primer-what-makes-hardware-notes-different">1. Quick Primer: What Makes Hardware Notes Different?</h2>

<p>If you are new to embedded systems or hardware engineering, welcome! Embedded engineering is the art of programming tiny computers (microcontrollers) that interact with the physical world through sensors and chips.</p>

<p>Before diving into note-taking methods, here is a quick plain-English guide to the terms used in this guide:</p>

<div class="tip">
  <p><strong>Beginner’s Cheat Sheet:</strong></p>
  <ul>
    <li><strong>Microcontroller (MCU):</strong> The primary “mini-computer brain” of a device (like an STM32 or ESP32).</li>
    <li><strong>Analog Front-End (AFE) / Sensor:</strong> A specialized chip that reads physical signals (like heart rate, temperature, or motion) and turns them into digital data.</li>
    <li><strong>Datasheet:</strong> A 500-page “owner’s manual” provided by chip manufacturers, packed with electrical diagrams, pin layouts, and timing charts.</li>
    <li><strong>Errata:</strong> A manufacturer’s official list of known hardware bugs and design quirks.</li>
    <li><strong>Registers:</strong> Tiny digital switches inside a microchip that control its settings.</li>
  </ul>
</div>

<h3 id="the-information-overload-problem">The Information Overload Problem</h3>

<p>When was the last time you felt overwhelmed by dozens of saved PDFs, open tabs, bookmarks, or code snippets?</p>

<p><img src="/cover_images/posts/lot-papers.webp" alt="" /></p>

<div class="think">
  <p>The average person consumes roughly <strong>34 gigabytes of information daily</strong>. For developers and hardware engineers, this flood includes vendor manuals, bug reports, toolchain updates, and forum threads. This creates <em>Information Exhaustion</em>—a constant worry that you will forget a critical detail right when project deadlines arrive.</p>
</div>

<p>Hardware engineers manage two distinct types of information:</p>

<ol>
  <li><strong>Volatile Reference Material:</strong> Datasheets, chip bug lists, pinout changes, and forum workarounds—information that changes with every new chip revision.</li>
  <li><strong>Core Engineering Knowledge:</strong> Software design patterns, debugging techniques, and hardware testing procedures—knowledge that builds up over your entire career.</li>
</ol>

<p>Without a structured system, both types get mixed into a messy digital pile. This is the <strong>Information Paradox</strong>: <em>more saved notes equal less clarity when you actually need to build something</em>.</p>

<hr />

<h2 id="2-why-we-hoard-information">2. Why We Hoard Information</h2>

<p>Information overload happens when we become <em>digital hoarders</em>—saving files we don’t have time to read out of a fear of missing out (FOMO). <strong>Saving a 500-page datasheet feels like progress, but it is often just an illusion.</strong></p>

<p><img src="/cover_images/general/yoda.webp" alt="" /></p>

<ul>
  <li><strong>The Illusion of Preparation:</strong> Saving 50 PDF manuals feels like having everything under control. In reality, it is procrastination disguised as research.</li>
  <li><strong>Fear of Losing Information:</strong> <em>What if I delete this forum link and need it next year?</em> This fear causes us to hoard hundreds of bookmarks we never look at again.</li>
  <li><strong>The Rediscovery Myth:</strong> Raw facts (like chip pin numbers) are easy to look up online anytime. What is truly valuable is your own summary of how those facts apply to your <em>current</em> project.</li>
</ul>

<hr />

<h2 id="3-the-filing-cabinet-trap-organizing-by-topic-vs-action">3. The “Filing Cabinet” Trap: Organizing by Topic vs. Action</h2>

<p>We often confuse saving information with understanding it. Ask yourself: how many saved bookmarks or PDFs do you actually open a second time?</p>

<p><img src="/cover_images/posts/lib-retrive.webp" alt="" /></p>

<p>When you organize notes by broad topics—such as creating a folder named <em>Microcontrollers</em> or <em>Programming</em>—finding specific answers becomes impossible as your notes grow. Notes end up in an <em>idea graveyard</em>, buried under generic tutorials.</p>

<p>The fundamental mistake is organizing notes by <strong>where they came from</strong> instead of <strong>where you will use them</strong>.</p>

<div class="important">
  <p><strong>The Golden Rule of Second Brain Notetaking:</strong><br />
Don’t ask <em>“What topic is this about?”</em> — ask <strong>“Which active project will this help me complete?”</strong></p>
</div>

<hr />

<h2 id="4-the-code-method-your-information-pipeline">4. The CODE Method: Your Information Pipeline</h2>

<p>In his book <em>Building a Second Brain</em>, Tiago Forte explains that our brains are built for coming up with ideas, not storing raw data.</p>

<p>The <strong>CODE method</strong> provides a simple 4-step pipeline to process information:</p>

<pre><code class="language-mermaid">flowchart LR
    C["1. CAPTURE\n───────────\nSave only what matters\nSnippet over whole PDF\nKey bug workarounds\nClient specs"]
    O["2. ORGANIZE\n───────────\nFile by active project\nUse simple tags\nKeep focus on action\nArchive when done"]
    D["3. DISTILL\n───────────\nSummarize in stages\nBold key numbers\nHighlight 'why'\nWrite 3-bullet summary"]
    E["4. EXPRESS\n───────────\nCreate reusable blocks\nDriver stubs &amp; checklists\nDesign decision notes\nBuild faster next time"]

    C --&gt; O --&gt; D --&gt; E
</code></pre>

<table>
  <thead>
    <tr>
      <th>CODE Step</th>
      <th>What It Means</th>
      <th>Simple Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>1. Capture</strong></td>
      <td>Save only what is useful right now</td>
      <td>Clip a 2-page register diagram or bug fix, not the whole 500-page manual.</td>
    </tr>
    <tr>
      <td><strong>2. Organize</strong></td>
      <td>File notes by active project first</td>
      <td>Store notes in your current project folder (<code class="language-plaintext highlighter-rouge">[P] Smart Watch</code>), not a generic reference bin.</td>
    </tr>
    <tr>
      <td><strong>3. Distill</strong></td>
      <td>Highlight key details progressively</td>
      <td>Bold critical numbers (like <strong>3.3 V max voltage</strong> or <strong>100 µs wakeup time</strong>) so you spot them in 2 seconds.</td>
    </tr>
    <tr>
      <td><strong>4. Express</strong></td>
      <td>Turn notes into real output</td>
      <td>Reuse note snippets to write code, design schematics, or send status reports to clients.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="5-the-para-system-4-simple-buckets-for-everything">5. The PARA System: 4 Simple Buckets for Everything</h2>

<p><strong>PARA</strong> is a top-down organizational system that sorts information into four clear buckets based on urgency:</p>

<pre><code class="language-mermaid">flowchart LR
    note(["New note captured"])

    note --&gt; q1{"Is this for an active\nproject with a deadline?"}
    q1 -- "Yes" --&gt; P["[P] Projects\n─────────\n[P] Smart Watch Bring-up\n[P] Debug Temperature Sensor\n[P] Battery Life Test"]
    q1 -- "No" --&gt; q2{"Is this an ongoing\nresponsibility?"}
    q2 -- "Yes" --&gt; A["[A] Areas\n─────────\n[A] Code Style Guide\n[A] Hardware Testing Setup\n[A] Client Communications"]
    q2 -- "No" --&gt; q3{"Is this general reference\nfor future interest?"}
    q3 -- "Yes" --&gt; R["[R] Resources\n─────────\n[R] Sensor Datasheets\n[R] Bluetooth Design Patterns\n[R] C++ Reference"]
    q3 -- "No" --&gt; X["[X] Archives\n─────────\n[X] Completed Projects\n[X] Old 2024 Client Files"]

    P -. "project completes" .-&gt; X
</code></pre>

<h3 id="the-4-para-folders">The 4 PARA Folders</h3>

<ol>
  <li><strong>[P] Projects:</strong> Work with a current goal and a deadline (e.g., <code class="language-plaintext highlighter-rouge">[P] Smart Watch Bring-up</code>).</li>
  <li><strong>[A] Areas:</strong> Ongoing responsibilities without a fixed end date (e.g., <code class="language-plaintext highlighter-rouge">[A] Code Style Guide</code>, <code class="language-plaintext highlighter-rouge">[A] Testing Procedures</code>).</li>
  <li><strong>[R] Resources:</strong> General topics or reference materials for future learning (e.g., <code class="language-plaintext highlighter-rouge">[R] Bluetooth Patterns</code>, <code class="language-plaintext highlighter-rouge">[R] Vendor Manuals</code>).</li>
  <li><strong>[X] Archives:</strong> Completed projects or inactive notes kept out of sight to stay organized (e.g., <code class="language-plaintext highlighter-rouge">[X] Completed 2024 Projects</code>).</li>
</ol>

<div class="warning">
  <p><strong>Rule of Thumb:</strong> If a note is for something you are working on <em>right now</em>, put it directly into your <code class="language-plaintext highlighter-rouge">[P]</code> Project folder!</p>
</div>

<h3 id="simple-tagging-for-quick-searches">Simple Tagging for Quick Searches</h3>

<p>Use short tags so you can find related notes across different project folders:</p>
<ul>
  <li><strong>By Vendor:</strong> <code class="language-plaintext highlighter-rouge">.Vendor_TI</code>, <code class="language-plaintext highlighter-rouge">.Vendor_ST</code>, <code class="language-plaintext highlighter-rouge">.Vendor_Espressif</code></li>
  <li><strong>By Type:</strong> <code class="language-plaintext highlighter-rouge">.Req_Hardware</code>, <code class="language-plaintext highlighter-rouge">.Req_Software</code></li>
  <li><strong>By Watch-out:</strong> <code class="language-plaintext highlighter-rouge">.EdgeCase</code> (for known chip bugs or quirks)</li>
  <li><strong>By Status:</strong> <code class="language-plaintext highlighter-rouge">.IntermediatePacket</code> (for reusable code snippets)</li>
</ul>

<hr />

<h2 id="6-progressive-summarization-highlighting-what-matters">6. Progressive Summarization: Highlighting What Matters</h2>

<p>Don’t waste time summarizing every note from start to finish. Distill notes <strong>only when you reopen them to do work</strong>.</p>

<p>Progressive Summarization uses 4 easy levels:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Level 1: Raw captured snippet (Diagram clip, forum fix, specification table)
 └── Level 2: Bold key numbers (Voltage limits, timing, power consumption)
      └── Level 3: Highlight design rationale ("Why" a design choice was made)
           └── Level 4: 3-bullet summary written in your own words at the top
</code></pre></div></div>

<ul>
  <li><strong>Level 1 — Raw Snippet:</strong> The text or table as clipped.</li>
  <li><strong>Level 2 — Bold Critical Specs:</strong> Bold values your future self needs to see instantly (e.g., <strong>max 3.3 V</strong>, <strong>wakeup latency 200 µs</strong>).</li>
  <li><strong>Level 3 — Highlight Rationale:</strong> Highlight passages explaining <em>why</em> a decision was made.</li>
  <li><strong>Level 4 — 3-Bullet Summary:</strong> Write a 3-sentence summary at the top of foundational notes so you get the context in 5 seconds.</li>
</ul>

<div class="note">
  <p><strong>Why Distill?</strong><br />
Distillation turns a 15-minute search through a messy PDF into a 5-second glance at bolded numbers.</p>
</div>

<hr />

<h2 id="7-expressing-work-intermediate-packets-ips">7. Expressing Work: Intermediate Packets (IPs)</h2>

<p>Instead of starting every new project from scratch, build a library of <strong>Intermediate Packets (IPs)</strong>—small, reusable chunks of work saved from past projects:</p>

<table>
  <thead>
    <tr>
      <th>Reusable Asset (IP)</th>
      <th>What It Is</th>
      <th>How It Helps You</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Code Templates</strong></td>
      <td>Basic driver setup for I2C or SPI sensors</td>
      <td>Start writing sensor code in 2 minutes instead of 2 hours.</td>
    </tr>
    <tr>
      <td><strong>Testing Checklists</strong></td>
      <td>Step-by-step hardware bring-up guide</td>
      <td>Never forget a critical safety test during prototype checks.</td>
    </tr>
    <tr>
      <td><strong>Architecture Diagrams</strong></td>
      <td>Simple block diagrams showing how parts connect</td>
      <td>Easily explain your design to teammates or clients.</td>
    </tr>
    <tr>
      <td><strong>Bug Post-Mortems</strong></td>
      <td>Notes on past bugs and how you fixed them</td>
      <td>Avoid making the exact same mistake twice.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="8-real-world-case-study-one-device-four-notes">8. Real-World Case Study: One Device, Four Notes</h2>

<p>To see how this works in practice, let’s look at a single real-world chip: the <strong>Texas Instruments AFE4404 Optical Heart-Rate Sensor</strong> (Datasheet ID: <code class="language-plaintext highlighter-rouge">SBAS689D</code>).</p>

<p>Imagine you are designing a low-power wearable fitness wristband powered by a small 180 mAh rechargeable battery. You need to decide how to configure the sensor so the battery lasts <strong>at least 72 hours</strong>.</p>

<p>Here are 4 short notes pulled from a Second Brain to make that decision:</p>

<hr />

<h3 id="note-1--datasheet-excerpt-power-down-modes">Note 1 — Datasheet Excerpt: Power-Down Modes</h3>
<ul>
  <li><strong>Source:</strong> <code class="language-plaintext highlighter-rouge">TI AFE4404 Datasheet (SBAS689D), Section 7.5</code></li>
  <li>
    <table>
      <tbody>
        <tr>
          <td><strong>Folder:</strong> <code class="language-plaintext highlighter-rouge">[R] Vendor: TI</code></td>
          <td><strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Vendor_TI</code></td>
        </tr>
      </tbody>
    </table>
  </li>
</ul>

<div class="info">
  <p><strong>Clipped Note (Level 2 Bolded):</strong><br />
The AFE4404 includes a <strong>Dynamic Power-Down (DPD)</strong> mode that turns off internal analog blocks between sensor measurements. In DPD mode, receiver supply current drops to <strong>300 µA typical</strong> (compared to <strong>620 µA</strong> in continuous always-on mode).</p>
</div>

<hr />

<h3 id="note-2--datasheet-excerpt-led-current--timing">Note 2 — Datasheet Excerpt: LED Current &amp; Timing</h3>
<ul>
  <li><strong>Source:</strong> <code class="language-plaintext highlighter-rouge">TI AFE4404 Datasheet (SBAS689D), Section 8.3.6.4 &amp; Table 9</code></li>
  <li>
    <table>
      <tbody>
        <tr>
          <td><strong>Folder:</strong> <code class="language-plaintext highlighter-rouge">[R] Vendor: TI</code></td>
          <td><strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Vendor_TI</code></td>
        </tr>
      </tbody>
    </table>
  </li>
</ul>

<div class="info">
  <p><strong>Clipped Note (Level 2 Bolded):</strong><br />
Programmable LED pulse current: 0 to 50 mA. Running LEDs at 50 mA peak with a 25% duty cycle draws <strong>12.5 mA average current</strong>. Reducing to a <strong>1% duty cycle (100 µs pulse duration at 25 Hz sample rate)</strong> drops average LED current to <strong>125 µA</strong>. The DPD wake-up setup margin $t_9$ requires <strong>$t_9 \ge 200\ \mu\text{s}$</strong> before the next measurement.</p>
</div>

<hr />

<h3 id="note-3--application-note-ambient-light-calibration">Note 3 — Application Note: Ambient Light Calibration</h3>
<ul>
  <li><strong>Source:</strong> <code class="language-plaintext highlighter-rouge">TI AFE4404 Application Guide, Section 8.3.3</code></li>
  <li>
    <table>
      <tbody>
        <tr>
          <td><strong>Folder:</strong> <code class="language-plaintext highlighter-rouge">[P] Wristband Project</code></td>
          <td><strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Vendor_TI</code>, <code class="language-plaintext highlighter-rouge">.EdgeCase</code></td>
        </tr>
      </tbody>
    </table>
  </li>
</ul>

<div class="bug">
  <p><strong>Clipped Note (Level 2 Bolded):</strong><br />
Sunlight and room lighting create DC optical noise. <strong>Workaround: Enable the internal Offset Cancellation DAC and use <code class="language-plaintext highlighter-rouge">PD_DISCONNECT</code> mode at startup to auto-calibrate ambient light levels before reading heart-rate data.</strong></p>
</div>

<hr />

<h3 id="note-4--client-requirements">Note 4 — Client Requirements</h3>
<ul>
  <li><strong>Source:</strong> <code class="language-plaintext highlighter-rouge">Client Kickoff Meeting Notes</code></li>
  <li>
    <table>
      <tbody>
        <tr>
          <td><strong>Folder:</strong> <code class="language-plaintext highlighter-rouge">[P] Wristband Project</code></td>
          <td><strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Req_Hardware</code></td>
        </tr>
      </tbody>
    </table>
  </li>
</ul>

<div class="important">
  <p><strong>Clipped Note (Level 2 Bolded):</strong><br />
Target battery life: <strong>72 hours continuous use</strong> on a 180 mAh LiPo battery. Heart-rate sampling rate: <strong>25 Hz minimum</strong>. Maximum allowable average system current budget: <strong>~2.1 mA</strong> (calculated as $180\text{ mAh} \div 72\text{ h} \times 0.85\text{ safety margin}$).</p>
</div>

<hr />

<h3 id="the-synthesis-turning-4-notes-into-a-clear-decision">The Synthesis: Turning 4 Notes Into a Clear Decision</h3>

<div class="success">
  <p><strong>What Combining These 4 Notes Tells Us:</strong></p>

  <ol>
    <li><strong>Clear Power Strategy:</strong> Running the sensor in continuous mode with a 25% LED duty cycle draws 12.5 mA—which would drain the battery in under 15 hours! By configuring a <strong>1% duty cycle (100 µs pulse at 25 Hz)</strong> and enabling <strong>Dynamic Power-Down mode</strong>, average LED current drops to <strong>125 µA</strong> and receiver current drops below <strong>200 µA</strong>. Total sensor draw stays well under <strong>0.5 mA</strong>, comfortably fitting within our 2.1 mA system budget.</li>
    <li><strong>Timing Fits Effortlessly:</strong> The 200 µs setup margin ($t_9$) needed for DPD mode easily fits inside the 40,000 µs (40 ms) window between 25 Hz heart-rate pulses.</li>
    <li><strong>Actionable Checklist:</strong> Before taking measurements, our firmware must run the Offset Cancellation DAC calibration to remove ambient sunlight interference.</li>
  </ol>
</div>

<hr />

<h2 id="9-ready-to-use-note-templates">9. Ready-to-Use Note Templates</h2>

<p>Copy these simple fill-in templates into your favorite note app (Obsidian, Notion, Evernote, Apple Notes, or OneNote):</p>

<hr />

<h3 id="template-1--datasheet-summary-single-device">Template 1 — Datasheet Summary (Single Device)</h3>

<p><strong>Location:</strong> <code class="language-plaintext highlighter-rouge">[R] Vendor: XX</code> (or <code class="language-plaintext highlighter-rouge">[P] Active Project</code>)<br />
<strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Vendor_XX</code></p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Device Name &amp; Part Number</strong></td>
      <td>e.g. TI AFE4404 Heart-Rate Sensor</td>
    </tr>
    <tr>
      <td><strong>Manufacturer</strong></td>
      <td>e.g. Texas Instruments</td>
    </tr>
    <tr>
      <td><strong>Link to Official PDF</strong></td>
      <td> </td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Guidance / Your Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>What does this chip do in 1 sentence?</strong></td>
      <td><em>e.g., Measures optical heart rate and pulse oximetry signals.</em></td>
    </tr>
    <tr>
      <td><strong>What are the key electrical limits?</strong></td>
      <td><em>e.g., Supply voltage: 2.0 V to 3.6 V. Max LED current: 50 mA.</em></td>
    </tr>
    <tr>
      <td><strong>What timing limits matter?</strong></td>
      <td><em>e.g., Requires 200 µs setup margin in DPD mode.</em></td>
    </tr>
    <tr>
      <td><strong>Any known bugs or quirks?</strong></td>
      <td><em>e.g., Needs Offset Cancellation DAC calibration for ambient light.</em></td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="template-2--client-requirements-summary">Template 2 — Client Requirements Summary</h3>

<p><strong>Location:</strong> <code class="language-plaintext highlighter-rouge">[P] Active Project</code><br />
<strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.Req_Hardware</code></p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Project Name</strong></td>
      <td> </td>
    </tr>
    <tr>
      <td><strong>Client / Stakeholder</strong></td>
      <td> </td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Guidance / Your Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>What main job must this device do?</strong></td>
      <td><em>e.g., Track wristband heart-rate during daily workouts.</em></td>
    </tr>
    <tr>
      <td><strong>What is the battery &amp; power target?</strong></td>
      <td><em>e.g., 180 mAh battery, 72-hour battery life = 2.1 mA max budget.</em></td>
    </tr>
    <tr>
      <td><strong>What are the key performance specs?</strong></td>
      <td><em>e.g., Minimum 25 Hz heart-rate sample rate.</em></td>
    </tr>
    <tr>
      <td><strong>Are there physical or environment constraints?</strong></td>
      <td><em>e.g., Splash-proof enclosure, operating range 0°C to 50°C.</em></td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="template-3--design-decision-note">Template 3 — Design Decision Note</h3>

<p><strong>Location:</strong> <code class="language-plaintext highlighter-rouge">[P] Active Project</code><br />
<strong>Tags:</strong> <code class="language-plaintext highlighter-rouge">.IntermediatePacket</code></p>

<table>
  <thead>
    <tr>
      <th>Field</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Decision Title</strong></td>
      <td>e.g. AFE4404 Sampling Mode (Dynamic Power-Down vs. Continuous)</td>
    </tr>
    <tr>
      <td><strong>Date &amp; Author</strong></td>
      <td> </td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Guidance / Your Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>What choice needed to be made?</strong></td>
      <td><em>Should we run the sensor continuously or use Dynamic Power-Down?</em></td>
    </tr>
    <tr>
      <td><strong>What option was chosen and why?</strong></td>
      <td><em>Chosen Dynamic Power-Down + 1% duty cycle. Cuts current from 12.5 mA to &lt;0.5 mA to hit battery target.</em></td>
    </tr>
    <tr>
      <td><strong>What software changes are required?</strong></td>
      <td><em>Enable DPD mode in register 23h and run startup DAC calibration.</em></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="10-summary-faq">10. Summary FAQ</h2>

<p><strong>“Do I need to be a senior engineer to use this system?”</strong><br />
Not at all! In fact, starting early with PARA and CODE prevents bad notetaking habits and makes learning new hardware much faster.</p>

<p><strong>“Do I need to distill every note I save?”</strong><br />
No. Only distill notes that you reopen to do actual project work.</p>

<p><strong>“What note-taking app should I use?”</strong><br />
Any app works—Obsidian, Notion, Evernote, OneNote, or Apple Notes. The method matters far more than the software.</p>

<hr />

<p><em>Remember: The goal of a Second Brain is not to collect perfect notes. It is to build a reliable personal system that helps you find the right answers fast so you can build great projects with confidence.</em></p>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="productivity" /><category term="notetaking" /><summary type="html"><![CDATA[More information does not equal more clarity. Here is a beginner-friendly guide to turning information overload into clear engineering decisions using Tiago Forte's CODE and PARA systems.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/notes.webp" /><media:content medium="image" url="/cover_images/posts/notes.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Portable and Secure C- A Practical Guide to MISRA C &amp;amp; CERT C</title><link href="/blog/industry/2026-02-16-portable-C/" rel="alternate" type="text/html" title="Portable and Secure C- A Practical Guide to MISRA C &amp;amp; CERT C" /><published>2026-02-16T00:00:00+00:00</published><updated>2026-07-22T14:18:12+00:00</updated><id>/blog/industry/portable-C</id><content type="html" xml:base="/blog/industry/2026-02-16-portable-C/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#why-portability-matters-and-why-its-harder-than-you-think" id="markdown-toc-why-portability-matters-and-why-its-harder-than-you-think">Why portability matters, and why it’s harder than you think</a></li>
  <li><a href="#the-great-myth-of-embedded-c-vs-c" id="markdown-toc-the-great-myth-of-embedded-c-vs-c">The great myth of “Embedded C vs C”</a>    <ul>
      <li><a href="#the-misconception" id="markdown-toc-the-misconception"><strong>The misconception</strong></a></li>
    </ul>
  </li>
  <li><a href="#who-sets-the-guidelines-and-by-what-authority" id="markdown-toc-who-sets-the-guidelines-and-by-what-authority">Who sets the guidelines, and by what authority</a>    <ul>
      <li><a href="#iso--iec-jtc1-sc22-wg14" id="markdown-toc-iso--iec-jtc1-sc22-wg14">ISO / IEC JTC1 SC22 WG14</a></li>
      <li><a href="#misra" id="markdown-toc-misra">MISRA</a></li>
      <li><a href="#cert--carnegie-mellon-sei" id="markdown-toc-cert--carnegie-mellon-sei">CERT / Carnegie Mellon SEI</a></li>
      <li><a href="#iec-autosar-do-178c" id="markdown-toc-iec-autosar-do-178c">IEC, AUTOSAR, DO-178C</a></li>
    </ul>
  </li>
  <li><a href="#misra-c-and-cert-c-why-two-standards-when-we-want-both" id="markdown-toc-misra-c-and-cert-c-why-two-standards-when-we-want-both">MISRA C and CERT C: why two standards when we want both?</a></li>
  <li><a href="#industry-values-and-testing-for-security-and-portability" id="markdown-toc-industry-values-and-testing-for-security-and-portability">Industry values and testing for security and portability</a>    <ul>
      <li><a href="#the-compliance-theatre-problem" id="markdown-toc-the-compliance-theatre-problem">The compliance theatre problem</a></li>
    </ul>
  </li>
  <li><a href="#testing-strategies-that-complement-static-analysis" id="markdown-toc-testing-strategies-that-complement-static-analysis">Testing strategies that complement static analysis</a></li>
  <li><a href="#failures-liabilities-and-the-price-of-non-compliance" id="markdown-toc-failures-liabilities-and-the-price-of-non-compliance">Failures, liabilities, and the price of non-compliance</a>    <ul>
      <li><a href="#ariane-5-flight-501-1996" id="markdown-toc-ariane-5-flight-501-1996">Ariane 5, Flight 501 (1996)</a></li>
      <li><a href="#therac-25-radiation-therapy-device-19851987" id="markdown-toc-therac-25-radiation-therapy-device-19851987">Therac-25 Radiation Therapy Device (1985–1987)</a></li>
      <li><a href="#boeing-737-max-mcas-20182019" id="markdown-toc-boeing-737-max-mcas-20182019">Boeing 737 MAX MCAS (2018–2019)</a></li>
      <li><a href="#the-regulatory-shift" id="markdown-toc-the-regulatory-shift">The regulatory shift</a></li>
    </ul>
  </li>
  <li><a href="#the-future-of-c-c23-new-iso-directions-and-the-long-game" id="markdown-toc-the-future-of-c-c23-new-iso-directions-and-the-long-game">The future of C: C23, new ISO directions, and the long game</a>    <ul>
      <li><a href="#nullptr-and-_bitintn" id="markdown-toc-nullptr-and-_bitintn"><code class="language-plaintext highlighter-rouge">nullptr</code> and <code class="language-plaintext highlighter-rouge">_BitInt(N)</code></a></li>
      <li><a href="#typeof-and-auto" id="markdown-toc-typeof-and-auto"><code class="language-plaintext highlighter-rouge">typeof</code> and <code class="language-plaintext highlighter-rouge">auto</code></a></li>
      <li><a href="#why-this-helps" id="markdown-toc-why-this-helps">Why this helps:</a></li>
      <li><a href="#standardised-attributes" id="markdown-toc-standardised-attributes">Standardised attributes</a></li>
      <li><a href="#checked-integer-arithmetic" id="markdown-toc-checked-integer-arithmetic">Checked integer arithmetic</a></li>
      <li><a href="#misra-c-and-c23-alignment" id="markdown-toc-misra-c-and-c23-alignment">MISRA C and C23 alignment</a></li>
    </ul>
  </li>
  <li><a href="#rust-as-a-reference-point" id="markdown-toc-rust-as-a-reference-point">Rust as a reference point</a>    <ul>
      <li><a href="#the-question-nobody-is-asking-loudly-enough" id="markdown-toc-the-question-nobody-is-asking-loudly-enough">The question nobody is asking loudly enough</a></li>
    </ul>
  </li>
</ul>

<h2 id="why-portability-matters-and-why-its-harder-than-you-think">Why portability matters, and why it’s harder than you think</h2>

<p>Ask a firmware engineer what portability means and you’ll likely hear something about target architectures: ARM Cortex-M versus RISC-V versus MIPS. Swap the compiler flags, rewrite the linker script, done. That’s migration, not portability.</p>

<p>Real portability is subtler and far more dangerous. It lives in the gaps between what the C standard defines, what your compiler assumes, and what your hardware silently does. It hides in a line like:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">65536</span><span class="p">;</span>
<span class="kt">short</span> <span class="n">s</span> <span class="o">=</span> <span class="p">(</span><span class="kt">short</span><span class="p">)</span><span class="n">x</span><span class="p">;</span>  <span class="cm">/* Implementation-defined. Could be 0. Could be -1. Could be anything. */</span>
</code></pre></div></div>

<p>On your 32-bit x86 development machine with GCC, that cast might produce a predictable result. On a target with a different ABI, a different <code class="language-plaintext highlighter-rouge">INT_MAX</code>, or a compiler that treats overflow as a licence to optimise away your entire conditional, the result is anything but.</p>

<blockquote>
  <p><em>“Portability is not a feature you bolt on at the end. It’s a constraint you design to from the start, or you pay for it with bugs that only manifest in the field.”</em></p>
</blockquote>

<div class="warning">

  <p>Portability matters because C is the language of constrained targets, and constrained targets are everywhere: automotive ECUs, industrial PLCs, medical implants, avionics, SCADA systems. The code you write today will outlive the compiler you used to build it. The board it runs on today may be replaced by something with a different word size tomorrow. Designing for portability isn’t an academic exercise. It’s professional hygiene.</p>

</div>

<hr />

<h2 id="the-great-myth-of-embedded-c-vs-c">The great myth of “Embedded C vs C”</h2>

<p>There is a persistent belief in firmware circles that “Embedded C” is a separate dialect: a hardened, stripped-down version of the language with special rules for registers, memory-mapped I/O, and interrupt handlers.</p>

<p>This is fiction. There is no Embedded C standard separate from C89, C99, C11, or C23.</p>

<blockquote>
  <p>There are only C implementations targeting constrained environments, and all of them are still governed by ISO/IEC 9899.</p>
</blockquote>

<div class="note">

  <h3 id="the-misconception"><strong>The misconception</strong></h3>

  <p>The myth likely originated from the fact that bare-metal code frequently uses genuinely non-portable constructs like the</p>

  <ul>
    <li>inline assembly (code enclosed with #asm … #endasm)</li>
    <li><code class="language-plaintext highlighter-rouge">volatile</code>-mapped peripherals</li>
    <li>
      <p>interrupt attribute macros</p>

      <p>which does feel “different” from desktop C. They are. But they’re non-portable not because they’re “embedded”; they’re non-portable because they’re <em>implementation extensions</em>. The C standard has always accounted for this through its notion of undefined and implementation-defined behaviour. Recognising the boundary between standard C and your vendor’s extensions is what separates professional firmware from code that breaks mysteriously when you switch toolchains.</p>
    </li>
  </ul>

</div>

<p>The practical consequence of the myth is that it discourages engineers from applying rigorous coding standards. MISRA C was written <em>specifically</em> for embedded and safety-critical C. Every rule in MISRA C 2023 has been crafted with resource-constrained, compiler-diverse, long-lived firmware in mind.</p>

<p>The myth also conflates two distinct problems. Platform-specific code the <code class="language-plaintext highlighter-rouge">__attribute__((interrupt))</code>, the DMA register layout, the linker section pragma belongs in a hardware abstraction layer (HAL), clearly bounded and isolated. walled off from everything else. But the features like:</p>

<ul>
  <li>The business logic</li>
  <li>The protocol stack</li>
  <li>The sensor driver</li>
</ul>

<p>that code should be portable, standard C.</p>

<p>Calling everything “Embedded C” and shrugging at portability issues is how you end up with 40,000-line monolithic HAL files that might take months to port to a new SoC generation.</p>

<hr />

<h2 id="who-sets-the-guidelines-and-by-what-authority">Who sets the guidelines, and by what authority</h2>

<p>The landscape of C coding standards is fragmented in a way that reflects the industries that depend on C most heavily. Understanding who writes the rules and why their authority matters is essential before you can meaningfully apply them.</p>

<h3 id="iso--iec-jtc1-sc22-wg14">ISO / IEC JTC1 SC22 WG14</h3>

<p>The C language itself is standardised by ISO Working Group 14. Their output - C89, C99, C11, C17, and now C23, is the normative foundation. Every compiler, every coding standard, every static analysis tool references this document. WG14 doesn’t write portability guidelines for embedded systems; they define what the language is.</p>

<h3 id="misra">MISRA</h3>

<p>The Motor Industry Software Reliability Association began as a UK automotive consortium in the early 1990s. Their first C guidelines (1998) were explicitly aimed at making C safer for vehicular embedded systems. MISRA C is now on its fourth major edition (2023) and has been adopted far beyond automotive: aerospace (DO-178C contexts), medical devices, railway (EN 50128), and industrial control. MISRA’s the standard is a purchased document, which shapes how it propagates through supply chains (usually mandated by OEMs on their Tier 1 and Tier 2 suppliers).</p>

<h3 id="cert--carnegie-mellon-sei">CERT / Carnegie Mellon SEI</h3>

<p>Rather than to satisfy automotive functional safety requirements,the CERT Coordination Center at Carnegie Mellon’s Software Engineering Institute took a different approach. Their CERT C Coding Standard (now in its third edition, aligned to C11) emerged from secure software research and is publicly available. CERT’s perspective is fundamentally security-first: the rules exist to eliminate classes of vulnerability like buffer overflows, integer wrapping exploits, race conditions.</p>

<h3 id="iec-autosar-do-178c">IEC, AUTOSAR, DO-178C</h3>

<p>Domain-specific standards layer on top of or reference MISRA and CERT. IEC 61508 (functional safety) mandates the use of appropriate coding standards; AUTOSAR’s C++ guidelines borrow heavily from MISRA philosophy; DO-178C for airborne software requires “coding standards” without specifying which — in practice, MISRA C is the industry answer.</p>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>Standard</th>
      <th>Focus</th>
      <th>Access</th>
      <th>Primary Domain</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ISO WG14</td>
      <td>ISO/IEC 9899</td>
      <td>Language definition</td>
      <td>Purchased</td>
      <td>Universal</td>
    </tr>
    <tr>
      <td>MISRA</td>
      <td>MISRA C 2023</td>
      <td>Safety + portability</td>
      <td>Purchased</td>
      <td>Automotive, safety-critical</td>
    </tr>
    <tr>
      <td>CERT SEI</td>
      <td>CERT C (C11)</td>
      <td>Security + correctness</td>
      <td>Free / online</td>
      <td>Systems, secure software</td>
    </tr>
    <tr>
      <td>IEC</td>
      <td>IEC 61508</td>
      <td>Functional safety process</td>
      <td>Purchased</td>
      <td>Industrial, all domains</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="misra-c-and-cert-c-why-two-standards-when-we-want-both">MISRA C and CERT C: why two standards when we want both?</h2>

<p>This is the question every team asks when they first encounter the landscape. Both standards exist to make C safer and more correct. Both prohibit dangerous constructs. Both reference the same underlying C standard. So why are there two, and which one should you use?</p>

<p>The answer reveals something important about the nature of “safe code” itself: the threat model changes depending on what you’re protecting against.</p>

<p><strong>MISRA C 2023</strong> <em>(Safety-by-design)</em></p>

<ul>
  <li>Rooted in functional safety - IEC 61508, ISO 26262</li>
  <li>Focuses on determinism, defined behaviour, and portability across toolchains</li>
  <li>Rule-based with mandatory, required, and advisory tiers</li>
  <li>Mandates use of <code class="language-plaintext highlighter-rouge">stdint.h</code> fixed-width types</li>
  <li>Prohibits dynamic memory allocation entirely (Rule 21.3)</li>
  <li>Restricts recursion (Rule 17.2) - stack depth must be bounded</li>
  <li>Designed for compliance auditing and tool certification</li>
  <li>Deviation process: formal documented process for justified departures</li>
</ul>

<p><strong>CERT C Coding Standard</strong> <em>(Security-by-default)</em></p>

<ul>
  <li>Rooted in secure software research - CVE patterns, CWE taxonomy (CWE - Common Vulnerabilities and Exposures)</li>
  <li>Focuses on eliminating exploitable vulnerability classes</li>
  <li>Recommendation-based with risk severity (L1-L3) ratings</li>
  <li>Prioritises input validation and boundary checking</li>
  <li>Addresses concurrency and signal safety (POSIX context)</li>
  <li>More permissive on dynamic memory, but with strict rules for its use</li>
  <li>Free to use, openly published, constantly updated</li>
  <li>Maps to CWE identifiers for traceability to vulnerability databases</li>
</ul>

<p>The key distinction: MISRA asks</p>

<p><strong>“will this code behave correctly on the target?”</strong></p>

<p>while CERT asks</p>

<p><strong>“can an adversary abuse this code?”</strong></p>

<p>These are related but not the same question. A MISRA-compliant pacemaker firmware is deterministic and portable, but if it has a buffer overread in a BLE command parser, CERT would flag it as exploitable.</p>

<p>A CERT-hardened connected gateway might handle all attack surfaces correctly but contain integer types that invoke implementation-defined behaviour on a 16-bit MCU, which MISRA would prohibit.</p>

<div class="tip">

  <p><strong>The pragmatic answer</strong></p>

  <p>Use both, layered.</p>

  <p>MISRA C as the portability and safety baseline, especially for any component that must pass a functional safety assessment.</p>

  <p>CERT C as the security review lens, especially for any component that handles external inputs:</p>
  <ul>
    <li>network packets</li>
    <li>file parsing</li>
    <li>command interfaces.</li>
  </ul>

  <p>Where the two overlap, you get duplicate safeguards for extra coverage. In case, where they diverge, document your risk decision explicitly.</p>

</div>

<p>Teams building under IEC 62443 (industrial security) or UNECE WP.29 (automotive cybersecurity regulation) increasingly need to demonstrate compliance with both families simultaneously. The tools have caught up: modern static analysers like</p>

<ul>
  <li>PC-lint Plus</li>
  <li>Polyspace</li>
  <li>Helix QAC</li>
  <li>CodeSonar</li>
</ul>

<p>support simultaneous MISRA and CERT rule sets with unified dashboards.</p>

<hr />

<h2 id="industry-values-and-testing-for-security-and-portability">Industry values and testing for security and portability</h2>

<p>Knowing the standards is necessary but not sufficient. The real question is how engineering organisations internalise them and how they verify compliance in a way that is meaningful rather than performative.</p>

<h3 id="the-compliance-theatre-problem">The compliance theatre problem</h3>

<div class="warning">

  <p>A deviation log is a formal record of every place where your code intentionally violates a rule from a coding standard (like MISRA C or CERT C), along with a justification for why that violation is acceptable.</p>

  <p>It’s not a workaround, it’s actually part of the official compliance process. If your deviation log is longer than your coding standard compliance summary, something has gone wrong. Deviations should be documented with specific justification, and reviewed</p>

</div>

<p>This pattern is very common in the Embedded Industry, a project adopts MISRA, runs a static analyser at the end of development, gets 1000s of violations, and proceeds to suppress them all with blanket deviation justifications before the audit. The code ships. The certificate is granted. But it doesn’t changes the risk profile.</p>

<h2 id="testing-strategies-that-complement-static-analysis">Testing strategies that complement static analysis</h2>

<p>Static analysis finds structural violations. It doesn’t find all runtime behaviour. A few approaches that work well alongside it:</p>

<p><strong>Mutation testing</strong> verifies that your test suite actually detects behavioural changes. A test that doesn’t kill a mutant isn’t testing anything. Tools like <code class="language-plaintext highlighter-rouge">mull</code> (LLVM-based) work with embedded cross-compilation pipelines.</p>

<p><strong>Sanitisers on host-side unit tests</strong> - ASAN, UBSAN, and MSAN on your portable logic layer catch undefined behaviour at runtime, before the hardware ever sees the code.</p>

<p>UBSAN is especially useful for the integer overflow and type aliasing issues that MISRA rules are designed to prevent.</p>

<p><strong>Fuzz testing command interfaces</strong> - any code that parses external input (AT commands, CAN frames, Modbus PDUs, BLE characteristics) should be fuzz-tested.</p>

<p>LibFuzzer and AFL++ can be integrated against your protocol layer running on a host simulator.</p>

<div class="important">

  <p>**Architecture First Approach **</p>

  <p>Traditional embedded V-model development defers security and portability testing to verification phases.</p>

  <p>By that point, architectural decisions that create portability problems are already baked in. Make type choices, integer promotion decisions, and platform assumptions part of the design review, not the code review.</p>

</div>

<hr />

<h2 id="failures-liabilities-and-the-price-of-non-compliance">Failures, liabilities, and the price of non-compliance</h2>

<p>The standards community presents MISRA and CERT as good professional practice. Industry regulators increasingly present them as legal instruments. Understanding both framings changes how seriously engineers and management take them.</p>

<h3 id="ariane-5-flight-501-1996">Ariane 5, Flight 501 (1996)</h3>

<p>The inertial reference system reused from Ariane 4 attempted to convert a 64-bit floating-point velocity value to a 16-bit signed integer.</p>

<p>The value was outside the representable range. The conversion triggered a hardware exception. The backup SRI failed identically. The rocket self-destructed 37 seconds after launch.</p>

<p><img src="/cover_images/posts/ARIANE-5.webp" alt="" /></p>

<p>Source - <a href="https://www.youtube.com/watch?v=wGeZEUjUKvc">https://www.youtube.com/watch?v=wGeZEUjUKvc</a></p>

<p><strong>Root cause:</strong> Undefined behaviour on integer conversion.</p>
<ul>
  <li>MISRA Rule 10.3 :- a value must be within the range of the target type, would have flagged this at static analysis time.</li>
  <li>Damage Cost: approximately $370M USD.</li>
</ul>

<p>&lt;/div&gt;</p>

<h3 id="therac-25-radiation-therapy-device-19851987">Therac-25 Radiation Therapy Device (1985–1987)</h3>

<p>Race conditions in the control software allowed a high-power beam to fire without the physical safety shield in place. At least six patients received massive radiation overdoses. Three died.</p>

<p><img src="/cover_images/posts/therapy.webp" alt="" /></p>
<div class="danger">
  <p>Source and Read Further - <a href="https://pvs-studio.com/en/blog/posts/0438/">Killer Bug. Therac-25: Quick-and-Dirty</a></p>

  <p><strong>Root cause:</strong> 
Shared mutable state accessed without synchronisation.</p>
  <ul>
    <li>CERT CON32-C (prevent data races when accessing bit-fields from multiple threads) directly addresses this class of vulnerability. No software coding standard was in use.</li>
  </ul>

</div>

<h3 id="boeing-737-max-mcas-20182019">Boeing 737 MAX MCAS (2018–2019)</h3>

<p>The Maneuvering Characteristics Augmentation System relied on input from a single angle-of-attack sensor. Insufficient validation of sensor inputs and a lack of defensive coding around single-point-of-failure data paths contributed to two crashes killing 346 people.</p>

<div class="danger">

  <p><strong>Root cause:</strong> Insufficient input validation and defensive programming. CERT input validation guidelines apply directly. Boeing paid over $2.5 billion in settlements and penalties. The FAA and EASA have since tightened software assurance requirements for aviation.</p>

</div>

<h3 id="the-regulatory-shift">The regulatory shift</h3>

<p>Since 2020, the regulatory environment for software-intensive embedded systems has shifted measurably. UNECE WP.29 requires automotive OEMs to demonstrate cybersecurity management processes, including coding standards. The EU Cyber Resilience Act (2024) extends software security requirements to all connected products sold in Europe. FDA guidance on medical device software explicitly references IEC 62443 and CERT-aligned practices.
Source - https://digital-strategy.ec.europa.eu/en/policies/cyber-resilience-act</p>

<p>In regulated markets, shipping without demonstrable adherence to appropriate coding standards is a compliance exposure. Engineering teams that treat MISRA and CERT as optional will increasingly find themselves in front of procurement questionnaires, regulatory auditors, and after incidents, liability lawyers who understand the standards as well as they do.</p>

<hr />

<h2 id="the-future-of-c-c23-new-iso-directions-and-the-long-game">The future of C: C23, new ISO directions, and the long game</h2>

<p>C is not standing still. C23 (ISO/IEC 9899:2024, formally published in October 2024) is the most significant revision in over a decade, and several of its additions directly address the portability and safety concerns that drove MISRA and CERT in the first place.</p>

<h3 id="nullptr-and-_bitintn"><code class="language-plaintext highlighter-rouge">nullptr</code> and <code class="language-plaintext highlighter-rouge">_BitInt(N)</code></h3>

<p>A proper null pointer constant eliminates the <code class="language-plaintext highlighter-rouge">NULL</code>/<code class="language-plaintext highlighter-rouge">0</code> ambiguity that has caused subtle bugs in type-generic code for decades. <code class="language-plaintext highlighter-rouge">_BitInt(N)</code> provides arbitrary-width integers — a direct answer to MISRA’s long-standing concern about implementation-defined integer sizes on 16-bit and 32-bit targets. You can now declare <code class="language-plaintext highlighter-rouge">_BitInt(12)</code> and get exactly 12 bits, portably.</p>

<h3 id="typeof-and-auto"><code class="language-plaintext highlighter-rouge">typeof</code> and <code class="language-plaintext highlighter-rouge">auto</code></h3>

<p>Type inference joins standard C. Used carefully, <code class="language-plaintext highlighter-rouge">typeof</code> can eliminate dangerous implicit conversions in macro-heavy embedded code, like the kind of macro gymnastics that previously forced engineers to choose between portability and readability. MISRA C may treat these cautiously in its next edition, but the direction is sound.</p>

<p>For example:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define MAX(a, b) ((a) &gt; (b) ? (a) : (b))
</span></code></pre></div></div>

<p>Looks harmless,</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
<span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>

<span class="n">printf</span><span class="p">(</span><span class="s">"%u</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">MAX</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">));</span>
</code></pre></div></div>
<p>but <code class="language-plaintext highlighter-rouge">a</code> gets implicitly converted to <code class="language-plaintext highlighter-rouge">unsigned</code> and comparision becomes unexpected.</p>

<p>Thats goes against MISRA</p>

<p>To protect against the implicit typedef conversion use <code class="language-plaintext highlighter-rouge">typedef</code>:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define MAX(a, b) ({            \
    typeof(a) _a = (a);         \
    typeof(b) _b = (b);         \
    _a &gt; _b ? _a : _b;          \
})
</span></code></pre></div></div>
<h3 id="why-this-helps">Why this helps:</h3>

<ul>
  <li>Each argument evaluated once</li>
  <li>Preserves original types</li>
  <li>Avoids unintended promotions
So if you run the program again :</li>
</ul>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
<span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>

<span class="n">printf</span><span class="p">(</span><span class="s">"%d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">MAX</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">));</span>
</code></pre></div></div>

<p>Behavior now is predictable based on actual types stored in <code class="language-plaintext highlighter-rouge">_a</code> and <code class="language-plaintext highlighter-rouge">_b</code></p>
<h3 id="standardised-attributes">Standardised attributes</h3>

<p><code class="language-plaintext highlighter-rouge">[[nodiscard]]</code>, <code class="language-plaintext highlighter-rouge">[[deprecated]]</code>, and <code class="language-plaintext highlighter-rouge">[[noreturn]]</code> are now standard C, not GCC extensions. This reduces reliance on <code class="language-plaintext highlighter-rouge">__attribute__</code> compiler-specific syntax — a portability win that MISRA has long advocated through its restrictions on implementation extensions. Your toolchain-agnostic attribute usage no longer requires a portability shim.</p>

<h3 id="checked-integer-arithmetic">Checked integer arithmetic</h3>

<p><code class="language-plaintext highlighter-rouge">ckd_add</code>, <code class="language-plaintext highlighter-rouge">ckd_sub</code>, <code class="language-plaintext highlighter-rouge">ckd_mul</code> provide a standard way to detect integer overflow without invoking undefined behaviour. This addresses the single most common source of both MISRA violations and CERT-catalogued CVEs. There is no longer any excuse for unchecked arithmetic on values whose range you cannot statically guarantee.</p>

<h3 id="misra-c-and-c23-alignment">MISRA C and C23 alignment</h3>

<p>MISRA C 2023 was released before C23 finalised. Expect MISRA C 2026 or a technical corrigendum to address C23 constructs, particularly <code class="language-plaintext highlighter-rouge">_BitInt</code>, checked arithmetic, and the new attribute syntax.</p>

<p>The direction is towards tighter integration between language-level safety features and the rule sets that enforce their correct use.</p>

<h2 id="rust-as-a-reference-point">Rust as a reference point</h2>

<p>Rust is the most hyped programming language of 2025-26, driven by its compile-time memory-safety guarantees via ownership and lifetimes. These features are accelerating C standards work on bounded pointers (Annex V concepts) and lifetime annotations for verifiable static constraints. Rather than wholesale Rust ports, C is evolving ownership as native idioms. NSA’s 2022 guidance on memory-safe languages has pushed the C community toward language changes beyond mere coding practices</p>

<p>Source - <a href="https://industrialcyber.co/secure-by-design/nsa-cisa-guidance-push-for-adoption-of-memory-safe-languages-in-software-development-to-boost-resilience/">NSA, CISA guidance push for adoption of memory safe languages in software development to boost resilience - Industrial Cyber</a></p>
<h3 id="the-question-nobody-is-asking-loudly-enough">The question nobody is asking loudly enough</h3>

<p>C has survived because nothing else reliably delivers deterministic performance, minimal runtime overhead, direct hardware access, and cross-compiler portability at scale. Rust is genuinely better at memory safety. It is not yet better at the full embedded development workflow , build system fragility, toolchain maturity for exotic targets (custom MCU’s), and the sheer mass of existing validated C codebases all slow adoption. That industry is changing, faster than most embedded teams realise.</p>

<blockquote>
  <p><em>“The future of safe C is not C that has learned to be Rust (Better memory safety guarantee). It’s C that has learned to be honest about its own undefined behaviour, and provides language-level tools to manage it.”</em></p>
</blockquote>

<p>MISRA and CERT are documenting, systematically, the discipline that makes C survivable. C23’s checked arithmetic functions and <code class="language-plaintext highlighter-rouge">_BitInt</code> are the standard committee’s acknowledgment that MISRA and CERT identified real problems, and the language needed to respond.</p>

<p>For working embedded engineers, the practical takeaway is simple:</p>

<blockquote>
  <p>learn both standards. Not to satisfy auditors, but because they encode hard-won knowledge about how C actually behaves in production on constrained hardware.</p>
</blockquote>

<p>The rules are not arbitrary. Every MISRA rule has a horror story behind it. Every CERT recommendation has a CVE attached.</p>

<p>Read the standards. Run the tools. And the next time someone tells you that portability doesn’t matter because “this is just embedded code”  tell them the Ariane 5 telemetry story.</p>

<hr />

<div class="info">

  <p><strong>Further reading</strong></p>

  <ul>
    <li><strong>MISRA C 2023</strong> — <a href="https://misra.org.uk">misra.org.uk</a></li>
    <li><strong>CERT C Coding Standard</strong> — <a href="https://wiki.sei.cmu.edu/confluence/display/c">wiki.sei.cmu.edu/confluence/display/c</a></li>
    <li><strong>C23 final draft (N3220)</strong> — <a href="https://www.open-std.org/jtc1/sc22/wg14/">open-std.org/jtc1/sc22/wg14</a></li>
    <li><strong>Static analysis tooling</strong> — PC-lint Plus, Helix QAC (Perforce), Polyspace (MathWorks), Cppcheck with MISRA addon</li>
  </ul>

</div>

<hr />]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="industry" /><category term="c" /><category term="security" /><summary type="html"><![CDATA[The C language has powered satellites, pacemakers, and aircraft flight computers for five decades. That longevity is not accidental, and neither is the discipline required to keep it safe.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/portable-c.webp" /><media:content medium="image" url="/cover_images/posts/portable-c.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Binary Search Tree in C</title><link href="/blog/coding/2026-02-01-c-bst/" rel="alternate" type="text/html" title="Binary Search Tree in C" /><published>2026-02-01T00:00:00+00:00</published><updated>2026-07-20T21:00:35+00:00</updated><id>/blog/coding/c-bst</id><content type="html" xml:base="/blog/coding/2026-02-01-c-bst/"><![CDATA[<p>A guide to BST operations in C with LeetCode problem mappings</p>

<ul id="markdown-toc">
  <li><a href="#1-what-is-a-binary-search-tree" id="markdown-toc-1-what-is-a-binary-search-tree">1. What is a Binary Search Tree?</a>    <ul>
      <li><a href="#bst-property" id="markdown-toc-bst-property">BST Property</a></li>
      <li><a href="#time-complexity" id="markdown-toc-time-complexity">Time Complexity</a></li>
    </ul>
  </li>
  <li><a href="#2-node-structure" id="markdown-toc-2-node-structure">2. Node Structure</a>    <ul>
      <li><a href="#memory-layout" id="markdown-toc-memory-layout">Memory Layout</a></li>
    </ul>
  </li>
  <li><a href="#3-core-bst-operations" id="markdown-toc-3-core-bst-operations">3. Core BST Operations</a>    <ul>
      <li><a href="#insertion" id="markdown-toc-insertion">Insertion</a>        <ul>
          <li><a href="#insertion-flow" id="markdown-toc-insertion-flow">Insertion Flow</a></li>
        </ul>
      </li>
      <li><a href="#leetcode-problems-using-insertion" id="markdown-toc-leetcode-problems-using-insertion">LeetCode Problems Using Insertion</a></li>
      <li><a href="#search" id="markdown-toc-search">Search</a>        <ul>
          <li><a href="#search-flow" id="markdown-toc-search-flow">Search Flow</a></li>
        </ul>
      </li>
      <li><a href="#leetcode-problems-using-search" id="markdown-toc-leetcode-problems-using-search">LeetCode Problems Using Search</a></li>
      <li><a href="#find-min--max" id="markdown-toc-find-min--max">Find Min / Max</a></li>
      <li><a href="#deletion" id="markdown-toc-deletion">Deletion</a>        <ul>
          <li><a href="#deletion-decision-flow" id="markdown-toc-deletion-decision-flow">Deletion Decision Flow</a></li>
        </ul>
      </li>
      <li><a href="#leetcode-problems-using-deletion" id="markdown-toc-leetcode-problems-using-deletion">LeetCode Problems Using Deletion</a></li>
    </ul>
  </li>
  <li><a href="#4-tree-traversals" id="markdown-toc-4-tree-traversals">4. Tree Traversals</a>    <ul>
      <li><a href="#traversal-overview" id="markdown-toc-traversal-overview">Traversal Overview</a></li>
      <li><a href="#traversal-order-comparison" id="markdown-toc-traversal-order-comparison">Traversal Order Comparison</a></li>
      <li><a href="#inorder-left--root--right" id="markdown-toc-inorder-left--root--right">Inorder (Left → Root → Right)</a></li>
      <li><a href="#preorder-root--left--right" id="markdown-toc-preorder-root--left--right">Preorder (Root → Left → Right)</a></li>
      <li><a href="#postorder-left--right--root" id="markdown-toc-postorder-left--right--root">Postorder (Left → Right → Root)</a></li>
      <li><a href="#level-order-bfs" id="markdown-toc-level-order-bfs">Level Order (BFS)</a>        <ul>
          <li><a href="#bfs-process" id="markdown-toc-bfs-process">BFS Process</a></li>
        </ul>
      </li>
      <li><a href="#leetcode-problems-using-traversals" id="markdown-toc-leetcode-problems-using-traversals">LeetCode Problems Using Traversals</a></li>
    </ul>
  </li>
  <li><a href="#5-bst-properties--utilities" id="markdown-toc-5-bst-properties--utilities">5. BST Properties &amp; Utilities</a>    <ul>
      <li><a href="#height-of-tree" id="markdown-toc-height-of-tree">Height of Tree</a></li>
      <li><a href="#count-nodes" id="markdown-toc-count-nodes">Count Nodes</a></li>
      <li><a href="#validate-bst" id="markdown-toc-validate-bst">Validate BST</a>        <ul>
          <li><a href="#validation-strategy" id="markdown-toc-validation-strategy">Validation Strategy</a></li>
        </ul>
      </li>
      <li><a href="#lowest-common-ancestor" id="markdown-toc-lowest-common-ancestor">Lowest Common Ancestor</a>        <ul>
          <li><a href="#lca-decision-flow" id="markdown-toc-lca-decision-flow">LCA Decision Flow</a></li>
        </ul>
      </li>
      <li><a href="#kth-smallest-element" id="markdown-toc-kth-smallest-element">Kth Smallest Element</a></li>
      <li><a href="#leetcode-problems-using-bst-properties" id="markdown-toc-leetcode-problems-using-bst-properties">LeetCode Problems Using BST Properties</a></li>
    </ul>
  </li>
  <li><a href="#6-leetcode-practice-problems" id="markdown-toc-6-leetcode-practice-problems">6. LeetCode Practice Problems</a>    <ul>
      <li><a href="#problem-1-convert-sorted-array-to-bst" id="markdown-toc-problem-1-convert-sorted-array-to-bst">Problem 1: Convert Sorted Array to BST</a></li>
      <li><a href="#problem-2-invert-binary-tree" id="markdown-toc-problem-2-invert-binary-tree">Problem 2: Invert Binary Tree</a></li>
      <li><a href="#problem-3-two-sum-iv-bst" id="markdown-toc-problem-3-two-sum-iv-bst">Problem 3: Two Sum IV (BST)</a></li>
      <li><a href="#problem-4-trim-a-bst" id="markdown-toc-problem-4-trim-a-bst">Problem 4: Trim a BST</a></li>
      <li><a href="#problem-5-minimum-absolute-difference" id="markdown-toc-problem-5-minimum-absolute-difference">Problem 5: Minimum Absolute Difference</a></li>
      <li><a href="#complete-leetcode-reference-table" id="markdown-toc-complete-leetcode-reference-table">Complete LeetCode Reference Table</a></li>
    </ul>
  </li>
  <li><a href="#7-full-working-code" id="markdown-toc-7-full-working-code">7. Full Working Code</a></li>
  <li><a href="#8-reference" id="markdown-toc-8-reference">8. Reference</a>    <ul>
      <li><a href="#operations-quick-reference" id="markdown-toc-operations-quick-reference">Operations Quick Reference</a></li>
    </ul>
  </li>
  <li><a href="#final-tips" id="markdown-toc-final-tips">Final Tips</a></li>
</ul>

<h2 id="1-what-is-a-binary-search-tree">1. What is a Binary Search Tree?</h2>

<h3 id="bst-property">BST Property</h3>

<pre><code class="language-mermaid">graph TD
    A((50)) --&gt; B((30))
    A --&gt; C((70))
    B --&gt; D((20))
    B --&gt; E((40))
    C --&gt; F((60))
    C --&gt; G((80))

    style A fill:#6c5ce7,color:#fff
    style B fill:#00b894,color:#fff
    style C fill:#e17055,color:#fff
    style D fill:#00b894,color:#fff
    style E fill:#00b894,color:#fff
    style F fill:#e17055,color:#fff
    style G fill:#e17055,color:#fff
</code></pre>

<p><em>Every node follows one rule: <span style="color:#00b894">left children are smaller</span> and <span style="color:#e17055">right children are larger</span>. For example, 30 and 20 sit left of 50 (both smaller), while 70, 60, 80 sit right (all larger).</em></p>

<p>A <strong>Binary Search Tree (BST)</strong> is a binary tree where for every node:</p>
<ul>
  <li>All values in the <strong>left subtree</strong> are <strong>less</strong> than the node’s value</li>
  <li>All values in the <strong>right subtree</strong> are <strong>greater</strong> than the node’s value</li>
  <li>Both left and right subtrees are also BSTs</li>
</ul>

<p><strong>Example BST:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        50
       /  \
      30    70
     / \   / \
    20  40 60  80
</code></pre></div></div>

<h3 id="time-complexity">Time Complexity</h3>

<table>
  <thead>
    <tr>
      <th>Operation</th>
      <th>Average</th>
      <th>Worst (skewed)</th>
      <th>Space</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Search</td>
      <td>O(log n)</td>
      <td>O(n)</td>
      <td>O(1) iterative</td>
    </tr>
    <tr>
      <td>Insert</td>
      <td>O(log n)</td>
      <td>O(n)</td>
      <td>O(1) iterative</td>
    </tr>
    <tr>
      <td>Delete</td>
      <td>O(log n)</td>
      <td>O(n)</td>
      <td>O(1) iterative</td>
    </tr>
    <tr>
      <td>Traversal</td>
      <td>O(n)</td>
      <td>O(n)</td>
      <td>O(n)</td>
    </tr>
  </tbody>
</table>

<p><em>This table shows BST operation complexities: average case is O(log n) for search, insert, and delete, but degrades to O(n) when the tree becomes skewed (like a linked list).</em></p>

<hr />

<h2 id="2-node-structure">2. Node Structure</h2>

<h3 id="memory-layout">Memory Layout</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> ┌──────────────────────────┐
 │       TreeNode           │
 ├──────────┬───────────────┤
 │ int val  │      50       │  ← data
 ├──────────┼───────────────┤
 │ left  ●──┼──→ [Node 30]  │  ← pointer to left child
 ├──────────┼───────────────┤
 │ right ●──┼──→ [Node 70]  │  ← pointer to right child
 └──────────┴───────────────┘
               │
        ┌──────┴──────┐
        ▼             ▼
   ┌─────────┐   ┌─────────┐
   │ val: 30 │   │ val: 70 │
   │ left: ∅ │   │ left: ∅ │
   │ right: ∅│   │ right: ∅│
   └─────────┘   └─────────┘
</code></pre></div></div>

<p><em>Each node is a C struct with three fields: <code class="language-plaintext highlighter-rouge">int val</code> stores the data, <code class="language-plaintext highlighter-rouge">left</code> and <code class="language-plaintext highlighter-rouge">right</code> are pointers to child nodes (or NULL if no child exists). Node 50 points left to 30 (smaller) and right to 70 (larger).</em></p>

<p><strong>Required headers:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;limits.h&gt;</span><span class="cp">
</span></code></pre></div></div>

<p><strong>Node definition and constructor:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">createNode</span><span class="p">(</span><span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">TreeNode</span><span class="p">));</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">val</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">node</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h2 id="3-core-bst-operations">3. Core BST Operations</h2>

<h3 id="insertion">Insertion</h3>

<h4 id="insertion-flow">Insertion Flow</h4>

<p><strong>Inserting 35 into this BST:</strong></p>

<pre><code class="language-mermaid">graph TD
    A((50)) -.-&gt;|"35 &lt; 50 → go left"| B((30))
    B -.-&gt;|"35 &gt; 30 → go right"| C((40))
    C -.-&gt;|"35 &lt; 40 → go left"| D["✅ NULL → insert 35 here"]

    style A fill:#636e72,color:#fff
    style B fill:#636e72,color:#fff
    style C fill:#636e72,color:#fff
    style D fill:#00b894,color:#fff
</code></pre>

<p><em>Starting at the root, each comparison tells us exactly which direction to go. We keep moving down until we hit a NULL spot — that’s where the new node goes.</em></p>

<p><strong>Iterative insertion:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">createNode</span><span class="p">(</span><span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">TreeNode</span><span class="p">));</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">val</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">node</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">insert</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">newNode</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="n">val</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="n">newNode</span><span class="p">;</span>

    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">curr</span> <span class="o">=</span> <span class="n">root</span><span class="p">;</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">parent</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>

    <span class="k">while</span> <span class="p">(</span><span class="n">curr</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">parent</span> <span class="o">=</span> <span class="n">curr</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">curr</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
            <span class="n">curr</span> <span class="o">=</span> <span class="n">curr</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
        <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">curr</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
            <span class="n">curr</span> <span class="o">=</span> <span class="n">curr</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>
        <span class="k">else</span>
            <span class="k">return</span> <span class="n">root</span><span class="p">;</span>  <span class="c1">// duplicate — no insertion</span>
    <span class="p">}</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">parent</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">parent</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">newNode</span><span class="p">;</span>
    <span class="k">else</span>
        <span class="n">parent</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">newNode</span><span class="p">;</span>

    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">root</span> <span class="o">=</span> <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">50</span><span class="p">);</span>
    <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">30</span><span class="p">);</span>
    <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">70</span><span class="p">);</span>
    <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">20</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Tree built with 4 nodes</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Iterative insertion avoids recursion overhead and is preferred in performance-critical embedded systems and low-level C code.</p>

<p><strong>Recursive insertion:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">insertRecursive</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="n">createNode</span><span class="p">(</span><span class="n">val</span><span class="p">);</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">insertRecursive</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>
    <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">insertRecursive</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>

    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>  <span class="c1">// duplicate — unchanged</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="leetcode-problems-using-insertion">LeetCode Problems Using Insertion</h3>

<table>
  <thead>
    <tr>
      <th>Problem</th>
      <th>What it asks (simplified)</th>
      <th>Customization</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://leetcode.com/problems/insert-into-a-binary-search-tree/">701. Insert into a BST</a></td>
      <td>Insert a value into a BST and return the root</td>
      <td>Use the recursive insertion above directly.</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="search">Search</h3>

<h4 id="search-flow">Search Flow</h4>

<pre><code class="language-mermaid">graph LR
    A["search(root, val)"] --&gt; B{root == NULL?}
    B --&gt;|Yes| C[Return NULL]
    B --&gt;|No| D{"val == root-&gt;val?"}
    D --&gt;|Yes| E[Found — return node]
    D --&gt;|No| F{"val &lt; root-&gt;val?"}
    F --&gt;|Yes| G[Search left]
    F --&gt;|No| H[Search right]
</code></pre>

<p><em>The search flow exploits BST ordering: at each node, the value tells us exactly which subtree to explore, eliminating half the tree at each step.</em></p>

<p><strong>Iterative search:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">search</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">!=</span> <span class="nb">NULL</span> <span class="o">&amp;&amp;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">!=</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
            <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
        <span class="k">else</span>
            <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>  <span class="c1">// NULL if not found</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Recursive search:</strong></p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">searchRecursive</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span> <span class="o">||</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">==</span> <span class="n">val</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">root</span><span class="p">;</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">searchRecursive</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>
    <span class="k">else</span>
        <span class="k">return</span> <span class="n">searchRecursive</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="leetcode-problems-using-search">LeetCode Problems Using Search</h3>

<table>
  <thead>
    <tr>
      <th>Problem</th>
      <th>What it asks (simplified)</th>
      <th>Customization</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://leetcode.com/problems/search-in-a-binary-search-tree/">700. Search in a BST</a></td>
      <td>Return the subtree rooted at the node with the target value</td>
      <td>Use either iterative or recursive search above.</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="find-min--max">Find Min / Max</h3>

<table>
  <thead>
    <tr>
      <th>Operation</th>
      <th>Logic</th>
      <th>Code</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Find Min</td>
      <td>Go left until <code class="language-plaintext highlighter-rouge">left == NULL</code></td>
      <td><code class="language-plaintext highlighter-rouge">while (root &amp;&amp; root-&gt;left) root = root-&gt;left;</code></td>
    </tr>
    <tr>
      <td>Find Max</td>
      <td>Go right until <code class="language-plaintext highlighter-rouge">right == NULL</code></td>
      <td><code class="language-plaintext highlighter-rouge">while (root &amp;&amp; root-&gt;right) root = root-&gt;right;</code></td>
    </tr>
  </tbody>
</table>

<p><em>The minimum value in a BST is always the leftmost node, and the maximum is always the rightmost. Both traverse a single path from root to leaf.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">findMin</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">&amp;&amp;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span>
        <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">findMax</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">&amp;&amp;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">)</span>
        <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h3 id="deletion">Deletion</h3>

<h4 id="deletion-decision-flow">Deletion Decision Flow</h4>

<pre><code class="language-mermaid">graph TD
    A["deleteNode(root, key)"] --&gt; B{Found node?}
    B --&gt;|No| C[Return NULL]
    B --&gt;|Yes| D{How many children?}
    D --&gt;|0 - Leaf| E[Free node, return NULL]
    D --&gt;|1 child| F[Replace with child]
    D --&gt;|2 children| G[Find inorder successor]
    G --&gt; H["Copy successor's value"]
    H --&gt; I["Delete successor from right subtree"]
</code></pre>

<p><em>Deletion has three cases: leaf nodes are simply removed, single-child nodes are replaced by their child, and two-child nodes use the inorder successor (smallest value in right subtree) as a replacement.</em></p>

<p><strong>Three cases explained:</strong></p>

<table>
  <thead>
    <tr>
      <th>Case</th>
      <th>Condition</th>
      <th>Action</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Leaf</td>
      <td>No children</td>
      <td>Free node, return NULL</td>
      <td>Delete 20 from tree</td>
    </tr>
    <tr>
      <td>One child</td>
      <td>Left or right is NULL</td>
      <td>Bypass node with its child</td>
      <td>Delete node with only right child</td>
    </tr>
    <tr>
      <td>Two children</td>
      <td>Both children exist</td>
      <td>Replace with inorder successor, delete successor</td>
      <td>Delete 30 (replace with 35)</td>
    </tr>
  </tbody>
</table>

<p><em>This table summarizes the three deletion cases: each requires different handling based on the node’s child count.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">deleteNode</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">key</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">key</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">key</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">key</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">key</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="c1">// Case 1 &amp; 2: No child or one child</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">TreeNode</span><span class="o">*</span> <span class="n">temp</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>
            <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
            <span class="k">return</span> <span class="n">temp</span><span class="p">;</span>
        <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">TreeNode</span><span class="o">*</span> <span class="n">temp</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
            <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
            <span class="k">return</span> <span class="n">temp</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="c1">// Case 3: Two children</span>
        <span class="n">TreeNode</span><span class="o">*</span> <span class="n">successor</span> <span class="o">=</span> <span class="n">findMin</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">successor</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">successor</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Deletion visualized (deleting 30):</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  Case 3: Two children          After replacing with
        50                      inorder successor (35)
       /  \                          50
      30    70         →            /  \
     / \   / \                    35    70
    20  40 60  80                /  \  / \
       /                        20  40 60 80
      35
</code></pre></div></div>

<h3 id="leetcode-problems-using-deletion">LeetCode Problems Using Deletion</h3>

<table>
  <thead>
    <tr>
      <th>Problem</th>
      <th>What it asks (simplified)</th>
      <th>Customization</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://leetcode.com/problems/delete-node-in-a-bst/">450. Delete Node in a BST</a></td>
      <td>Delete a value and return the updated root</td>
      <td>Use the three-case deletion above directly.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/trim-a-bst/">669. Trim a BST</a></td>
      <td>Remove all nodes outside range [low, high]</td>
      <td>If <code class="language-plaintext highlighter-rouge">val &lt; low</code>, return trimmed right subtree. If <code class="language-plaintext highlighter-rouge">val &gt; high</code>, return trimmed left subtree.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="4-tree-traversals">4. Tree Traversals</h2>

<h3 id="traversal-overview">Traversal Overview</h3>

<pre><code class="language-mermaid">graph TD
    A[Tree Traversals] --&gt; B[DFS - Depth First]
    A --&gt; C[BFS - Breadth First]
    B --&gt; D["Inorder: L → Root → R"]
    B --&gt; E["Preorder: Root → L → R"]
    B --&gt; F["Postorder: L → R → Root"]
    C --&gt; G["Level Order: Level by level"]
    D --&gt; H["Sorted output for BST"]
    E --&gt; I["Tree serialization"]
    F --&gt; J["Safe deletion"]
    G --&gt; K["Uses a Queue"]
</code></pre>

<p><em>Tree traversals are either depth-first (going deep before wide) or breadth-first (going wide before deep). Each DFS variant has specific use cases: inorder for sorting, preorder for copying, postorder for deletion.</em></p>

<h3 id="traversal-order-comparison">Traversal Order Comparison</h3>

<table>
  <thead>
    <tr>
      <th>Traversal</th>
      <th>Order</th>
      <th>BST Use Case</th>
      <th>Output for example tree</th>
      <th> </th>
      <th> </th>
      <th> </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Inorder</td>
      <td>Left → Root → Right</td>
      <td><strong>Sorted</strong> sequence</td>
      <td>20 30 35 40 50 60 70 80</td>
      <td> </td>
      <td> </td>
      <td> </td>
    </tr>
    <tr>
      <td>Preorder</td>
      <td>Root → Left → Right</td>
      <td>Serialize / copy tree</td>
      <td>50 30 20 40 35 70 60 80</td>
      <td> </td>
      <td> </td>
      <td> </td>
    </tr>
    <tr>
      <td>Postorder</td>
      <td>Left → Right → Root</td>
      <td>Safe free / delete</td>
      <td>20 35 40 30 60 80 70 50</td>
      <td> </td>
      <td> </td>
      <td> </td>
    </tr>
    <tr>
      <td>Level Order</td>
      <td>Level by level</td>
      <td>BFS analysis</td>
      <td>50</td>
      <td>30 70</td>
      <td>20 40 60 80</td>
      <td>35</td>
    </tr>
  </tbody>
</table>

<p><em>This table compares the four traversal orders, their visit sequence, practical use cases, and the output produced when applied to the example BST.</em></p>

<hr />

<h3 id="inorder-left--root--right">Inorder (Left → Root → Right)</h3>

<p>Produces a <strong>sorted sequence</strong> of values from a BST.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">createNode</span><span class="p">(</span><span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">TreeNode</span><span class="p">));</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">val</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">node</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">inorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="c1">// Store inorder result in array (useful for many LeetCode problems)</span>
<span class="kt">void</span> <span class="nf">inorderToArray</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">arr</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">idx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">inorderToArray</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">arr</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
    <span class="n">arr</span><span class="p">[(</span><span class="o">*</span><span class="n">idx</span><span class="p">)</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>
    <span class="n">inorderToArray</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">arr</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="mi">50</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="mi">30</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="mi">70</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="mi">20</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="mi">40</span><span class="p">);</span>

    <span class="n">printf</span><span class="p">(</span><span class="s">"Inorder: "</span><span class="p">);</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>  <span class="c1">// 20 30 40 50 70</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Inorder traversal is the most important BST traversal — it produces a sorted sequence, which is the basis for problems like Kth Smallest, Validate BST, and finding minimum differences.</p>

<hr />

<h3 id="preorder-root--left--right">Preorder (Root → Left → Right)</h3>

<p>Used to <strong>serialize or copy</strong> a tree. The root is visited first, making it ideal for reconstruction.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">preorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h3 id="postorder-left--right--root">Postorder (Left → Right → Root)</h3>

<p>Used to <strong>safely delete/free</strong> a tree — children are freed before the parent.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">postorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
<span class="p">}</span>

<span class="c1">// Free entire tree using postorder</span>
<span class="kt">void</span> <span class="nf">freeTree</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Always use postorder to free a tree in C. Freeing a parent before its children creates dangling pointers and memory leaks — a common mistake in manual memory management.</p>

<hr />

<h3 id="level-order-bfs">Level Order (BFS)</h3>

<h4 id="bfs-process">BFS Process</h4>

<pre><code class="language-mermaid">graph LR
    A[Enqueue root] --&gt; B[Dequeue node]
    B --&gt; C[Process node]
    C --&gt; D{Has left child?}
    D --&gt;|Yes| E[Enqueue left]
    D --&gt;|No| F{Has right child?}
    E --&gt; F
    F --&gt;|Yes| G[Enqueue right]
    F --&gt;|No| H{Queue empty?}
    G --&gt; H
    H --&gt;|No| B
    H --&gt;|Yes| I[Done]
</code></pre>

<p><em>Level order traversal uses a queue: enqueue the root, then repeatedly dequeue a node, process it, and enqueue its children. This visits all nodes level by level from top to bottom.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="c1">// Simple queue for BFS</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">data</span><span class="p">[</span><span class="mi">10000</span><span class="p">];</span>
    <span class="kt">int</span> <span class="n">front</span><span class="p">,</span> <span class="n">rear</span><span class="p">;</span>
<span class="p">}</span> <span class="n">Queue</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">initQueue</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">)</span> <span class="p">{</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">front</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">rear</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="p">}</span>
<span class="kt">void</span> <span class="nf">enqueue</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">,</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span><span class="p">)</span> <span class="p">{</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">data</span><span class="p">[</span><span class="n">q</span><span class="o">-&gt;</span><span class="n">rear</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">node</span><span class="p">;</span> <span class="p">}</span>
<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">dequeue</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">data</span><span class="p">[</span><span class="n">q</span><span class="o">-&gt;</span><span class="n">front</span><span class="o">++</span><span class="p">];</span> <span class="p">}</span>
<span class="n">bool</span> <span class="nf">isEmpty</span><span class="p">(</span><span class="n">Queue</span><span class="o">*</span> <span class="n">q</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">front</span> <span class="o">==</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">rear</span><span class="p">;</span> <span class="p">}</span>

<span class="kt">void</span> <span class="nf">levelOrder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>

    <span class="n">Queue</span> <span class="n">q</span><span class="p">;</span>
    <span class="n">initQueue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">);</span>
    <span class="n">enqueue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">,</span> <span class="n">root</span><span class="p">);</span>

    <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">isEmpty</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">))</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">size</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">rear</span> <span class="o">-</span> <span class="n">q</span><span class="p">.</span><span class="n">front</span><span class="p">;</span>  <span class="c1">// nodes at current level</span>

        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">size</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="n">dequeue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">);</span>
            <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>

            <span class="k">if</span> <span class="p">(</span><span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span>  <span class="n">enqueue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">,</span> <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
            <span class="k">if</span> <span class="p">(</span><span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">)</span> <span class="n">enqueue</span><span class="p">(</span><span class="o">&amp;</span><span class="n">q</span><span class="p">,</span> <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
        <span class="p">}</span>
        <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>  <span class="c1">// new line per level</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Level order traversal is essential for problems that require processing nodes level-by-level, such as finding the width of a tree, right-side view, or zigzag traversal.</p>

<h3 id="leetcode-problems-using-traversals">LeetCode Problems Using Traversals</h3>

<table>
  <thead>
    <tr>
      <th>Problem</th>
      <th>What it asks (simplified)</th>
      <th>Customization</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://leetcode.com/problems/binary-tree-inorder-traversal/">94. Binary Tree Inorder Traversal</a></td>
      <td>Return inorder sequence as array</td>
      <td>Use <code class="language-plaintext highlighter-rouge">inorderToArray</code> helper, return the array.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/binary-tree-preorder-traversal/">144. Binary Tree Preorder Traversal</a></td>
      <td>Return preorder sequence as array</td>
      <td>Visit root first, then left, then right into array.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/binary-tree-postorder-traversal/">145. Binary Tree Postorder Traversal</a></td>
      <td>Return postorder sequence as array</td>
      <td>Visit left, right, then root into array.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/binary-tree-level-order-traversal/">102. Binary Tree Level Order Traversal</a></td>
      <td>Return values grouped by level</td>
      <td>Use BFS with queue, track level size.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/binary-tree-level-order-traversal-ii/">107. Level Order Traversal II</a></td>
      <td>Level order bottom to top</td>
      <td>Same as 102, then reverse the result array.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="5-bst-properties--utilities">5. BST Properties &amp; Utilities</h2>

<h3 id="height-of-tree">Height of Tree</h3>

<pre><code class="language-mermaid">graph TD
    A["height(root)"] --&gt; B{root == NULL?}
    B --&gt;|Yes| C["Return -1"]
    B --&gt;|No| D["leftH = height(left)"]
    D --&gt; E["rightH = height(right)"]
    E --&gt; F["Return 1 + max(leftH, rightH)"]
</code></pre>

<p><em>Height is computed recursively: the height of an empty tree is -1, and each node adds 1 to the maximum height of its subtrees.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">height</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>  <span class="c1">// -1 for edges, 0 for nodes</span>

    <span class="kt">int</span> <span class="n">leftH</span>  <span class="o">=</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="kt">int</span> <span class="n">rightH</span> <span class="o">=</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>

    <span class="k">return</span> <span class="mi">1</span> <span class="o">+</span> <span class="p">(</span><span class="n">leftH</span> <span class="o">&gt;</span> <span class="n">rightH</span> <span class="o">?</span> <span class="n">leftH</span> <span class="o">:</span> <span class="n">rightH</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Example:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        50          height = 3
       /  \
      30    70      height = 2
     / \   / \
    20  40 60  80   height = 1
       /
      35            height = 0
</code></pre></div></div>

<hr />

<h3 id="count-nodes">Count Nodes</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">countNodes</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span> <span class="o">+</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h3 id="validate-bst">Validate BST</h3>

<h4 id="validation-strategy">Validation Strategy</h4>

<pre><code class="language-mermaid">graph TD
    A["isValidBST(root, min, max)"] --&gt; B{root == NULL?}
    B --&gt;|Yes| C["Return true"]
    B --&gt;|No| D{"val &lt;= min OR val &gt;= max?"}
    D --&gt;|Yes| E["Return false — violates BST"]
    D --&gt;|No| F["Check left with (min, root-&gt;val)"]
    F --&gt; G["Check right with (root-&gt;val, max)"]
    G --&gt; H["Return left AND right"]
</code></pre>

<p><em>Validation uses a min/max range that narrows at each level: left children must be less than the parent, right children must be greater. This catches the common mistake of only checking immediate parent.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;limits.h&gt;</span><span class="cp">
</span>
<span class="n">bool</span> <span class="nf">isValidBSTHelper</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">long</span> <span class="n">min</span><span class="p">,</span> <span class="kt">long</span> <span class="n">max</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&lt;=</span> <span class="n">min</span> <span class="o">||</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&gt;=</span> <span class="n">max</span><span class="p">)</span>
        <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>

    <span class="k">return</span> <span class="n">isValidBSTHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">min</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span> <span class="o">&amp;&amp;</span>
           <span class="n">isValidBSTHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">,</span> <span class="n">max</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">bool</span> <span class="nf">isValidBST</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">isValidBSTHelper</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">LONG_MIN</span><span class="p">,</span> <span class="n">LONG_MAX</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Why just checking parent is wrong:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        5
       / \
      1    6      ← Looks valid at each parent
          / \
         3   7    ← 3 &lt; 5 — violates BST property!
</code></pre></div></div>

<p>The range-based approach catches this: node 3 would need to be in range (5, 6), but 3 &lt; 5, so it fails.</p>

<hr />

<h3 id="lowest-common-ancestor">Lowest Common Ancestor</h3>

<h4 id="lca-decision-flow">LCA Decision Flow</h4>

<pre><code class="language-mermaid">graph TD
    A["LCA(root, p, q)"] --&gt; B{"p &lt; root AND q &lt; root?"}
    B --&gt;|Yes| C["Both in left → go left"]
    B --&gt;|No| D{"p &gt; root AND q &gt; root?"}
    D --&gt;|Yes| E["Both in right → go right"]
    D --&gt;|No| F["Split point = LCA!"]
</code></pre>

<p><em>In a BST, the LCA is the first node where the two target values diverge into different subtrees. This makes it O(log n) instead of the O(n) general tree approach.</em></p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">lowestCommonAncestor</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">p</span><span class="p">,</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">q</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">p</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&amp;&amp;</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
            <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>       <span class="c1">// both in left subtree</span>
        <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">p</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&amp;&amp;</span> <span class="n">q</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
            <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>      <span class="c1">// both in right subtree</span>
        <span class="k">else</span>
            <span class="k">return</span> <span class="n">root</span><span class="p">;</span>             <span class="c1">// split point = LCA</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<hr />

<h3 id="kth-smallest-element">Kth Smallest Element</h3>

<p>Use <strong>inorder traversal</strong> (sorted order) and count down to k.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">kthSmallestHelper</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">k</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">count</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">result</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span> <span class="o">||</span> <span class="o">*</span><span class="n">count</span> <span class="o">&gt;=</span> <span class="n">k</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>

    <span class="n">kthSmallestHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">count</span><span class="p">,</span> <span class="n">result</span><span class="p">);</span>

    <span class="p">(</span><span class="o">*</span><span class="n">count</span><span class="p">)</span><span class="o">++</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">*</span><span class="n">count</span> <span class="o">==</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
        <span class="o">*</span><span class="n">result</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>
        <span class="k">return</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="n">kthSmallestHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">count</span><span class="p">,</span> <span class="n">result</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">kthSmallest</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">count</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">result</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
    <span class="n">kthSmallestHelper</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">count</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">result</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Example (k=3):</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Inorder: 20, 30, [35], 40, 50, 60, 70, 80
                   ↑
              3rd smallest = 35
</code></pre></div></div>

<h3 id="leetcode-problems-using-bst-properties">LeetCode Problems Using BST Properties</h3>

<table>
  <thead>
    <tr>
      <th>Problem</th>
      <th>What it asks (simplified)</th>
      <th>Customization</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><a href="https://leetcode.com/problems/validate-binary-search-tree/">98. Validate BST</a></td>
      <td>Check if tree is a valid BST</td>
      <td>Use min/max range validation. Do NOT just check parent — check global range.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/maximum-depth-of-binary-tree/">104. Maximum Depth</a></td>
      <td>Return height of the tree</td>
      <td>Recursively compute <code class="language-plaintext highlighter-rouge">1 + max(left, right)</code>. Base case: NULL returns 0 (or -1 for edge count).</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/balanced-binary-tree/">110. Balanced Binary Tree</a></td>
      <td>Check if height difference ≤ 1 at every node</td>
      <td>Modify height function to return -1 if unbalanced, propagate up.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/count-complete-tree-nodes/">222. Count Complete Tree Nodes</a></td>
      <td>Count nodes in complete tree</td>
      <td>Simple recursion <code class="language-plaintext highlighter-rouge">1 + count(left) + count(right)</code>. Optimized: compare left/right heights.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/kth-smallest-element-in-a-bst/">230. Kth Smallest</a></td>
      <td>Find kth smallest value in BST</td>
      <td>Inorder traversal with counter. Stop early when count reaches k.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/">235. LCA of BST</a></td>
      <td>Find lowest common ancestor</td>
      <td>Exploit BST ordering: follow split point where p and q diverge.</td>
    </tr>
    <tr>
      <td><a href="https://leetcode.com/problems/minimum-absolute-difference-in-bst/">530. Min Absolute Difference</a></td>
      <td>Find minimum difference between any two nodes</td>
      <td>Inorder traversal, track previous value, compute min diff at each step.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="6-leetcode-practice-problems">6. LeetCode Practice Problems</h2>

<h3 id="problem-1-convert-sorted-array-to-bst">Problem 1: Convert Sorted Array to BST</h3>

<p>Build a height-balanced BST from a sorted array by always picking the middle element as root.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">createNode</span><span class="p">(</span><span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">TreeNode</span><span class="p">));</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">val</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">node</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">sortedArrayToBST</span><span class="p">(</span><span class="kt">int</span><span class="o">*</span> <span class="n">nums</span><span class="p">,</span> <span class="kt">int</span> <span class="n">left</span><span class="p">,</span> <span class="kt">int</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">left</span> <span class="o">&gt;</span> <span class="n">right</span><span class="p">)</span> <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>

    <span class="kt">int</span> <span class="n">mid</span> <span class="o">=</span> <span class="n">left</span> <span class="o">+</span> <span class="p">(</span><span class="n">right</span> <span class="o">-</span> <span class="n">left</span><span class="p">)</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="n">nums</span><span class="p">[</span><span class="n">mid</span><span class="p">]);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span>  <span class="o">=</span> <span class="n">sortedArrayToBST</span><span class="p">(</span><span class="n">nums</span><span class="p">,</span> <span class="n">left</span><span class="p">,</span> <span class="n">mid</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">sortedArrayToBST</span><span class="p">(</span><span class="n">nums</span><span class="p">,</span> <span class="n">mid</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">right</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">inorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">nums</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="o">-</span><span class="mi">10</span><span class="p">,</span> <span class="o">-</span><span class="mi">3</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">9</span><span class="p">};</span>
    <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span> <span class="o">=</span> <span class="n">sortedArrayToBST</span><span class="p">(</span><span class="n">nums</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">n</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Inorder: "</span><span class="p">);</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>  <span class="c1">// -10 -3 0 5 9</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Process:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Array: [-10, -3, 0, 5, 9]
                ↓
Mid = 0 (index 2)         →  Root = 0
Left half:  [-10, -3]     →  Left subtree
Right half: [5, 9]        →  Right subtree

Result:
        0
       / \
     -3    9
     /    /
   -10   5
</code></pre></div></div>

<p>This technique guarantees a balanced tree (height difference ≤ 1). It’s a divide-and-conquer approach used in database index construction and balanced tree initialization.</p>

<hr />

<h3 id="problem-2-invert-binary-tree">Problem 2: Invert Binary Tree</h3>

<p>Swap left and right children at every node recursively.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">invertTree</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>

    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">temp</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span>  <span class="o">=</span> <span class="n">invertTree</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">invertTree</span><span class="p">(</span><span class="n">temp</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Process:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before:         After:
    4              4
   / \            / \
  2    7   →    7    2
 / \  / \      / \  / \
1   3 6   9   9   6 3   1
</code></pre></div></div>

<p>Tree inversion is a classic recursion problem famously associated with Homebrew creator Max Howell’s Google interview. It tests understanding of tree structure manipulation and recursive thinking.</p>

<hr />

<h3 id="problem-3-two-sum-iv-bst">Problem 3: Two Sum IV (BST)</h3>

<p>Flatten BST to sorted array via inorder, then use two pointers.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="kt">void</span> <span class="nf">inorderToArray</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">arr</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">idx</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">inorderToArray</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">arr</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
    <span class="n">arr</span><span class="p">[(</span><span class="o">*</span><span class="n">idx</span><span class="p">)</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>
    <span class="n">inorderToArray</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">arr</span><span class="p">,</span> <span class="n">idx</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">bool</span> <span class="nf">findTarget</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">k</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">arr</span><span class="p">[</span><span class="mi">10001</span><span class="p">];</span>
    <span class="kt">int</span> <span class="n">idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">inorderToArray</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">arr</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">idx</span><span class="p">);</span>

    <span class="kt">int</span> <span class="n">lo</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="n">hi</span> <span class="o">=</span> <span class="n">idx</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">lo</span> <span class="o">&lt;</span> <span class="n">hi</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">sum</span> <span class="o">=</span> <span class="n">arr</span><span class="p">[</span><span class="n">lo</span><span class="p">]</span> <span class="o">+</span> <span class="n">arr</span><span class="p">[</span><span class="n">hi</span><span class="p">];</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">sum</span> <span class="o">==</span> <span class="n">k</span><span class="p">)</span> <span class="k">return</span> <span class="nb">true</span><span class="p">;</span>
        <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">sum</span> <span class="o">&lt;</span> <span class="n">k</span><span class="p">)</span> <span class="n">lo</span><span class="o">++</span><span class="p">;</span>
        <span class="k">else</span> <span class="n">hi</span><span class="o">--</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Example (k=9):</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Inorder: [2, 3, 4, 6, 7]
          ↑            ↑     2+7=9 ✓ Found!
         lo           hi
</code></pre></div></div>

<p>This combines BST inorder traversal with the classic two-pointer technique. The sorted property of inorder output makes two-pointer search optimal at O(n) time and O(n) space.</p>

<hr />

<h3 id="problem-4-trim-a-bst">Problem 4: Trim a BST</h3>

<p>Remove all nodes outside the range [low, high].</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TreeNode</span><span class="o">*</span> <span class="nf">trimBST</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">low</span><span class="p">,</span> <span class="kt">int</span> <span class="n">high</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>

    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">low</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">trimBST</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">low</span><span class="p">,</span> <span class="n">high</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">high</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">trimBST</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">low</span><span class="p">,</span> <span class="n">high</span><span class="p">);</span>

    <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span>  <span class="o">=</span> <span class="n">trimBST</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">low</span><span class="p">,</span> <span class="n">high</span><span class="p">);</span>
    <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">trimBST</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">low</span><span class="p">,</span> <span class="n">high</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Process (trim [1, 3]):</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before:         After:
    3              3
   / \            /
  0    4   →    2
   \           /
    2         1
   /
  1
</code></pre></div></div>

<p>Trimming uses BST ordering: if a node is below the range, all its left children are too, so we only keep the right subtree (and vice versa). This makes it efficient — no need to check every node twice.</p>

<hr />

<h3 id="problem-5-minimum-absolute-difference">Problem 5: Minimum Absolute Difference</h3>

<p>Find the minimum difference between any two node values in a BST.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;limits.h&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">minDiffHelper</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">prev</span><span class="p">,</span> <span class="kt">int</span><span class="o">*</span> <span class="n">minDiff</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>

    <span class="n">minDiffHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">prev</span><span class="p">,</span> <span class="n">minDiff</span><span class="p">);</span>

    <span class="k">if</span> <span class="p">(</span><span class="o">*</span><span class="n">prev</span> <span class="o">!=</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">diff</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">-</span> <span class="o">*</span><span class="n">prev</span><span class="p">;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">diff</span> <span class="o">&lt;</span> <span class="o">*</span><span class="n">minDiff</span><span class="p">)</span> <span class="o">*</span><span class="n">minDiff</span> <span class="o">=</span> <span class="n">diff</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="o">*</span><span class="n">prev</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>

    <span class="n">minDiffHelper</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">prev</span><span class="p">,</span> <span class="n">minDiff</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">getMinimumDifference</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">prev</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">minDiff</span> <span class="o">=</span> <span class="n">INT_MAX</span><span class="p">;</span>
    <span class="n">minDiffHelper</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">prev</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">minDiff</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">minDiff</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Example:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Inorder: 1, 2, 3, 4, 6
Diffs:     1  1  1  2
Min diff = 1
</code></pre></div></div>

<p>Since inorder traversal of a BST is sorted, the minimum difference must be between consecutive elements. This avoids the O(n²) brute-force approach.</p>

<h3 id="complete-leetcode-reference-table">Complete LeetCode Reference Table</h3>

<table>
  <thead>
    <tr>
      <th>#</th>
      <th>Problem</th>
      <th>Core Operation</th>
      <th>Difficulty</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>94</td>
      <td><a href="https://leetcode.com/problems/binary-tree-inorder-traversal/">Inorder Traversal</a></td>
      <td>Inorder traversal</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>98</td>
      <td><a href="https://leetcode.com/problems/validate-binary-search-tree/">Validate BST</a></td>
      <td>Range validation</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>100</td>
      <td><a href="https://leetcode.com/problems/same-tree/">Same Tree</a></td>
      <td>Recursive comparison</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>101</td>
      <td><a href="https://leetcode.com/problems/symmetric-tree/">Symmetric Tree</a></td>
      <td>Mirror check</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>102</td>
      <td><a href="https://leetcode.com/problems/binary-tree-level-order-traversal/">Level Order Traversal</a></td>
      <td>BFS / Queue</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>104</td>
      <td><a href="https://leetcode.com/problems/maximum-depth-of-binary-tree/">Maximum Depth</a></td>
      <td>Height recursion</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>108</td>
      <td><a href="https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/">Sorted Array to BST</a></td>
      <td>Build balanced BST</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>110</td>
      <td><a href="https://leetcode.com/problems/balanced-binary-tree/">Balanced Binary Tree</a></td>
      <td>Height + balance check</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>144</td>
      <td><a href="https://leetcode.com/problems/binary-tree-preorder-traversal/">Preorder Traversal</a></td>
      <td>Preorder traversal</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>145</td>
      <td><a href="https://leetcode.com/problems/binary-tree-postorder-traversal/">Postorder Traversal</a></td>
      <td>Postorder traversal</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>222</td>
      <td><a href="https://leetcode.com/problems/count-complete-tree-nodes/">Count Complete Tree Nodes</a></td>
      <td>Count nodes</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>226</td>
      <td><a href="https://leetcode.com/problems/invert-binary-tree/">Invert Binary Tree</a></td>
      <td>Swap children</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>230</td>
      <td><a href="https://leetcode.com/problems/kth-smallest-element-in-a-bst/">Kth Smallest in BST</a></td>
      <td>Inorder + counter</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>235</td>
      <td><a href="https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/">LCA of BST</a></td>
      <td>BST split point</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>236</td>
      <td><a href="https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/">LCA of Binary Tree</a></td>
      <td>General DFS</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>450</td>
      <td><a href="https://leetcode.com/problems/delete-node-in-a-bst/">Delete Node in BST</a></td>
      <td>Deletion with successor</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>530</td>
      <td><a href="https://leetcode.com/problems/minimum-absolute-difference-in-bst/">Min Absolute Difference</a></td>
      <td>Inorder + diff</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>653</td>
      <td><a href="https://leetcode.com/problems/two-sum-iv-input-is-a-bst/">Two Sum IV - BST</a></td>
      <td>Inorder + two pointers</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>669</td>
      <td><a href="https://leetcode.com/problems/trim-a-bst/">Trim a BST</a></td>
      <td>Recursive trim</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>700</td>
      <td><a href="https://leetcode.com/problems/search-in-a-binary-search-tree/">Search in BST</a></td>
      <td>BST search</td>
      <td>Easy</td>
    </tr>
    <tr>
      <td>701</td>
      <td><a href="https://leetcode.com/problems/insert-into-a-binary-search-tree/">Insert into BST</a></td>
      <td>BST insertion</td>
      <td>Medium</td>
    </tr>
    <tr>
      <td>783</td>
      <td><a href="https://leetcode.com/problems/minimum-distance-between-bst-nodes/">Min Distance Between BST Nodes</a></td>
      <td>Inorder + diff</td>
      <td>Easy</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="7-full-working-code">7. Full Working Code</h2>

<p>A complete program demonstrating all operations:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdbool.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;limits.h&gt;</span><span class="cp">
</span>
<span class="k">typedef</span> <span class="k">struct</span> <span class="n">TreeNode</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="n">val</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">left</span><span class="p">;</span>
    <span class="k">struct</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">right</span><span class="p">;</span>
<span class="p">}</span> <span class="n">TreeNode</span><span class="p">;</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">createNode</span><span class="p">(</span><span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">TreeNode</span><span class="p">));</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">val</span>   <span class="o">=</span> <span class="n">val</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">left</span>  <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="n">node</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">node</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">insert</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">root</span> <span class="o">==</span> <span class="nb">NULL</span><span class="p">)</span> <span class="k">return</span> <span class="n">createNode</span><span class="p">(</span><span class="n">val</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>
    <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">findMin</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">&amp;&amp;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span> <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">deleteNode</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">key</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">key</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">,</span> <span class="n">key</span><span class="p">);</span>
    <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">key</span> <span class="o">&gt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">key</span><span class="p">);</span>
    <span class="k">else</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span>  <span class="p">{</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">t</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span> <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span> <span class="k">return</span> <span class="n">t</span><span class="p">;</span> <span class="p">}</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">)</span> <span class="p">{</span> <span class="n">TreeNode</span><span class="o">*</span> <span class="n">t</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>  <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span> <span class="k">return</span> <span class="n">t</span><span class="p">;</span> <span class="p">}</span>
        <span class="n">TreeNode</span><span class="o">*</span> <span class="n">s</span> <span class="o">=</span> <span class="n">findMin</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">=</span> <span class="n">s</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">;</span>
        <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">,</span> <span class="n">s</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">TreeNode</span><span class="o">*</span> <span class="nf">search</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="kt">int</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="n">root</span> <span class="o">&amp;&amp;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span> <span class="o">!=</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">val</span> <span class="o">&lt;</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">)</span> <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">;</span>
        <span class="k">else</span> <span class="n">root</span> <span class="o">=</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">root</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">inorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">preorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
    <span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">postorder</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%d "</span><span class="p">,</span> <span class="n">root</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">height</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">l</span> <span class="o">=</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="kt">int</span> <span class="n">r</span> <span class="o">=</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">1</span> <span class="o">+</span> <span class="p">(</span><span class="n">l</span> <span class="o">&gt;</span> <span class="n">r</span> <span class="o">?</span> <span class="n">l</span> <span class="o">:</span> <span class="n">r</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">countNodes</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span> <span class="o">+</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">freeTree</span><span class="p">(</span><span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">root</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
    <span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">);</span>
    <span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">);</span>
    <span class="n">free</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">root</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">values</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">50</span><span class="p">,</span> <span class="mi">30</span><span class="p">,</span> <span class="mi">70</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">40</span><span class="p">,</span> <span class="mi">60</span><span class="p">,</span> <span class="mi">80</span><span class="p">,</span> <span class="mi">35</span><span class="p">};</span>
    <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">values</span><span class="p">)</span> <span class="o">/</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">values</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span>

    <span class="c1">// Build BST</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span>
        <span class="n">root</span> <span class="o">=</span> <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">values</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span>

    <span class="c1">// Traversals</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Inorder:   "</span><span class="p">);</span> <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>   <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Preorder:  "</span><span class="p">);</span> <span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>   <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Postorder: "</span><span class="p">);</span> <span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>  <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="c1">// Properties</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Height:    %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="p">));</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Count:     %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="p">));</span>

    <span class="c1">// Search</span>
    <span class="n">TreeNode</span><span class="o">*</span> <span class="n">found</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">40</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Search 40: %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">found</span> <span class="o">?</span> <span class="s">"Found"</span> <span class="o">:</span> <span class="s">"Not found"</span><span class="p">);</span>

    <span class="c1">// Delete</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">Deleting 30...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">root</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="mi">30</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Inorder:   "</span><span class="p">);</span> <span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span> <span class="n">printf</span><span class="p">(</span><span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="c1">// Min / Max</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Min value: %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">findMin</span><span class="p">(</span><span class="n">root</span><span class="p">)</span><span class="o">-&gt;</span><span class="n">val</span><span class="p">);</span>

    <span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Output:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Inorder:   20 30 35 40 50 60 70 80
Preorder:  50 30 20 40 35 70 60 80
Postorder: 20 35 40 30 60 80 70 50
Height:    3
Count:     8
Search 40: Found

Deleting 30...
Inorder:   20 35 40 50 60 70 80
Min value: 20
</code></pre></div></div>

<hr />

<h2 id="8-reference">8. Reference</h2>

<h3 id="operations-quick-reference">Operations Quick Reference</h3>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Create</span>
<span class="n">TreeNode</span><span class="o">*</span> <span class="n">node</span> <span class="o">=</span> <span class="n">createNode</span><span class="p">(</span><span class="n">val</span><span class="p">);</span>

<span class="c1">// Insert</span>
<span class="n">root</span> <span class="o">=</span> <span class="n">insert</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>

<span class="c1">// Search</span>
<span class="n">TreeNode</span><span class="o">*</span> <span class="n">found</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">val</span><span class="p">);</span>

<span class="c1">// Delete</span>
<span class="n">root</span> <span class="o">=</span> <span class="n">deleteNode</span><span class="p">(</span><span class="n">root</span><span class="p">,</span> <span class="n">key</span><span class="p">);</span>

<span class="c1">// Min / Max</span>
<span class="n">TreeNode</span><span class="o">*</span> <span class="n">min</span> <span class="o">=</span> <span class="n">findMin</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
<span class="n">TreeNode</span><span class="o">*</span> <span class="n">max</span> <span class="o">=</span> <span class="n">findMax</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>

<span class="c1">// Traversals</span>
<span class="n">inorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>    <span class="c1">// Sorted output</span>
<span class="n">preorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>   <span class="c1">// Serialize tree</span>
<span class="n">postorder</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>  <span class="c1">// Safe deletion</span>

<span class="c1">// Properties</span>
<span class="kt">int</span> <span class="n">h</span> <span class="o">=</span> <span class="n">height</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
<span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="n">countNodes</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
<span class="n">bool</span> <span class="n">valid</span> <span class="o">=</span> <span class="n">isValidBST</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>

<span class="c1">// Cleanup</span>
<span class="n">freeTree</span><span class="p">(</span><span class="n">root</span><span class="p">);</span>
</code></pre></div></div>

<hr />

<h2 id="final-tips">Final Tips</h2>

<p><strong>Do:</strong></p>
<ul>
  <li>Use inorder traversal for sorted-order problems</li>
  <li>Use min/max range for BST validation (not just parent check)</li>
  <li>Free trees with postorder traversal</li>
  <li>Consider iterative versions for production C code</li>
  <li>Handle NULL pointers at every function entry</li>
</ul>

<p><strong>Don’t:</strong></p>
<ul>
  <li>Forget to <code class="language-plaintext highlighter-rouge">free()</code> deleted nodes — memory leaks are silent bugs</li>
  <li>Only check immediate parent when validating BST</li>
  <li>Use recursion on extremely deep trees (stack overflow risk)</li>
  <li>Ignore the skewed tree worst case — use AVL/Red-Black for guarantees</li>
  <li>Assume BST operations are always O(log n) — they’re O(n) when skewed</li>
</ul>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="Coding" /><category term="c" /><summary type="html"><![CDATA[A complete guide to Solve the Binary Search Trees(BST) Problems in C & popular LeetCode BST problems]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/posts/c-algo.webp" /><media:content medium="image" url="/cover_images/posts/c-algo.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">std::pmr in Embedded C++ - Predictable Memory Without the Heap</title><link href="/blog/cppdev/2025-12-26-containers-std-vs-pmr/" rel="alternate" type="text/html" title="std::pmr in Embedded C++ - Predictable Memory Without the Heap" /><published>2025-12-26T00:00:00+00:00</published><updated>2026-07-22T14:18:12+00:00</updated><id>/blog/cppdev/containers-std-vs-pmr</id><content type="html" xml:base="/blog/cppdev/2025-12-26-containers-std-vs-pmr/"><![CDATA[<ul id="markdown-toc">
  <li><a href="#stdpmr-in-embedded-c---predictable-memory-without-the-heap" id="markdown-toc-stdpmr-in-embedded-c---predictable-memory-without-the-heap">std::pmr in Embedded C++ - Predictable Memory Without the Heap</a>    <ul>
      <li><a href="#what-is-the-stdpmr-introduced-in-c17" id="markdown-toc-what-is-the-stdpmr-introduced-in-c17">What is the std::pmr introduced in C++17</a>        <ul>
          <li><a href="#core-idea" id="markdown-toc-core-idea">Core Idea</a></li>
          <li><a href="#main-components" id="markdown-toc-main-components">Main Components</a></li>
          <li><a href="#important-predefined-memory-resources" id="markdown-toc-important-predefined-memory-resources">Important Predefined Memory Resources</a></li>
          <li><a href="#pmr-enabled-containers" id="markdown-toc-pmr-enabled-containers">pmr-enabled Containers</a></li>
        </ul>
      </li>
      <li><a href="#stdpmr-is-about-control--predictibility" id="markdown-toc-stdpmr-is-about-control--predictibility">std::pmr is about Control &amp; Predictibility</a></li>
      <li><a href="#quick-embedded-background-for-non-embedded-readers" id="markdown-toc-quick-embedded-background-for-non-embedded-readers">Quick embedded background (for non-embedded readers)</a></li>
      <li><a href="#why-embedded-engineers-care" id="markdown-toc-why-embedded-engineers-care">Why embedded engineers care</a></li>
      <li><a href="#what-pmr-actually-changes" id="markdown-toc-what-pmr-actually-changes">What PMR actually changes</a></li>
      <li><a href="#where-pmr-fits-best" id="markdown-toc-where-pmr-fits-best">Where PMR fits best</a>        <ul>
          <li><a href="#1-request-scoped-processing" id="markdown-toc-1-request-scoped-processing">1. Request-scoped processing</a></li>
          <li><a href="#2-bounded-buffers-in-real-time-code" id="markdown-toc-2-bounded-buffers-in-real-time-code">2. Bounded buffers in real-time code</a></li>
          <li><a href="#3-systems-that-need-runtime-allocator-choice" id="markdown-toc-3-systems-that-need-runtime-allocator-choice">3. Systems that need runtime allocator choice</a></li>
        </ul>
      </li>
      <li><a href="#what-pmr-does-not-solve" id="markdown-toc-what-pmr-does-not-solve">What PMR does not solve</a></li>
      <li><a href="#the-embedded-mental-model" id="markdown-toc-the-embedded-mental-model">The embedded mental model</a></li>
      <li><a href="#a-practical-buffer-sizing-strategy" id="markdown-toc-a-practical-buffer-sizing-strategy">A practical buffer sizing strategy</a></li>
      <li><a href="#how-to-think-about-performance" id="markdown-toc-how-to-think-about-performance">How to think about performance</a></li>
      <li><a href="#the-failure-mode-that-matters-most" id="markdown-toc-the-failure-mode-that-matters-most">The failure mode that matters most</a></li>
      <li><a href="#a-stricter-option-remove-the-heap-from-the-path-entirely" id="markdown-toc-a-stricter-option-remove-the-heap-from-the-path-entirely">A stricter option: remove the heap from the path entirely</a></li>
      <li><a href="#pmr-versus-other-embedded-options" id="markdown-toc-pmr-versus-other-embedded-options">PMR versus other embedded options</a></li>
      <li><a href="#a-simple-decision-tree" id="markdown-toc-a-simple-decision-tree">A simple decision tree</a></li>
      <li><a href="#why-i-used-a-custom-benchmark-harness-instead-of-google-benchmark" id="markdown-toc-why-i-used-a-custom-benchmark-harness-instead-of-google-benchmark">Why I used a custom benchmark harness instead of Google Benchmark</a></li>
      <li><a href="#the-benchmark-plots-and-what-they-indicate" id="markdown-toc-the-benchmark-plots-and-what-they-indicate">The Benchmark plots and what they indicate</a>        <ul>
          <li><a href="#vector-workload" id="markdown-toc-vector-workload">Vector workload</a></li>
          <li><a href="#string-workload" id="markdown-toc-string-workload">String workload</a></li>
          <li><a href="#map-workload" id="markdown-toc-map-workload">Map workload</a></li>
          <li><a href="#mixed-realistic-workload" id="markdown-toc-mixed-realistic-workload">Mixed realistic workload</a></li>
        </ul>
      </li>
      <li><a href="#when-i-would-recommend-stdpmr" id="markdown-toc-when-i-would-recommend-stdpmr">When I would recommend STD::PMR</a></li>
      <li><a href="#when-i-would-not" id="markdown-toc-when-i-would-not">When I would not</a></li>
      <li><a href="#bottom-line" id="markdown-toc-bottom-line">Bottom line</a></li>
      <li><a href="#references-and-code" id="markdown-toc-references-and-code">References and code</a></li>
    </ul>
  </li>
</ul>

<h1 id="stdpmr-in-embedded-c---predictable-memory-without-the-heap">std::pmr in Embedded C++ - Predictable Memory Without the Heap</h1>

<h2 id="what-is-the-stdpmr-introduced-in-c17">What is the std::pmr introduced in C++17</h2>

<p><strong>std::pmr</strong> (Polymorphic Memory Resources) is a major feature introduced in <strong>C++17</strong> that makes custom memory allocation much more convenient and flexible, especially when working with STL containers.</p>

<h3 id="core-idea">Core Idea</h3>

<p>Traditional C++ allocators are <strong>templated</strong> (e.g., std::vector&lt;T, MyAllocator<T>&gt;). This means the allocator type becomes part of the container type, leading to code bloat and compatibility issues between different allocator types.</T></p>

<p>std::pmr solves this by introducing <strong>type-erased</strong>, <strong>polymorphic</strong> memory resources. The allocator behavior is determined at runtime via a virtual interface instead of at compile time.</p>

<h3 id="main-components">Main Components</h3>

<table>
  <thead>
    <tr>
      <th>Component</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>std::pmr::memory_resource</strong></td>
      <td>Abstract base class (the core of the system). Defines allocate(), deallocate(), and is_equal().</td>
    </tr>
    <tr>
      <td>**std::pmr::polymorphic_allocator<T>**</T></td>
      <td>A standard allocator that forwards requests to a memory_resource*. This is what containers actually use.</td>
    </tr>
    <tr>
      <td><strong>Predefined resources</strong></td>
      <td>Ready-to-use memory resources provided by the standard library.</td>
    </tr>
  </tbody>
</table>

<h3 id="important-predefined-memory-resources">Important Predefined Memory Resources</h3>

<ul>
  <li><strong>std::pmr::new_delete_resource()</strong> — Default resource (uses ::operator new/delete).</li>
  <li><strong>std::pmr::null_memory_resource()</strong> — Throws std::bad_alloc on any allocation (useful for testing).</li>
  <li><strong>std::pmr::monotonic_buffer_resource</strong> — Very fast bump allocator. Allocations just move a pointer forward. Great for temporary arenas.</li>
  <li><strong>std::pmr::synchronized_pool_resource</strong> — Thread-safe pool allocator (good for many small allocations).</li>
  <li><strong>std::pmr::unsynchronized_pool_resource</strong> — Faster version when thread safety isn’t needed.</li>
  <li><strong>std::pmr::tracked_memory_resource</strong> (since C++20) — Helps with debugging memory usage.</li>
</ul>

<h3 id="pmr-enabled-containers">pmr-enabled Containers</h3>

<p>C++17 provides std::pmr versions of most standard containers in the std::pmr namespace:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span>      <span class="n">v</span><span class="p">(</span><span class="o">&amp;</span><span class="n">resource</span><span class="p">);</span>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">string</span>           <span class="nf">s</span><span class="p">(</span><span class="o">&amp;</span><span class="n">resource</span><span class="p">);</span>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="kt">double</span><span class="o">&gt;</span> <span class="n">m</span><span class="p">(</span><span class="o">&amp;</span><span class="n">resource</span><span class="p">);</span>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">unordered_set</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">set</span><span class="p">(</span><span class="o">&amp;</span><span class="n">resource</span><span class="p">);</span>

</code></pre></div></div>

<p>These all use <code class="language-plaintext highlighter-rouge">std::pmr::polymorphic_allocator</code> by default.</p>

<h2 id="stdpmr-is-about-control--predictibility">std::pmr is about Control &amp; Predictibility</h2>

<p>If you build embedded software long enough, you eventually hit the same trade-off:
you want the convenience of <code class="language-plaintext highlighter-rouge">std::vector</code>, <code class="language-plaintext highlighter-rouge">std::string</code>, and <code class="language-plaintext highlighter-rouge">std::map</code>, but you
do not want unpredictable heap behavior in the middle of a timing-critical path.</p>

<p>That is the space <code class="language-plaintext highlighter-rouge">std::pmr</code> fills.</p>

<p>PMR does not replace the standard library. It gives you a way to keep standard
containers while taking ownership of where their memory comes from. That matters
when the question is not “can this code run?” but “can this code run within a
bounded budget, every time?”</p>

<pre><code class="language-mermaid">flowchart LR
    A[Embedded workload] --&gt; B{Need dynamic containers?}
    B --&gt;|No| C[Static storage]
    B --&gt;|Yes| D{Need predictable memory?
}
    D --&gt;|Yes| E[std::pmr + bounded resource]
    D --&gt;|No| F[std::allocator or custom allocator]
</code></pre>

<p>The practical message is simple:</p>

<ul>
  <li>Use static storage when the shape of the problem is fixed.</li>
  <li>Use PMR when the shape is dynamic, but the memory budget is still bounded.</li>
  <li>Use a custom allocator when the allocator type is known at compile time and you
want the lowest overhead.</li>
</ul>

<h2 id="quick-embedded-background-for-non-embedded-readers">Quick embedded background (for non-embedded readers)</h2>

<p>If you come from backend or desktop development, the core embedded difference is
that timing and memory behavior are product requirements, not implementation
details.</p>

<p>In many embedded systems:</p>

<ul>
  <li>RAM is small and fixed for the life of the process.</li>
  <li>There is no virtual memory safety net.</li>
  <li>A long-tail latency spike can violate a control-loop deadline.</li>
  <li>Memory fragmentation can cause failures long after startup.</li>
</ul>

<p>That is why teams often distinguish:</p>

<ul>
  <li>Mean performance: useful for throughput.</li>
  <li>Tail latency (p95/p99) and jitter: critical for deadline reliability.</li>
</ul>

<p>This post focuses on that second category: reducing unpredictable behavior when
containers allocate memory in timing-sensitive code.</p>

<h2 id="why-embedded-engineers-care">Why embedded engineers care</h2>

<p>On desktop systems, allocation latency is usually background noise. In embedded
systems, it is often part of the product requirement.</p>

<p>Typical constraints look like this:</p>

<ul>
  <li>No heap at all in safety-critical paths.</li>
  <li>Strict latency budgets for control loops, protocol handlers, and sensor fusion.</li>
  <li>Small RAM footprints where fragmentation matters.</li>
  <li>A preference for failure you can detect early rather than failure that appears
later under load.</li>
</ul>

<p>PMR is useful because it lets you make memory behavior explicit. Instead of
sprinkling allocation policy across types and APIs, you pass a resource into the
place that needs it.</p>

<h2 id="what-pmr-actually-changes">What PMR actually changes</h2>

<p><strong>With ordinary containers, the allocator is usually baked into the type.</strong></p>

<p><strong>With PMR containers, the resource is supplied at runtime.</strong></p>

<p>That means the code that owns the workload can decide whether memory comes from a
stack buffer, an arena, a pool, or a custom resource tuned for the device.</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">char</span> <span class="n">buffer</span><span class="p">[</span><span class="mi">8192</span><span class="p">];</span>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">monotonic_buffer_resource</span> <span class="n">arena</span><span class="p">{</span>
    <span class="n">buffer</span><span class="p">,</span>
    <span class="k">sizeof</span><span class="p">(</span><span class="n">buffer</span><span class="p">),</span>
    <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">null_memory_resource</span><span class="p">()</span>
<span class="p">};</span>

<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">SensorReading</span><span class="o">&gt;</span> <span class="n">readings</span><span class="p">{</span><span class="o">&amp;</span><span class="n">arena</span><span class="p">};</span>
<span class="n">readings</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="mi">64</span><span class="p">);</span>

<span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="n">sample</span> <span class="o">:</span> <span class="n">samples</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">readings</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">sample</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Two details matter here:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">reserve()</code> is still important when you already know the likely size.</li>
  <li><code class="language-plaintext highlighter-rouge">std::pmr::null_memory_resource()</code> is a good guardrail when you want overflow
to fail loudly instead of quietly falling back to the heap.</li>
</ol>

<h2 id="where-pmr-fits-best">Where PMR fits best</h2>

<p>PMR is strongest when the workload is local, bounded, and repeatable.</p>

<h3 id="1-request-scoped-processing">1. Request-scoped processing</h3>

<p>Parsing a packet, formatting a response, or building a temporary AST are all good
examples.</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">handle_packet</span><span class="p">(</span><span class="k">const</span> <span class="n">Packet</span><span class="o">&amp;</span> <span class="n">packet</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">char</span> <span class="n">scratch</span><span class="p">[</span><span class="mi">4096</span><span class="p">];</span>
    <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">monotonic_buffer_resource</span> <span class="n">arena</span><span class="p">{</span>
        <span class="n">scratch</span><span class="p">,</span>
        <span class="k">sizeof</span><span class="p">(</span><span class="n">scratch</span><span class="p">),</span>
        <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">null_memory_resource</span><span class="p">()</span>
    <span class="p">};</span>

    <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">string</span> <span class="n">payload</span><span class="p">{</span><span class="o">&amp;</span><span class="n">arena</span><span class="p">};</span>
    <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">Field</span><span class="o">&gt;</span> <span class="n">fields</span><span class="p">{</span><span class="o">&amp;</span><span class="n">arena</span><span class="p">};</span>

    <span class="n">parse</span><span class="p">(</span><span class="n">packet</span><span class="p">,</span> <span class="n">payload</span><span class="p">,</span> <span class="n">fields</span><span class="p">);</span>
    <span class="n">process</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="n">fields</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The lifetime matches the scope. You get predictable cleanup and no per-object free
logic.</p>

<h3 id="2-bounded-buffers-in-real-time-code">2. Bounded buffers in real-time code</h3>

<p>When a task has a known upper bound, PMR lets you express that bound directly in
code.</p>

<p>That is much more useful than “hope the heap behaves today.”</p>

<h3 id="3-systems-that-need-runtime-allocator-choice">3. Systems that need runtime allocator choice</h3>

<p>If the same code path sometimes uses a DMA buffer, sometimes shared memory, and
sometimes an arena in RAM, PMR gives you one interface for all of it.</p>

<p>That is especially handy in embedded frameworks where memory policy belongs to the
platform layer, not each individual container type.</p>

<h2 id="what-pmr-does-not-solve">What PMR does not solve</h2>

<p>PMR is not a universal upgrade.</p>

<p>It is not the right answer if:</p>

<ul>
  <li>You can fully size the data structure at compile time.</li>
  <li>You need the absolute lowest overhead and the allocator is known ahead of time.</li>
  <li>You need frequent individual frees across a long-lived structure.</li>
  <li>You want a general-purpose container replacement that improves every workload.</li>
</ul>

<p>If the allocator is known at compile time, a custom allocator template is often a
better fit than PMR.</p>

<p>If the data structure is fixed-size, a static buffer or <code class="language-plaintext highlighter-rouge">boost::static_vector</code>
style approach can be simpler and faster.</p>

<p>If the workload is long-lived and fragmented, a pool resource or a purpose-built
allocator may be better than a monotonic arena.</p>

<h2 id="the-embedded-mental-model">The embedded mental model</h2>

<p>The biggest benefit of PMR is not raw speed. It is that memory policy becomes a
first-class design decision.</p>

<p>That gives you three things embedded teams care about:</p>

<ul>
  <li>Predictable failure mode when memory is exhausted.</li>
  <li>Cleaner separation between algorithm and memory source.</li>
  <li>Easier tuning per subsystem without rewriting container-heavy code.</li>
</ul>

<pre><code class="language-mermaid">flowchart TB
    A[Subsystem] --&gt; B[Container logic]
    A --&gt; C[Memory policy]
    C --&gt; D[Stack buffer]
    C --&gt; E[Arena]
    C --&gt; F[Pool]
    C --&gt; G[DMA / shared memory]

    style A fill:#74c0fc,stroke:#1971c2
    style C fill:#ffe066,stroke:#f08c00
</code></pre>

<p>That separation is the real value proposition. PMR makes it possible to say:</p>

<ul>
  <li>this path may use memory, but only from here,</li>
  <li>this path must never hit the general heap,</li>
  <li>this path can fail early if the budget is wrong.</li>
</ul>

<h2 id="a-practical-buffer-sizing-strategy">A practical buffer sizing strategy</h2>

<p>The most common mistake with PMR is not PMR itself. It is undersizing the buffer.</p>

<p>For monotonic resources, the buffer needs room for the working set plus growth.
If you let a container grow and the arena is too small, the upstream resource will
be used unless you deliberately block that fallback.</p>

<p>That means the safe workflow is:</p>

<ol>
  <li>Measure the peak memory use of the workload.</li>
  <li>Add headroom for growth and allocator metadata.</li>
  <li>Use <code class="language-plaintext highlighter-rouge">null_memory_resource()</code> in tests to catch bad sizing early.</li>
  <li>Only allow fallback if the product really wants it.</li>
</ol>

<p>Rule of thumb:</p>

<ul>
  <li>Monotonic buffers: peak workload usage + margin.</li>
  <li>Pool resources: expected concurrent allocations and size classes.</li>
  <li>Safety-critical code: fail fast on exhaustion, then handle it explicitly.</li>
</ul>

<h2 id="how-to-think-about-performance">How to think about performance</h2>

<p>If you benchmark PMR incorrectly, you can make it look bad even when the design
is sound.</p>

<p>The common traps are:</p>

<ul>
  <li>benchmark code that accidentally falls back to the heap,</li>
  <li>omitted <code class="language-plaintext highlighter-rouge">reserve()</code> in a workload that already knows the size,</li>
  <li>debug builds or unoptimized binaries,</li>
  <li>comparing the wrong thing, such as “PMR” versus “std” instead of allocator
policy versus allocator policy.</li>
</ul>

<p>The useful conclusion is not “PMR is always faster” or “PMR is always slower.”
It is this:</p>

<ul>
  <li>PMR makes memory behavior explicit.</li>
  <li>Explicit memory behavior is valuable in embedded systems.</li>
  <li>Whether that is worth the overhead depends on the workload.</li>
</ul>

<h2 id="the-failure-mode-that-matters-most">The failure mode that matters most</h2>

<p>In embedded software, the worst outcome is often not a crash. It is a silent
fallback to a slower or less predictable path that still appears to work.</p>

<p>With PMR, that can happen if a monotonic resource runs out of space and is allowed
to fall back to an upstream allocator. Your code may still succeed, but now you
are timing the wrong thing.</p>

<p>That is why the fail-fast pattern matters:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">char</span> <span class="n">buffer</span><span class="p">[</span><span class="mi">8192</span><span class="p">];</span>
<span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">monotonic_buffer_resource</span> <span class="n">arena</span><span class="p">{</span>
  <span class="n">buffer</span><span class="p">,</span>
  <span class="k">sizeof</span><span class="p">(</span><span class="n">buffer</span><span class="p">),</span>
  <span class="n">std</span><span class="o">::</span><span class="n">pmr</span><span class="o">::</span><span class="n">null_memory_resource</span><span class="p">()</span>
<span class="p">};</span>
</code></pre></div></div>

<p>If the buffer is too small, the failure happens where you can see it, instead of
quietly changing the timing model.</p>

<h2 id="a-stricter-option-remove-the-heap-from-the-path-entirely">A stricter option: remove the heap from the path entirely</h2>

<p>Some embedded teams go further and treat heap usage as a design error in critical
paths.</p>

<p>That can mean:</p>

<ul>
  <li>banning <code class="language-plaintext highlighter-rouge">new</code>/<code class="language-plaintext highlighter-rouge">delete</code> in selected modules,</li>
  <li>reviewing allocator usage at the API boundary,</li>
  <li>and forcing all dynamic storage through an approved resource or static policy.</li>
</ul>

<p>PMR fits well into that style because it makes memory sourcing explicit. You can
route requests to a bounded arena, a pool, or a device-specific resource instead of
letting each container decide implicitly.</p>

<p>The important point is not that PMR eliminates the heap forever. It is that PMR
lets you decide where heap-like behavior is allowed.</p>

<h2 id="pmr-versus-other-embedded-options">PMR versus other embedded options</h2>

<p>PMR is only one answer. In many codebases the real choice is between three
approaches:</p>

<table>
  <thead>
    <tr>
      <th>Approach</th>
      <th>Strength</th>
      <th>Trade-off</th>
      <th>Best fit</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">std::vector</code> / <code class="language-plaintext highlighter-rouge">std::string</code></td>
      <td>Familiar, fast, minimal ceremony</td>
      <td>Memory policy is implicit</td>
      <td>General-purpose code</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">std::pmr</code> containers</td>
      <td>Runtime-selected bounded resources</td>
      <td>Slight extra abstraction and tuning</td>
      <td>Embedded subsystems with known memory budgets</td>
    </tr>
    <tr>
      <td>Fixed-capacity containers / ETL-style containers</td>
      <td>Fully bounded, no heap dependency</td>
      <td>Less flexibility, fixed maximum size</td>
      <td>Hard real-time and very constrained systems</td>
    </tr>
  </tbody>
</table>

<p>If you already know the maximum size at compile time, a fixed-capacity container
can be the cleanest answer. If you need some flexibility but still want to control
where memory comes from, PMR is the middle ground.</p>

<h2 id="a-simple-decision-tree">A simple decision tree</h2>

<pre><code class="language-mermaid">flowchart TD
  A[Need container-like storage?] --&gt; B{Is the size fixed at compile time?}
  B --&gt;|Yes| C[Use static or fixed-capacity containers]
  B --&gt;|No| D{Need runtime control over memory source?}
  D --&gt;|No| E[Use std containers or a custom allocator]
  D --&gt;|Yes| F{Need bounded / fail-fast allocation?}
  F --&gt;|Yes| G[Use std::pmr with null upstream or bounded arena]
  F --&gt;|No| H[Use PMR with an appropriate upstream resource]
</code></pre>

<p>This is the simplest way to think about the design space:</p>

<ul>
  <li>fixed size: static wins,</li>
  <li>known allocator type: custom allocator wins,</li>
  <li>runtime-selected bounded memory: PMR wins.</li>
</ul>

<h2 id="why-i-used-a-custom-benchmark-harness-instead-of-google-benchmark">Why I used a custom benchmark harness instead of Google Benchmark</h2>

<p>While Google Benchmark is excellent for measuring average throughput, embedded and real-time systems often prioritize tail latency (p95/p99), jitter, and predictable failure modes—metrics that aren’t its primary focus.
For this analysis, I needed a harness that:</p>

<p>Explicitly tracks tail behavior (p95/p99 latency, variance across runs).
Simulates memory exhaustion (e.g., using null_memory_resource() to test bounded buffers).
Generates plots for visualizing trade-offs between speed, consistency, and memory policy.
This approach aligns with the needs of embedded engineers, where worst-case behavior matters more than average performance.</p>

<h2 id="the-benchmark-plots-and-what-they-indicate">The Benchmark plots and what they indicate</h2>

<p><img src="/cover_images/posts/pmr_plots-2/speedup_summary.webp" alt="Overall speedup summary" />
<em>What this indicates: PMR is workload-dependent. In some paths it improves both speed and jitter, in others it mainly improves predictability, and in some it can lose on both metrics.</em></p>

<h3 id="vector-workload">Vector workload</h3>

<p><img src="/cover_images/posts/pmr_plots-2/vector_push_back_comparison.webp" alt="Vector push_back comparison" />
<em>What this indicates: with correctly sized PMR resources, monotonic PMR can be very competitive and often faster for this bounded push-heavy pattern.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/vector_push_back_percentiles.webp" alt="Vector push_back percentiles" />
<em>What this indicates: percentile spread shows tail behavior. The tighter PMR spread here reflects fewer outlier spikes when memory is pre-bounded.</em></p>

<h3 id="string-workload">String workload</h3>

<p><img src="/cover_images/posts/pmr_plots-2/string_concat_comparison.webp" alt="String concatenation comparison" />
<em>What this indicates: string-heavy code can benefit from PMR by reducing allocation churn in repeated temporary construction.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/string_concat_percentiles.webp" alt="String concatenation percentiles" />
<em>What this indicates: PMR often improves p95 and p99 consistency for concat paths, which matters more than mean in deadline-driven systems.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/string_sso_comparison.webp" alt="String SSO comparison" />
<em>What this indicates: for short-string dominated paths, PMR and std can be close in mean time, so jitter and memory-policy needs become the deciding factor.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/string_sso_percentiles.webp" alt="String SSO percentiles" />
<em>What this indicates: even when averages are similar, percentile tails reveal which allocator path is more stable under repeated short-lived string churn.</em></p>

<h3 id="map-workload">Map workload</h3>

<p><img src="/cover_images/posts/pmr_plots-2/map_int_key_comparison.webp" alt="Map int-key comparison" />
<em>What this indicates: PMR can help ordered-map int-key inserts, but gains depend on node allocation pattern and resource choice.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/map_int_key_percentiles.webp" alt="Map int-key percentiles" />
<em>What this indicates: map tail latency can diverge from average behavior, so p95/p99 should drive allocator decisions for registry-like structures.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/map_string_key_comparison.webp" alt="Map string-key comparison" />
<em>What this indicates: string-key maps are mixed; allocator policy and string storage behavior dominate results, so measure this path specifically in your firmware.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/map_string_key_percentiles.webp" alt="Map string-key percentiles" />
<em>What this indicates: string-heavy node allocations can amplify outliers, and percentile plots show whether PMR is helping or hurting worst-case latency.</em></p>

<h3 id="mixed-realistic-workload">Mixed realistic workload</h3>

<p><img src="/cover_images/posts/pmr_plots-2/sensor_collection_comparison.webp" alt="Sensor collection comparison" />
<em>What this indicates: in realistic container mixes, PMR can reduce both runtime and jitter when working sets are bounded and reused per task/scope.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/sensor_collection_percentiles.webp" alt="Sensor collection percentiles" />
<em>What this indicates: this view highlights deadline risk directly. Smaller p95 to p99 gaps mean fewer long-tail surprises in data-collection loops.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/message_queue_comparison.webp" alt="Message queue comparison" />
<em>What this indicates: this is the profile to watch for embedded messaging paths. PMR can lower long-tail latency by avoiding general heap behavior during bursts.</em></p>

<p><img src="/cover_images/posts/pmr_plots-2/message_queue_percentiles.webp" alt="Message queue percentiles" />
<em>What this indicates: queue workloads are often bursty; the percentile chart confirms whether PMR is tightening tail latency where it matters operationally.</em></p>

<h2 id="when-i-would-recommend-stdpmr">When I would recommend STD::PMR</h2>

<p>Use PMR when you need one or more of the following:</p>

<ul>
  <li>A bounded, non-fragmenting allocation model for a subsystem.</li>
  <li>Runtime selection of memory source.</li>
  <li>Cleaner APIs for request-scoped or task-scoped work.</li>
  <li>Standard containers without taking a dependency on the global heap.</li>
  <li>Easier testing of memory exhaustion and fallback paths.</li>
</ul>

<h2 id="when-i-would-not">When I would not</h2>

<p>Do not reach for PMR just because it sounds modern.</p>

<p>If your use case is fixed-size, a static container or custom structure is often
more direct.</p>

<p>If the allocator is part of the type and never changes, a custom allocator is
usually cheaper.</p>

<p>If you need a high-throughput general-purpose container, the default STL path may
still be the best choice.</p>

<h2 id="bottom-line">Bottom line</h2>

<p>PMR is not a performance gimmick.</p>

<p>It is a way to bring memory policy under control in code that still wants the
expressiveness of standard containers.</p>

<p>For embedded engineers, that matters because the hard part is often not writing
the algorithm. It is making sure the algorithm runs within a fixed memory and
latency envelope every single time.</p>

<p>If you already know the exact allocator at compile time, use a custom allocator.
If the data is fixed-size, use static storage.
If you need runtime control over bounded memory, PMR is the middle ground worth
considering.</p>

<h2 id="references-and-code">References and code</h2>

<p>The benchmark code and plots that informed this post are in the companion repo:
<a href="https://github.com/saptarshi-max/pmr-benchmark">pmr-benchmark</a></p>]]></content><author><name>{&quot;picture&quot;=&gt;&quot;/assets/img/me3.jpg&quot;, &quot;social&quot;=&gt;{&quot;twitter&quot;=&gt;&quot;Saptarshi108&quot;, &quot;kaggle&quot;=&gt;&quot;https://www.kaggle.com/saptarshinag01&quot;}}</name></author><category term="blog" /><category term="cppdev" /><category term="c++" /><summary type="html"><![CDATA[A practical embedded-first guide to std::pmr: what it solves, where it fits, how to size buffers safely, and when to prefer a custom allocator or plain static storage instead.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="/cover_images/std-pmr.webp" /><media:content medium="image" url="/cover_images/std-pmr.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>